{
  "slug": "lucid.quantization",
  "name": "quantization",
  "path": "lucid.quantization",
  "kind": "module",
  "source": "https://github.com/ChanLumerico/lucid/blob/4b22d32cc/lucid/quantization/__init__.py#L1",
  "members": [
    {
      "name": "dequantize",
      "path": "lucid.quantization._functional.dequantize",
      "kind": "function",
      "labels": [],
      "signature": "dequantize(q: Tensor, scale: _ScaleLike, zero_point: _ZeroPointLike, ch_axis: int | None = None)",
      "source": "https://github.com/ChanLumerico/lucid/blob/4b22d32cc/lucid/quantization/_functional.py#L176",
      "subcategory": "functional",
      "summary": "Decode integer codes back to a `float32` approximation of the original.",
      "extended": "`dequantize` is the inverse of `quantize` — the *read* side of the\naffine map. It undoes the offset and scaling to recover a floating-point\ntensor from the stored integer codes. It is the operation that lets a\nquantized weight participate in an ordinary float matmul: the sidecar-design\nquantized modules keep their weight as int8 codes, then call `dequantize`\non every forward to reconstruct a float weight before the GEMM. The result\nis not the exact original — it is the original rounded to the nearest grid\npoint — but the error per element is bounded by `s / 2`.\n\nThe decode is the affine inverse:\n\n$$\n\\hat{x} = (q - z)\\, s\n$$\n\nwhere the subtraction removes the `zero_point` offset `z` and the\nmultiply restores the physical step `s`. As in `quantize`, a\nper-channel `scale` / `zero_point` (given `ch_axis`) is reshaped to\nbroadcast along the channel axis, so each channel is decoded with its own\nstep. The whole computation is a subtract-then-multiply on the public\n`lucid.*` op surface (H4), identical on the Accelerate and MLX streams.",
      "parameters": [
        {
          "name": "q",
          "annotation": "Tensor",
          "description": "Integer codes previously produced by `quantize` (or an equivalent\nencoder). Cast up to `float32` internally before the affine decode.",
          "default": null
        },
        {
          "name": "scale",
          "annotation": "Tensor or float",
          "description": "The same quantization step `s` used to produce `q`. A length-`C`\ntensor for per-channel; a scalar for per-tensor.",
          "default": null
        },
        {
          "name": "zero_point",
          "annotation": "Tensor, float, or int",
          "description": "The same zero-point offset `z` used to produce `q`.",
          "default": null
        },
        {
          "name": "ch_axis",
          "annotation": "int",
          "description": "Channel axis for per-channel decode; `None` (default) uses a single\nscalar `scale` / `zero_point` for the whole tensor.",
          "default": "None"
        }
      ],
      "returns": {
        "annotation": "Tensor",
        "description": "A `float32` reconstruction of the original tensor, the same shape as\n`q`."
      },
      "raises": [],
      "examples": [
        ">>> import lucid\n>>> import lucid.quantization as Q\n>>> x = lucid.tensor([-1.0, 0.0, 0.5, 2.0])\n>>> q = Q.quantize(x, scale=0.05, zero_point=0, qdtype=Q.qint8)\n>>> x_hat = Q.dequantize(q, scale=0.05, zero_point=0)\n>>> bool((lucid.abs(x_hat - x) <= 0.05).all().item())   # within one step\nTrue\nPer-channel decode mirrors the encode axis exactly:\n>>> w = lucid.randn(4, 8)\n>>> s, zp = lucid.ones(4) * 0.02, lucid.zeros(4)\n>>> codes = Q.quantize(w, s, zp, Q.qint8, ch_axis=0)\n>>> Q.dequantize(codes, s, zp, ch_axis=0).shape\n(4, 8)"
      ],
      "notes": [
        "- Lossy by construction — `dequantize(quantize(x)) ≈ x` only up to the\n  grid resolution; values that saturated during encode are *not* recovered.\n- Must be called with the **same** `scale` / `zero_point` / `ch_axis`\n  used to encode; mismatched qparams silently produce wrong magnitudes.\n- Output is always `float32` regardless of the input storage dtype, and\n  the op carries no straight-through gradient (it is a plain composite, not\n  a custom autograd Function)."
      ],
      "attributes": [],
      "warns": [],
      "see_also": [
        {
          "name": "lucid.quantization.quantize",
          "description": "The forward affine encode `round(x/s) + z`."
        },
        {
          "name": "lucid.quantization.fake_quantize",
          "description": "Fused encode→decode with an STE gradient."
        },
        {
          "name": "lucid.nn.quantized.Linear",
          "description": "Uses `dequantize` on its int8 weight each forward."
        }
      ]
    },
    {
      "name": "fake_quantize",
      "path": "lucid.quantization._functional.fake_quantize",
      "kind": "function",
      "labels": [],
      "signature": "fake_quantize(x: Tensor, scale: _ScaleLike, zero_point: _ZeroPointLike, quant_min: int, quant_max: int, ch_axis: int | None = None)",
      "source": "https://github.com/ChanLumerico/lucid/blob/4b22d32cc/lucid/quantization/_functional.py#L315",
      "subcategory": "functional",
      "summary": "Simulate quantization in the float domain with a straight-through gradient.",
      "extended": "`fake_quantize` composes `quantize` and `dequantize` into a\nsingle float→float round-trip: it quantizes `x` to the integer grid and\nimmediately decodes it back, returning a `float32` tensor that carries the\n*rounding error* quantization would introduce while staying differentiable.\nThat is what makes quantization-aware training (QAT) possible — a network can\n*feel* the low-precision grid on every forward, and thus learn weights that\nare robust to it, while gradients continue to flow and the optimizer keeps\nworking in float. It is the operation behind `FakeQuantize`, every\n`nn.qat` layer, and the output re-quantization step of the sidecar\nquantized inference modules.\n\nThe forward is the encode→decode round-trip:\n\n$$\n\\hat{x} = \\bigl(\\operatorname{clip}(\\operatorname{round}(x/s) + z,\\\n    q_{\\min},\\ q_{\\max}) - z\\bigr)\\, s\n$$\n\nThe round has no useful derivative, so the backward is the **straight-through\nestimator** (STE): the gradient passes unchanged where the pre-clip code\nlands inside the grid and is zeroed where the value saturates:\n\n$$\n\\frac{\\partial \\hat{x}}{\\partial x} =\n\\begin{cases}\n    1 & q_{\\min} \\le \\operatorname{round}(x/s) + z \\le q_{\\max} \\\\\n    0 & \\text{otherwise}\n\\end{cases}\n$$\n\nBecause of that custom backward it is implemented as a\n`lucid.autograd.Function` rather than a plain composite; only `x`\nis registered as a differentiable input (the qparams / bounds are passed as\nkeyword arguments), so a single gradient edge flows back to `x` alone.",
      "parameters": [
        {
          "name": "x",
          "annotation": "Tensor",
          "description": "Real-valued input; may require grad (this is the QAT training path).",
          "default": null
        },
        {
          "name": "scale",
          "annotation": "Tensor or float",
          "description": "Quantization step `s` (typically produced by an observer / learnable\nin QAT). A length-`C` tensor for per-channel.",
          "default": null
        },
        {
          "name": "zero_point",
          "annotation": "Tensor, float, or int",
          "description": "Integer zero-point offset `z`.",
          "default": null
        },
        {
          "name": "quant_min",
          "annotation": "int",
          "description": "Inclusive integer grid bounds (e.g. `0, 255` for `quint8`, `-128,\n127` for `qint8`); values rounding outside this range saturate.",
          "default": null
        },
        {
          "name": "quant_max",
          "annotation": "int",
          "description": "Inclusive integer grid bounds (e.g. `0, 255` for `quint8`, `-128,\n127` for `qint8`); values rounding outside this range saturate.",
          "default": null
        },
        {
          "name": "ch_axis",
          "annotation": "int",
          "description": "Channel axis for per-channel fake-quant; `None` (default) uses a\nsingle scalar `scale` / `zero_point` for the whole tensor.",
          "default": "None"
        }
      ],
      "returns": {
        "annotation": "Tensor",
        "description": "A fake-quantized `float32` tensor, the same shape as `x`, carrying\nthe STE gradient described above."
      },
      "raises": [],
      "examples": [
        ">>> import lucid\n>>> import lucid.quantization as Q\n>>> x = lucid.tensor([0.02, 0.31, 0.77], requires_grad=True)\n>>> y = Q.fake_quantize(x, scale=0.1, zero_point=0, quant_min=0, quant_max=255)\n>>> y.shape\n(3,)\nSTE in action — the gradient is 1 in-range and 0 where the input\nsaturates past quant_max:\n>>> x = lucid.tensor([0.5, 99.0], requires_grad=True)   # 99.0 rounds past 255*0.1\n>>> Q.fake_quantize(x, scale=0.1, zero_point=0, quant_min=0, quant_max=255).sum().backward()\n>>> [float(g) for g in x.grad]\n[1.0, 0.0]"
      ],
      "notes": [
        "- Output stays `float32` — nothing is physically stored as int here. Use\n  `quantize` when you actually want integer codes; use this when you\n  want the *effect* of quantization inside a differentiable graph.\n- The STE saturation mask is computed from the **pre-clip** code, so it\n  correctly zeroes the gradient for inputs that round to exactly the grid\n  edge from outside.\n- At inference the round-trip makes a module's output *bit-equivalent* to a\n  true integer kernel without needing one — the reason the sidecar\n  `lucid.nn.quantized.Linear` fake-quantizes its output.\n- Runs on the public `lucid.*` op surface (H4), so the STE is identical on\n  the Accelerate and MLX streams."
      ],
      "attributes": [],
      "warns": [],
      "see_also": [
        {
          "name": "lucid.quantization.quantize",
          "description": "The (non-differentiable) integer encode."
        },
        {
          "name": "lucid.quantization.dequantize",
          "description": "The affine decode half of the round-trip."
        },
        {
          "name": "lucid.quantization.FakeQuantize",
          "description": "Observer-driven module wrapper of this op."
        },
        {
          "name": "lucid.quantization.prepare_qat",
          "description": "Inserts fake-quant layers for QAT."
        }
      ]
    },
    {
      "name": "quantize",
      "path": "lucid.quantization._functional.quantize",
      "kind": "function",
      "labels": [],
      "signature": "quantize(x: Tensor, scale: _ScaleLike, zero_point: _ZeroPointLike, qdtype: QDtype, ch_axis: int | None = None)",
      "source": "https://github.com/ChanLumerico/lucid/blob/4b22d32cc/lucid/quantization/_functional.py#L57",
      "subcategory": "functional",
      "summary": "Encode a real tensor into integer codes on an affine quantization grid.",
      "extended": "`quantize` is the forward half of the affine quantization map and the\nlowest-level primitive in the whole subsystem — observers, quantized\nmodules, and QAT all bottom out here. Given a real tensor `x` and an\nalready-chosen `(scale, zero_point)` pair, it snaps every value to the\nnearest lattice point of the integer grid described by `qdtype` and\nreturns the integer *codes* that physically get stored. This is the step\nthat actually shrinks memory: a `float32` weight becomes an int8 (or int4)\ncode tensor roughly `4x` (or `8x`) smaller, with the tiny `scale` /\n`zero_point` sidecar carried alongside.\n\nThe map is affine — real values are divided by the step `s`, offset by the\ninteger `zero_point` `z`, rounded to the nearest integer, and clamped\ninto the representable `[quant_min, quant_max]` range before the storage\ncast:\n\n$$\nq = \\operatorname{clip}\\!\\bigl(\n    \\operatorname{round}(x / s) + z,\\ q_{\\min},\\ q_{\\max}\\bigr)\n$$\n\nRounding and clipping happen in the **float domain**; only the final cast\nlands in `qdtype.storage`. That ordering is deliberate — it keeps the path\non the public `lucid.*` op surface (no external libraries, H4) so it is\nbit-for-bit identical on the Accelerate (CPU) and MLX (GPU) streams, and it\nsidesteps the missing CPU integer-clamp kernel. Pass `ch_axis` to quantize\nper channel: a length-`C` `scale` / `zero_point` vector is reshaped to\nbroadcast along that axis, giving each channel its own step — the\naccuracy-preserving default for conv / linear weights.",
      "parameters": [
        {
          "name": "x",
          "annotation": "Tensor",
          "description": "Real-valued input tensor to encode.",
          "default": null
        },
        {
          "name": "scale",
          "annotation": "Tensor or float",
          "description": "Quantization step `s` — the width of one code interval. A Python float\n(or scalar tensor) for per-tensor; a length-`C` tensor when `ch_axis`\nis given.",
          "default": null
        },
        {
          "name": "zero_point",
          "annotation": "Tensor, float, or int",
          "description": "Integer offset `z` mapping the real value `0` onto a code, so zero is\nrepresented exactly (matters for zero-padded tensors).",
          "default": null
        },
        {
          "name": "qdtype",
          "annotation": "QDtype",
          "description": "Target grid descriptor — supplies `quant_min` / `quant_max` and the\n`storage` dtype the codes are cast to (`qint8` = `[-128, 127]` in\n`int8`, `quint8` = `[0, 255]`, `qint4` = `[-8, 7]`).",
          "default": null
        },
        {
          "name": "ch_axis",
          "annotation": "int",
          "description": "Channel axis for per-channel quantization; `None` (default) quantizes\nthe whole tensor with a single scalar `scale` / `zero_point`.",
          "default": "None"
        }
      ],
      "returns": {
        "annotation": "Tensor",
        "description": "Integer codes with dtype `getattr(lucid, qdtype.storage)` and the same\nshape as `x`."
      },
      "raises": [],
      "examples": [
        ">>> import lucid\n>>> import lucid.quantization as Q\n>>> x = lucid.tensor([-1.0, 0.0, 0.5, 2.0])\n>>> codes = Q.quantize(x, scale=0.05, zero_point=0, qdtype=Q.qint8)\n>>> codes.shape\n(4,)\nValues outside the grid saturate at quant_min / quant_max rather than\nwrapping — a common gotcha when scale is chosen too small:\n>>> hot = Q.quantize(lucid.tensor([100.0]), scale=0.05, zero_point=0, qdtype=Q.qint8)\n>>> int(hot.item())            # clamped to qint8's max, not 2000\n127\nPer-channel encode of a (out, in) weight — one step per output row:\n>>> w = lucid.randn(4, 8)\n>>> s = lucid.ones(4) * 0.02   # a scale for each output channel\n>>> zp = lucid.zeros(4)\n>>> Q.quantize(w, s, zp, Q.qint8, ch_axis=0).shape\n(4, 8)"
      ],
      "notes": [
        "- Pure encode with **no gradient** — it emits an integer tensor and is not\n  meant to sit inside a differentiable path. For a differentiable\n  simulate-quantization op (used by QAT), use `fake_quantize`, which\n  adds a straight-through estimator.\n- `quint8` currently maps to `int16` storage (the engine has no\n  `uint8`) and `qint4` packs into `int8`; codes stay within the grid\n  bounds regardless of the wider physical dtype.\n- Round-tripping `quantize` → `dequantize` is lossy by the rounding\n  step; the per-element reconstruction error is at most `s / 2`.\n- Device-agnostic: there is no stream-specific branch here, so CPU and Metal\n  produce identical codes."
      ],
      "attributes": [],
      "warns": [],
      "see_also": [
        {
          "name": "lucid.quantization.dequantize",
          "description": "The inverse affine decode `(q - z) * s`."
        },
        {
          "name": "lucid.quantization.fake_quantize",
          "description": "Differentiable quantize→dequantize round-trip."
        },
        {
          "name": "lucid.quantization.QDtype",
          "description": "Grid descriptor (bounds + storage dtype)."
        }
      ]
    },
    {
      "name": "QParams",
      "path": "lucid.quantization._qparams.QParams",
      "kind": "class",
      "class_kind": "dataclass",
      "bases": [],
      "labels": [],
      "signature": "QParams(scale: Tensor, zero_point: Tensor, qscheme: QScheme, qdtype: QDtype, ch_axis: int | None = None)",
      "source": "https://github.com/ChanLumerico/lucid/blob/4b22d32cc/lucid/quantization/_qparams.py#L22",
      "subcategory": "qparams",
      "methods": [
        {
          "name": "__init__",
          "path": "lucid.quantization._qparams.QParams.__init__",
          "kind": "function",
          "labels": [],
          "signature": "__init__(scale: Tensor, zero_point: Tensor, qscheme: QScheme, qdtype: QDtype, ch_axis: int | None = None)",
          "source": "https://github.com/ChanLumerico/lucid/blob/4b22d32cc/lucid/quantization/_qparams.py#L1",
          "subcategory": "qparams",
          "summary": null,
          "extended": null,
          "parameters": [],
          "returns": {
            "annotation": "None",
            "description": ""
          },
          "raises": [],
          "examples": [],
          "notes": [],
          "attributes": [],
          "warns": [],
          "see_also": []
        }
      ],
      "summary": "Immutable bundle of everything that defines a quantized tensor's grid.",
      "extended": "A quantized tensor in Lucid's *sidecar* representation is an ordinary integer\n`lucid.Tensor` plus the metadata needed to interpret its codes as real\nnumbers. `QParams` is that metadata gathered into one frozen value object:\nthe `scale` / `zero_point` produced by calibration, together with the\n`lucid.quantization.QScheme` (grid geometry) and\n`lucid.quantization.QDtype` (grid range) they were derived under, plus the\nchannel axis for per-channel schemes.\n\nIt is the return-shape of an observer's calibration: an observer records a range,\ncalls `calculate_qparams` to get `(scale, zero_point)`, and packages the\nresult — with the scheme / dtype it used — into a `QParams` so downstream\nquantize / dequantize / fake-quant calls have a single self-describing object to\nread from.",
      "parameters": [],
      "returns": null,
      "raises": [],
      "examples": [
        ">>> import lucid.quantization as Q\n>>> scale, zp = Q.calculate_qparams(-2.0, 6.0, Q.per_tensor_affine, Q.quint8)\n>>> p = Q.QParams(scale, zp, Q.per_tensor_affine, Q.quint8)\n>>> p.qscheme.is_per_channel, p.ch_axis\n(False, None)\n>>> round(float(p.scale.item()), 4)        # (6 - (-2)) / 255\n0.0314"
      ],
      "notes": [
        "- Frozen (`@dataclass(frozen=True)`): a `QParams` is a value, safe to share\n  and compare; re-calibration produces a new instance rather than mutating one.\n- `scale` and `zero_point` always share a shape, and it is that shape (scalar vs\n  length-`C`) — not `qscheme` alone — that a kernel broadcasts against.\n- The pair is consumed by `lucid.quantization.quantize` /\n  `lucid.quantization.dequantize` /\n  `lucid.quantization.fake_quantize`, which broadcast `scale` /\n  `zero_point` along `ch_axis`."
      ],
      "attributes": [
        {
          "name": "scale",
          "annotation": "Tensor",
          "description": "The quantization step size — how many real units one integer code spans. A\nscalar for per-tensor schemes; a length-`C` vector for per-channel schemes."
        },
        {
          "name": "zero_point",
          "annotation": "Tensor",
          "description": "The integer code that real 0 maps to, stored as an integer-valued float tensor.\n0 (or the grid midpoint) for symmetric schemes; solved for under affine\nschemes. Same shape as `scale`."
        },
        {
          "name": "qscheme",
          "annotation": "QScheme",
          "description": "The mapping family (per-tensor / per-channel × affine / symmetric) the\nparameters were derived under."
        },
        {
          "name": "qdtype",
          "annotation": "QDtype",
          "description": "The target integer grid (bit width + `[quant_min, quant_max]`) the codes\nclamp to."
        },
        {
          "name": "ch_axis",
          "annotation": "int or None, default None",
          "description": "The channel axis for per-channel schemes; `None` for per-tensor."
        }
      ],
      "warns": [],
      "see_also": [
        {
          "name": "calculate_qparams",
          "description": "Derives the `scale` / `zero_point` a `QParams` holds."
        },
        {
          "name": "lucid.quantization.QScheme",
          "description": "The grid-geometry field."
        },
        {
          "name": "lucid.quantization.QDtype",
          "description": "The grid-range field."
        }
      ]
    },
    {
      "name": "calculate_qparams",
      "path": "lucid.quantization._qparams.calculate_qparams",
      "kind": "function",
      "labels": [],
      "signature": "calculate_qparams(min_val: _RangeLike, max_val: _RangeLike, qscheme: QScheme, qdtype: QDtype, eps: float = 1e-08)",
      "source": "https://github.com/ChanLumerico/lucid/blob/4b22d32cc/lucid/quantization/_qparams.py#L93",
      "subcategory": "qparams",
      "summary": "Derive `(scale, zero_point)` from an observed value range.",
      "extended": "The shared calibration math behind every observer. Given the `[min_val, max_val]`\nrange an observer recorded, it solves for the affine map\n$q = \\operatorname{round}(x / s) + z$ that packs that range into the target\ngrid `[q_\\min, q_\\max]`. The observed range is first **extended to include zero**\n(`min <- min(min, 0)`, `max <- max(max, 0)`) so that real 0 is always exactly\nrepresentable — a property the padding / masking paths rely on.\n\nThe scheme's symmetry then selects the formula:\n\n$$\n\\textbf{affine:}\\quad\ns = \\frac{\\max - \\min}{q_\\max - q_\\min},\\qquad\nz = \\operatorname{clip}\\!\\bigl(q_\\min - \\operatorname{round}(\\min / s),\\;\n    q_\\min,\\; q_\\max\\bigr)\n$$\n\n$$\n\\textbf{symmetric:}\\quad\ns = \\frac{\\max(\\lvert\\min\\rvert,\\ \\lvert\\max\\rvert)}{(q_\\max - q_\\min) / 2},\n\\qquad\nz = \\begin{cases}\n    0 & \\text{signed} \\\\\n    \\lfloor (q_\\max + q_\\min + 1)/2 \\rfloor & \\text{unsigned}\n\\end{cases}\n$$\n\n`scale` is floored at `eps` in every branch so a degenerate (constant) range\nnever yields a zero or infinite step. The whole computation is elementwise, so\nscalar `min_val` / `max_val` produce per-tensor (scalar) parameters and\nlength-`C` inputs produce per-channel (vector) parameters — the function does not\nitself branch on granularity, only on symmetry.",
      "parameters": [
        {
          "name": "min_val",
          "annotation": "Tensor or float",
          "description": "The observed minimum / maximum of the tensor being calibrated — per-tensor\nscalars, or length-`C` vectors for a per-channel scheme. Plain Python floats\nare lifted to scalar tensors.",
          "default": null
        },
        {
          "name": "max_val",
          "annotation": "Tensor or float",
          "description": "The observed minimum / maximum of the tensor being calibrated — per-tensor\nscalars, or length-`C` vectors for a per-channel scheme. Plain Python floats\nare lifted to scalar tensors.",
          "default": null
        },
        {
          "name": "qscheme",
          "annotation": "QScheme",
          "description": "The mapping family; only its `QScheme.is_symmetric` flag is consulted\nhere (granularity is implied by the shape of `min_val` / `max_val`).",
          "default": null
        },
        {
          "name": "qdtype",
          "annotation": "QDtype",
          "description": "The target grid, supplying `quant_min` / `quant_max` and `signed`.",
          "default": null
        },
        {
          "name": "eps",
          "annotation": "float",
          "description": "Lower floor on `scale` to avoid a division-by-zero step on a degenerate\n(constant-valued) range.",
          "default": "1e-8"
        }
      ],
      "returns": {
        "annotation": "(Tensor, Tensor)",
        "description": "The `scale` and `zero_point` tensors, matching the shape of the inputs\n(scalar for per-tensor, length-`C` for per-channel). `zero_point` is\ninteger-valued but carried as a float tensor."
      },
      "raises": [],
      "examples": [
        ">>> import lucid.quantization as Q\n>>> # affine quint8 over an asymmetric activation range:\n>>> s, z = Q.calculate_qparams(-2.0, 6.0, Q.per_tensor_affine, Q.quint8)\n>>> round(float(s.item()), 4), int(z.item())     # (6 - (-2)) / 255, then clip\n(0.0314, 64)\n>>> # symmetric qint8 weight — zero_point pinned to 0:\n>>> s, z = Q.calculate_qparams(-0.8, 0.5, Q.per_channel_symmetric, Q.qint8)\n>>> int(z.item())\n0\n>>> # degenerate (constant) range floors scale at eps, never 0:\n>>> s, _ = Q.calculate_qparams(3.0, 3.0, Q.per_tensor_affine, Q.qint8)\n>>> bool(s.item() > 0)\nTrue"
      ],
      "notes": [
        "- **Zero is always representable.** Extending the range to include 0 guarantees a\n  code for real 0, which matters wherever padding / masking injects exact zeros.\n- **Symmetric `zero_point`.** Signed grids pin it to 0 (so integer matmuls need no\n  weight-offset correction); unsigned grids pin it to the grid midpoint.\n- **Granularity is shape-driven.** There is no per-channel branch — passing vectors\n  in yields vectors out, so a single code path serves both granularities.\n- Mirrors the reference framework's calibration but stays entirely on Lucid tensor\n  ops (no external numeric library)."
      ],
      "attributes": [],
      "warns": [],
      "see_also": [
        {
          "name": "QParams",
          "description": "Bundles the returned `(scale, zero_point)` with its scheme / dtype."
        },
        {
          "name": "lucid.quantization.QScheme",
          "description": "Selects affine vs symmetric here."
        },
        {
          "name": "lucid.quantization.QDtype",
          "description": "Supplies `quant_min` / `quant_max` / `signed`."
        },
        {
          "name": "lucid.quantization.quantize",
          "description": "Applies the derived parameters to produce codes."
        }
      ]
    },
    {
      "name": "QDtype",
      "path": "lucid.quantization._qscheme.QDtype",
      "kind": "class",
      "class_kind": "dataclass",
      "bases": [],
      "labels": [],
      "signature": "QDtype(name: str, bits: int, signed: bool, quant_min: int, quant_max: int, storage: str)",
      "source": "https://github.com/ChanLumerico/lucid/blob/4b22d32cc/lucid/quantization/_qscheme.py#L132",
      "subcategory": "qscheme",
      "methods": [
        {
          "name": "__repr__",
          "path": "lucid.quantization._qscheme.QDtype.__repr__",
          "kind": "function",
          "labels": [],
          "signature": "__repr__()",
          "source": "https://github.com/ChanLumerico/lucid/blob/4b22d32cc/lucid/quantization/_qscheme.py#L238",
          "subcategory": "qscheme",
          "summary": "Return `\"lucid.quantization.<name>\"`.",
          "extended": null,
          "parameters": [],
          "returns": {
            "annotation": "str",
            "description": ""
          },
          "raises": [],
          "examples": [],
          "notes": [],
          "attributes": [],
          "warns": [],
          "see_also": []
        },
        {
          "name": "__init__",
          "path": "lucid.quantization._qscheme.QDtype.__init__",
          "kind": "function",
          "labels": [],
          "signature": "__init__(name: str, bits: int, signed: bool, quant_min: int, quant_max: int, storage: str)",
          "source": "https://github.com/ChanLumerico/lucid/blob/4b22d32cc/lucid/quantization/_qscheme.py#L1",
          "subcategory": "qscheme",
          "summary": null,
          "extended": null,
          "parameters": [],
          "returns": {
            "annotation": "None",
            "description": ""
          },
          "raises": [],
          "examples": [],
          "notes": [],
          "attributes": [],
          "warns": [],
          "see_also": []
        }
      ],
      "summary": "Descriptor for a quantized integer grid — bits, signedness, range, storage.",
      "extended": "Where a `QScheme` fixes the *geometry* of the affine map, a `QDtype`\npins the *target grid itself*: how many bits the codes use, whether they are\nsigned, the inclusive `[quant_min, quant_max]` range they span, and the ordinary\nLucid dtype that physically stores them. Every observer and fake-quant reads\n`quant_min` / `quant_max` off a `QDtype` to clamp its codes.\n\nUnlike the reference framework, Lucid does **not** add quantized dtypes to the C++\nengine's dtype enum; a `QDtype` is a pure-Python descriptor and the integer\ncodes live in an ordinary `storage` dtype. This keeps the whole quantization\nsubsystem off the engine's critical path until a real low-precision GEMM is wired\nin, and lets 4-bit codes ride inside `int8` until bit-packing lands.\n\nFour instances are exported as module-level constants, spanning the grids:\n\n$$\n\\mathtt{qint8}:\\ [-128,\\ 127]\\ (\\text{8-bit signed}),\\qquad\n\\mathtt{quint8}:\\ [0,\\ 255]\\ (\\text{8-bit unsigned})\n$$\n\n$$\n\\mathtt{qint4}:\\ [-8,\\ 7]\\ (\\text{4-bit signed}),\\qquad\n\\mathtt{qint32}:\\ [-2^{31},\\ 2^{31}-1]\\ (\\text{32-bit signed})\n$$\n\n* `qint8` — 8-bit **signed** (`[-128, 127]`), stored in `int8`. The default\n  **weight** grid: symmetric weights centre on 0, so a signed grid uses its full\n  range. The workhorse of int8 inference.\n* `quint8` — 8-bit **unsigned** (`[0, 255]`), stored in `int16` (the engine\n  has no `uint8`, so codes ride in the next wider signed type). The default\n  **activation** grid: post-ReLU activations are $\\ge 0$, so an unsigned grid\n  spends all 256 codes on the range that actually occurs.\n* `qint4` — 4-bit **signed** (`[-8, 7]`), stored (unpacked) in `int8` until\n  4-bit packing lands. The aggressive **weight** grid for the real MLX GEMM: about\n  2x smaller than int8 for a modest accuracy cost, used for memory-bound decode.\n* `qint32` — 32-bit **signed** (`[-2**31, 2**31 - 1]`), stored in `int32`.\n  The **accumulator** grid: int8 × int8 partial sums accumulate in int32 before\n  requantization, so it rarely appears as a weight / activation dtype.",
      "parameters": [
        {
          "name": "name",
          "annotation": "str",
          "description": "Canonical name of the grid (`\"qint8\"`, `\"quint8\"`, …); also drives\n`__repr__` and appears in module `extra_repr` strings.",
          "default": null
        },
        {
          "name": "bits",
          "annotation": "int",
          "description": "Bit width of the grid (`4` / `8` / `32`).",
          "default": null
        },
        {
          "name": "signed",
          "annotation": "bool",
          "description": "Whether codes are signed. Governs where a symmetric `zero_point` is pinned\n(0 when signed, mid-range when unsigned).",
          "default": null
        },
        {
          "name": "quant_min",
          "annotation": "int",
          "description": "Inclusive lower bound of the representable integer range.",
          "default": null
        },
        {
          "name": "quant_max",
          "annotation": "int",
          "description": "Inclusive upper bound of the representable integer range.",
          "default": null
        },
        {
          "name": "storage",
          "annotation": "str",
          "description": "Name of the ordinary Lucid dtype that physically holds the codes. `quint8`\nmaps to `int16` (no engine `uint8`); `qint4` maps to `int8` (unpacked)\nuntil 4-bit packing lands.",
          "default": null
        }
      ],
      "returns": null,
      "raises": [],
      "examples": [
        ">>> import lucid.quantization as Q\n>>> Q.qint8.bits, Q.qint8.signed, (Q.qint8.quant_min, Q.qint8.quant_max)\n(8, True, (-128, 127))\n>>> Q.quint8.signed, (Q.quint8.quant_min, Q.quint8.quant_max)\n(False, (0, 255))\n>>> Q.qint4.quant_max - Q.qint4.quant_min + 1     # 2 ** 4\n16\n>>> repr(Q.qint8)\n'lucid.quantization.qint8'"
      ],
      "notes": [
        "- **Grid width.** `quant_max - quant_min + 1 == 2 ** bits` for every instance\n  (`256` for the 8-bit grids, `16` for `qint4`).\n- **Signed / unsigned pairing with schemes.** A symmetric scheme + signed dtype\n  pins `zero_point` to 0; a symmetric scheme + *unsigned* dtype pins it to the\n  grid midpoint. Affine schemes solve for `zero_point` regardless.\n- The MLX low-precision GEMM only consumes `bits` in {4, 8}; `qint32` is an\n  accumulator descriptor, not a kernel input.\n- `repr(qint8)` is `\"lucid.quantization.qint8\"` — the canonical import path,\n  not the field dump — so the descriptor round-trips visually."
      ],
      "attributes": [
        {
          "name": "name, bits, signed, quant_min, quant_max, storage",
          "annotation": null,
          "description": "The frozen fields above; a `QDtype` is an immutable value object\n(`@dataclass(frozen=True)`), so instances can be shared and compared by value."
        }
      ],
      "warns": [],
      "see_also": [
        {
          "name": "QScheme",
          "description": "The companion descriptor fixing grid geometry (granularity / symmetry)."
        },
        {
          "name": "lucid.quantization.calculate_qparams",
          "description": "Reads `quant_min` / `quant_max` to derive"
        },
        {
          "name": "``(scale",
          "description": ""
        },
        {
          "name": "zero_point)``.",
          "description": ""
        },
        {
          "name": "lucid.quantization.QParams",
          "description": "Bundles a `QDtype` with a scheme and params."
        }
      ]
    },
    {
      "name": "QScheme",
      "path": "lucid.quantization._qscheme.QScheme",
      "kind": "class",
      "class_kind": "regular",
      "bases": [
        "Enum"
      ],
      "labels": [],
      "signature": "QScheme()",
      "source": "https://github.com/ChanLumerico/lucid/blob/4b22d32cc/lucid/quantization/_qscheme.py#L25",
      "subcategory": "qscheme",
      "methods": [
        {
          "name": "is_per_channel",
          "path": "lucid.quantization._qscheme.QScheme.is_per_channel",
          "kind": "function",
          "labels": [
            "property"
          ],
          "signature": "is_per_channel: bool",
          "source": "https://github.com/ChanLumerico/lucid/blob/4b22d32cc/lucid/quantization/_qscheme.py#L103",
          "summary": "`True` for the two per-channel schemes, `False` for per-tensor.",
          "extended": "Lets calibration / quantization code branch on granularity — a per-channel\nscheme derives one `(scale, zero_point)` per channel along `ch_axis`, so\nthe qparams are length-`C` vectors rather than scalars.",
          "parameters": [],
          "returns": {
            "annotation": "bool",
            "description": ""
          },
          "raises": [],
          "examples": [],
          "notes": [],
          "attributes": [],
          "warns": [],
          "see_also": []
        },
        {
          "name": "is_symmetric",
          "path": "lucid.quantization._qscheme.QScheme.is_symmetric",
          "kind": "function",
          "labels": [
            "property"
          ],
          "signature": "is_symmetric: bool",
          "source": "https://github.com/ChanLumerico/lucid/blob/4b22d32cc/lucid/quantization/_qscheme.py#L113",
          "summary": "`True` when `zero_point` is pinned (the symmetric schemes).",
          "extended": "A symmetric scheme fixes `zero_point` (to 0 for a signed grid, or the grid\nmidpoint for unsigned) and derives `scale` from `max(|min|, |max|)`, so\nreal 0 always maps exactly to a code; an affine scheme instead solves for a\nfree `zero_point`. See `lucid.quantization.calculate_qparams`.",
          "parameters": [],
          "returns": {
            "annotation": "bool",
            "description": ""
          },
          "raises": [],
          "examples": [],
          "notes": [],
          "attributes": [],
          "warns": [],
          "see_also": []
        }
      ],
      "summary": "How `(scale, zero_point)` map real values onto the integer grid.",
      "extended": "A *quantization scheme* fixes two independent choices about the affine map\n$q = \\operatorname{round}(x / s) + z$ that turns a real value `x` into an\ninteger code `q`:\n\n1. **Granularity** — is there one `(scale, zero_point)` for the *whole tensor*\n   (*per-tensor*), or one pair *per channel* along a chosen axis (*per-channel*)?\n   Per-channel tracks layers whose channels span very different magnitudes far more\n   tightly, at the cost of a small vector of parameters instead of a scalar.\n2. **Symmetry** — is `zero_point` *free* (*affine* / asymmetric), so the grid can\n   straddle an arbitrary `[min, max]`, or is it *pinned* (*symmetric*) so that\n   real 0 maps exactly to a fixed code and the grid is centred on zero?\n\nThe four members are the cross-product of the two useful granularities with the two\nsymmetries. Four module-level aliases in lowercase mirror the members so call sites\nread naturally, e.g. `qscheme=lucid.quantization.per_channel_symmetric`.",
      "parameters": [],
      "returns": null,
      "raises": [],
      "examples": [
        ">>> import lucid.quantization as Q\n>>> Q.per_channel_symmetric is Q.QScheme.PER_CHANNEL_SYMMETRIC\nTrue\n>>> Q.per_channel_symmetric.is_per_channel\nTrue\n>>> Q.per_channel_symmetric.is_symmetric\nTrue\n>>> Q.per_tensor_affine.is_symmetric\nFalse"
      ],
      "notes": [
        "PER_TENSOR_AFFINE\n    One `(scale, zero_point)` for the whole tensor with a **free** `zero_point`.\n    The asymmetric grid fits ranges that do not straddle zero (e.g. post-ReLU\n    activations, all $\\ge 0$) without wasting codes on an unused half. The\n    default **activation** scheme. Exposed as `per_tensor_affine`.\nPER_TENSOR_SYMMETRIC\n    One `scale` for the whole tensor with `zero_point` **pinned** (0 for a\n    signed grid, mid-range for unsigned), so real 0 maps exactly to a code and the\n    grid is symmetric about zero. Exposed as `per_tensor_symmetric`.\nPER_CHANNEL_AFFINE\n    One `(scale, zero_point)` **per channel** along `ch_axis` with a free\n    `zero_point` — rarely needed, for weights whose per-channel ranges are\n    strongly skewed. Exposed as `per_channel_affine`.\nPER_CHANNEL_SYMMETRIC\n    One `scale` per channel with a pinned `zero_point` — the standard,\n    accuracy-preserving choice for convolution / linear **weights**, and the form\n    the low-precision GEMM kernels expect for their weight operand. Exposed as\n    `per_channel_symmetric`.",
        "- **Affine vs symmetric, precisely.** Affine derives `scale` from the full\n  `max - min` span and solves for `zero_point` so the endpoints land on grid\n  bounds; symmetric derives `scale` from `max(|min|, |max|)` over a half-range\n  and fixes `zero_point` — see `lucid.quantization.calculate_qparams`.\n- **Why symmetric weights.** Pinning `zero_point = 0` removes the cross-terms in\n  an integer matmul (a `w_q · x_q` product needs no weight-offset correction),\n  which is exactly what the packed low-precision GEMM assumes for its weight side.\n- The two boolean helpers `is_per_channel` / `is_symmetric` let callers\n  branch on granularity / symmetry without matching individual members."
      ],
      "attributes": [],
      "warns": [],
      "see_also": [
        {
          "name": "QDtype",
          "description": "The companion descriptor that fixes the integer grid (bits / range)."
        },
        {
          "name": "lucid.quantization.calculate_qparams",
          "description": "Turns an observed range into"
        },
        {
          "name": "``(scale",
          "description": ""
        },
        {
          "name": "zero_point)`` per this scheme.",
          "description": ""
        },
        {
          "name": "lucid.quantization.QParams",
          "description": "Bundles a scheme with its derived parameters."
        }
      ]
    },
    {
      "name": "FakeQuantize",
      "path": "lucid.quantization._fake_quantize.FakeQuantize",
      "kind": "class",
      "class_kind": "regular",
      "bases": [
        "Module"
      ],
      "labels": [],
      "signature": "FakeQuantize(observer: type[ObserverBase] = MovingAverageMinMaxObserver, observer_kwargs: object = {})",
      "source": "https://github.com/ChanLumerico/lucid/blob/4b22d32cc/lucid/quantization/_fake_quantize.py#L29",
      "subcategory": "fake_quantize",
      "methods": [
        {
          "name": "__init__",
          "path": "lucid.quantization._fake_quantize.FakeQuantize.__init__",
          "kind": "function",
          "labels": [],
          "signature": "__init__(observer: type[ObserverBase] = MovingAverageMinMaxObserver, observer_kwargs: object = {})",
          "source": "https://github.com/ChanLumerico/lucid/blob/4b22d32cc/lucid/quantization/_fake_quantize.py#L166",
          "subcategory": "fake_quantize",
          "summary": null,
          "extended": null,
          "parameters": [],
          "returns": {
            "annotation": "None",
            "description": ""
          },
          "raises": [],
          "examples": [],
          "notes": [],
          "attributes": [],
          "warns": [],
          "see_also": []
        },
        {
          "name": "forward",
          "path": "lucid.quantization._fake_quantize.FakeQuantize.forward",
          "kind": "function",
          "labels": [],
          "signature": "forward(x: Tensor)",
          "source": "https://github.com/ChanLumerico/lucid/blob/4b22d32cc/lucid/quantization/_fake_quantize.py#L181",
          "subcategory": "fake_quantize",
          "summary": "Refresh qparams (if observing) then fake-quantize (if enabled).",
          "extended": null,
          "parameters": [],
          "returns": {
            "annotation": "Tensor",
            "description": ""
          },
          "raises": [],
          "examples": [],
          "notes": [],
          "attributes": [],
          "warns": [],
          "see_also": []
        },
        {
          "name": "enable_observer",
          "path": "lucid.quantization._fake_quantize.FakeQuantize.enable_observer",
          "kind": "function",
          "labels": [],
          "signature": "enable_observer(enabled: bool = True)",
          "source": "https://github.com/ChanLumerico/lucid/blob/4b22d32cc/lucid/quantization/_fake_quantize.py#L200",
          "subcategory": "fake_quantize",
          "summary": "Enable/disable statistic collection; returns `self` for chaining.",
          "extended": null,
          "parameters": [],
          "returns": {
            "annotation": "FakeQuantize",
            "description": ""
          },
          "raises": [],
          "examples": [],
          "notes": [],
          "attributes": [],
          "warns": [],
          "see_also": []
        },
        {
          "name": "disable_observer",
          "path": "lucid.quantization._fake_quantize.FakeQuantize.disable_observer",
          "kind": "function",
          "labels": [],
          "signature": "disable_observer()",
          "source": "https://github.com/ChanLumerico/lucid/blob/4b22d32cc/lucid/quantization/_fake_quantize.py#L205",
          "subcategory": "fake_quantize",
          "summary": "Freeze the observer (stop refreshing `scale` / `zero_point`).",
          "extended": null,
          "parameters": [],
          "returns": {
            "annotation": "FakeQuantize",
            "description": ""
          },
          "raises": [],
          "examples": [],
          "notes": [],
          "attributes": [],
          "warns": [],
          "see_also": []
        },
        {
          "name": "enable_fake_quant",
          "path": "lucid.quantization._fake_quantize.FakeQuantize.enable_fake_quant",
          "kind": "function",
          "labels": [],
          "signature": "enable_fake_quant(enabled: bool = True)",
          "source": "https://github.com/ChanLumerico/lucid/blob/4b22d32cc/lucid/quantization/_fake_quantize.py#L209",
          "subcategory": "fake_quantize",
          "summary": "Enable/disable the fake-quant transform; returns `self`.",
          "extended": null,
          "parameters": [],
          "returns": {
            "annotation": "FakeQuantize",
            "description": ""
          },
          "raises": [],
          "examples": [],
          "notes": [],
          "attributes": [],
          "warns": [],
          "see_also": []
        },
        {
          "name": "disable_fake_quant",
          "path": "lucid.quantization._fake_quantize.FakeQuantize.disable_fake_quant",
          "kind": "function",
          "labels": [],
          "signature": "disable_fake_quant()",
          "source": "https://github.com/ChanLumerico/lucid/blob/4b22d32cc/lucid/quantization/_fake_quantize.py#L214",
          "subcategory": "fake_quantize",
          "summary": "Pass activations through unchanged (observer may still run).",
          "extended": null,
          "parameters": [],
          "returns": {
            "annotation": "FakeQuantize",
            "description": ""
          },
          "raises": [],
          "examples": [],
          "notes": [],
          "attributes": [],
          "warns": [],
          "see_also": []
        },
        {
          "name": "calculate_qparams",
          "path": "lucid.quantization._fake_quantize.FakeQuantize.calculate_qparams",
          "kind": "function",
          "labels": [],
          "signature": "calculate_qparams()",
          "source": "https://github.com/ChanLumerico/lucid/blob/4b22d32cc/lucid/quantization/_fake_quantize.py#L218",
          "subcategory": "fake_quantize",
          "summary": "Delegate to the wrapped observer.",
          "extended": null,
          "parameters": [],
          "returns": {
            "annotation": "tuple[Tensor, Tensor]",
            "description": ""
          },
          "raises": [],
          "examples": [],
          "notes": [],
          "attributes": [],
          "warns": [],
          "see_also": []
        },
        {
          "name": "with_args",
          "path": "lucid.quantization._fake_quantize.FakeQuantize.with_args",
          "kind": "function",
          "labels": [
            "classmethod"
          ],
          "signature": "with_args(kwargs: object = {})",
          "source": "https://github.com/ChanLumerico/lucid/blob/4b22d32cc/lucid/quantization/_fake_quantize.py#L222",
          "subcategory": "fake_quantize",
          "summary": "Return a zero-arg factory building this module with `kwargs`.",
          "extended": null,
          "parameters": [],
          "returns": {
            "annotation": "functools.partial[FakeQuantize]",
            "description": ""
          },
          "raises": [],
          "examples": [],
          "notes": [],
          "attributes": [],
          "warns": [],
          "see_also": []
        }
      ],
      "summary": "Observer + straight-through fake-quantization, toggleable for QAT.",
      "extended": "The building block of **quantization-aware training** (QAT). A `FakeQuantize` sits\non a weight or activation and, on every forward, does two things: it lets its wrapped\nobserver refresh `(scale, zero_point)` from the live statistics, then it applies\n`lucid.quantization.fake_quantize` — rounding the tensor onto the integer grid\nand back to float — so the downstream graph *sees the quantization error* while\ntraining still runs in float. This is what lets a network learn weights that are\nrobust to the eventual int8 inference numerics: the forward simulates quantization,\nand a straight-through estimator (STE) keeps the operation differentiable so the\nrounding does not block gradients.\n\n**Observer vs fake-quant, decoupled.** The two behaviours are independently gated by\n`enable_observer` / `enable_fake_quant`, which drives the standard QAT\nschedule: observe + fake-quant early so statistics settle and the net adapts, then\n`disable_observer` to freeze the grid once ranges converge (and, in fused\nstacks, freeze BN stats). With the observer off but fake-quant on, the layer keeps\nquantizing against the last-learned qparams; with fake-quant off it is a pure\npassthrough (the observer may still watch).\n\n**The straight-through fake-quant.** Writing $S, Z$ for the current\n`(scale, zero_point)` and $[q_\\min, q_\\max]$ for the grid, the forward\ncomputes the quantize→dequantize round-trip in the float domain:\n\n$$\n\\hat{x} = \\bigl(\\operatorname{clip}(\\operatorname{round}(x/S) + Z,\\\n    q_\\min,\\ q_\\max) - Z\\bigr)\\,S.\n$$\n\nThe $\\operatorname{round}$ step has a zero (a.e.) derivative, which would kill\ntraining, so the backward substitutes the **straight-through estimator** — gradient\npasses through unchanged inside the grid and is zeroed where the code saturates:\n\n$$\n\\frac{\\partial \\hat{x}}{\\partial x} =\n\\begin{cases}\n    1 & q_\\min \\le \\operatorname{round}(x/S) + Z \\le q_\\max \\\\\n    0 & \\text{otherwise}\n\\end{cases}\n$$\n\nso $\\nabla_x = \\nabla_{\\hat{x}} \\cdot \\mathbb{1}[\\,\\text{code in range}\\,]$.\nThe qparams $S, Z$ are treated as constants of the step (they are refreshed by\nthe observer, not learned through this gradient).",
      "parameters": [
        {
          "name": "observer",
          "annotation": "type[ObserverBase]",
          "description": "Observer *class* (not instance) instantiated once to track the statistics from\nwhich `(scale, zero_point)` are derived. The default matches the activation\nrecipe of `lucid.quantization.get_default_qat_qconfig`.",
          "default": "MovingAverageMinMaxObserver"
        },
        {
          "name": "**observer_kwargs",
          "annotation": "object",
          "description": "Forwarded verbatim to the observer constructor — e.g. `qscheme` / `qdtype` /\n`ch_axis` / `averaging_constant` — so one `FakeQuantize` can wrap any\nobserver configuration (per-tensor activation or per-channel weight).",
          "default": "{}"
        }
      ],
      "returns": null,
      "raises": [],
      "examples": [
        ">>> import lucid\n>>> import lucid.quantization as Q\n>>> fq = Q.FakeQuantize()                    # wraps MovingAverageMinMaxObserver\n>>> y = fq(lucid.randn(8, 16))               # observe, then round-trip through grid\n>>> y.shape\n(8, 16)\n>>> bool(fq.scale.item() > 0)                # qparams refreshed from the observer\nTrue\nFreezing the observer holds the grid fixed while fake-quant keeps applying it — the\nQAT \"freeze ranges near convergence\" step:\n>>> _ = fq.disable_observer()\n>>> frozen = fq.scale.item()\n>>> _ = fq(lucid.randn(8, 16) * 100.0)       # a wild batch cannot move the grid now\n>>> fq.scale.item() == frozen\nTrue\nTurning fake-quant off makes the module a pure passthrough (identity):\n>>> _ = fq.disable_fake_quant()\n>>> x = lucid.randn(2, 4)\n>>> bool((fq(x) == x).all().item())\nTrue"
      ],
      "notes": [
        "- **QAT schedule.** Typical use: start with both toggles on; call\n  `disable_observer` once activation ranges stabilise to freeze the grid, so the\n  final quantized weights are trained against fixed qparams.\n- **STE saturation mask.** The backward zeroes gradient for values whose code lands\n  outside `[q_min, q_max]` — saturated activations receive no learning signal,\n  which nudges the observer's range to cover them.\n- **Buffer re-registration.** Refreshed `scale` / `zero_point` are written back\n  via `register_buffer` (same name), the same in-place-update contract the\n  observers use, so the qparams persist through `state_dict`.\n- **Per-channel weights.** When wrapping a per-channel observer (`ch_axis` set), the\n  fake-quant applies a distinct `(scale, zero_point)` per channel — the QAT analogue\n  of the per-channel int8 weight used at inference by\n  `lucid.nn.quantized.Linear`.\n- **Default QAT recipe.** `lucid.quantization.get_default_qat_qconfig` builds\n  one `FakeQuantize` for activations (wrapping\n  `lucid.quantization.MovingAverageMinMaxObserver`) and one for weights\n  (wrapping `lucid.quantization.PerChannelMinMaxObserver`).\n- Use `with_args` to defer construction into a\n  `lucid.quantization.QConfig` as a zero-arg factory."
      ],
      "attributes": [
        {
          "name": "activation_post_process",
          "annotation": "ObserverBase",
          "description": "The live wrapped observer instance; its `calculate_qparams` supplies the\ngrid on each forward."
        },
        {
          "name": "scale",
          "annotation": "Tensor",
          "description": "Scalar (or length-`C` for per-channel) quantization step, refreshed from the\nobserver while observing is enabled. Seeded to `1.0`."
        },
        {
          "name": "zero_point",
          "annotation": "Tensor",
          "description": "The matching zero-point, refreshed alongside `scale`. Seeded to `0.0`."
        },
        {
          "name": "qdtype",
          "annotation": "QDtype",
          "description": "Target dtype, mirrored from the wrapped observer (supplies the grid bounds)."
        },
        {
          "name": "qscheme",
          "annotation": "QScheme",
          "description": "Target scheme, mirrored from the wrapped observer."
        },
        {
          "name": "ch_axis",
          "annotation": "int or None",
          "description": "Per-channel axis, mirrored from the wrapped observer (`None` for per-tensor),\npassed to `lucid.quantization.fake_quantize` so per-channel qparams\nbroadcast correctly."
        }
      ],
      "warns": [],
      "see_also": [
        {
          "name": "lucid.quantization.fake_quantize",
          "description": "The underlying STE round-trip primitive."
        },
        {
          "name": "lucid.quantization.MovingAverageMinMaxObserver",
          "description": "Default wrapped activation observer."
        },
        {
          "name": "lucid.quantization.PerChannelMinMaxObserver",
          "description": "Default wrapped weight observer."
        },
        {
          "name": "lucid.quantization.get_default_qat_qconfig",
          "description": "Builds the default QAT FakeQuantize pair."
        },
        {
          "name": "lucid.nn.quantized.Linear",
          "description": "The int8 inference layer QAT trains toward."
        }
      ]
    },
    {
      "name": "FixedQParamsObserver",
      "path": "lucid.quantization.observer.FixedQParamsObserver",
      "kind": "class",
      "class_kind": "regular",
      "bases": [
        "ObserverBase"
      ],
      "labels": [],
      "signature": "FixedQParamsObserver(scale: float, zero_point: int, qscheme: QScheme = per_tensor_affine, qdtype: QDtype = quint8, eps: float = 1e-08)",
      "source": "https://github.com/ChanLumerico/lucid/blob/4b22d32cc/lucid/quantization/observer.py#L947",
      "subcategory": "observer",
      "methods": [
        {
          "name": "__init__",
          "path": "lucid.quantization.observer.FixedQParamsObserver.__init__",
          "kind": "function",
          "labels": [],
          "signature": "__init__(scale: float, zero_point: int, qscheme: QScheme = per_tensor_affine, qdtype: QDtype = quint8, eps: float = 1e-08)",
          "source": "https://github.com/ChanLumerico/lucid/blob/4b22d32cc/lucid/quantization/observer.py#L1040",
          "subcategory": "observer",
          "summary": null,
          "extended": null,
          "parameters": [],
          "returns": {
            "annotation": "None",
            "description": ""
          },
          "raises": [],
          "examples": [],
          "notes": [],
          "attributes": [],
          "warns": [],
          "see_also": []
        },
        {
          "name": "forward",
          "path": "lucid.quantization.observer.FixedQParamsObserver.forward",
          "kind": "function",
          "labels": [],
          "signature": "forward(x: Tensor)",
          "source": "https://github.com/ChanLumerico/lucid/blob/4b22d32cc/lucid/quantization/observer.py#L1052",
          "subcategory": "observer",
          "summary": "Identity — the qparams are fixed, so no statistics are gathered.",
          "extended": null,
          "parameters": [],
          "returns": {
            "annotation": "Tensor",
            "description": ""
          },
          "raises": [],
          "examples": [],
          "notes": [],
          "attributes": [],
          "warns": [],
          "see_also": []
        },
        {
          "name": "calculate_qparams",
          "path": "lucid.quantization.observer.FixedQParamsObserver.calculate_qparams",
          "kind": "function",
          "labels": [],
          "signature": "calculate_qparams()",
          "source": "https://github.com/ChanLumerico/lucid/blob/4b22d32cc/lucid/quantization/observer.py#L1057",
          "subcategory": "observer",
          "summary": "Return `(scale, zero_point)` from the accumulated statistics.",
          "extended": null,
          "parameters": [],
          "returns": {
            "annotation": "tuple[Tensor, Tensor]",
            "description": ""
          },
          "raises": [],
          "examples": [],
          "notes": [],
          "attributes": [],
          "warns": [],
          "see_also": []
        }
      ],
      "summary": "Observer with a fixed `(scale, zero_point)` — no statistics collected.",
      "extended": "For ops whose output range is *known a priori*, calibration is pointless: the grid\ncan be fixed once and for all. The canonical cases are bounded activations —\n`sigmoid` → `[0, 1]`, `softmax` → `[0, 1]`, `tanh` → `[-1, 1]` — where\nthe ideal `(scale, zero_point)` is a closed-form function of the dtype grid, not\nsomething the data can improve on. `FixedQParamsObserver` carries that fixed pair,\nmakes `forward` a pure identity (no statistics gathered), and returns the supplied\nqparams verbatim from `calculate_qparams`.\n\n**When to pick it.** Use it on the output of a range-bounded op so calibration does\nnot waste effort (or worse, under-shoot the true bound) estimating a range you\nalready know. For a `sigmoid`/`softmax` output on the unsigned `quint8` grid\n`[0, 255]` the natural choice is `scale = 1/256, zero_point = 0` (mapping\n`[0, 1]` onto the full grid); a `tanh` output `[-1, 1]` uses `scale = 2/256`\nwith a mid-grid `zero_point`. For every unbounded tensor, use a statistics-collecting\nobserver (`MinMaxObserver`, `HistogramObserver`, …) instead.\n\nThere is no statistic and no reduction — the qparams are the constants themselves:\n\n$$\n(\\text{scale},\\ \\text{zero\\_point}) = (s_{\\text{fixed}},\\ z_{\\text{fixed}}),\n\\qquad \\hat{x} = \\bigl(\\operatorname{clip}(\\operatorname{round}(x/s_{\\text{fixed}})\n    + z_{\\text{fixed}},\\ q_{\\min},\\ q_{\\max}) - z_{\\text{fixed}}\\bigr)\\,\n    s_{\\text{fixed}}\n$$\n\nindependent of any observed data.",
      "parameters": [
        {
          "name": "scale",
          "annotation": "float",
          "description": "The fixed quantization step size $s_{\\text{fixed}}$.",
          "default": null
        },
        {
          "name": "zero_point",
          "annotation": "int",
          "description": "The fixed integer code that real value `0` maps to.",
          "default": null
        },
        {
          "name": "qscheme",
          "annotation": "QScheme",
          "description": "Target quantization scheme — metadata only; the qparams are fixed regardless.",
          "default": "per_tensor_affine"
        },
        {
          "name": "qdtype",
          "annotation": "QDtype",
          "description": "Target quantized dtype recorded for downstream consumers (grid bounds).",
          "default": "quint8"
        },
        {
          "name": "eps",
          "annotation": "float",
          "description": "Retained for interface symmetry with `ObserverBase`; unused because the\n`scale` is fixed (never derived, so never floored).",
          "default": "1e-8"
        }
      ],
      "returns": null,
      "raises": [],
      "examples": [
        "A fixed observer for a sigmoid output on the quint8 grid — the [0, 1]\nrange maps onto [0, 255] with scale = 1/256:\n>>> import lucid.quantization as Q\n>>> obs = Q.FixedQParamsObserver(scale=1.0 / 256, zero_point=0)\n>>> scale, zero_point = obs.calculate_qparams()   # valid with no calibration\n>>> round(scale.item(), 6), int(zero_point.item())\n(0.003906, 0)\nThe forward is a pure identity — feeding data changes nothing:\n>>> import lucid\n>>> before = obs.scale.item()\n>>> _ = obs(lucid.randn(4, 4))\n>>> obs.scale.item() == before\nTrue"
      ],
      "notes": [
        "- **Identity forward.** `forward` returns `x` unchanged and collects nothing —\n  the two buffers are set once at construction and never move.\n- **No calibration needed.** `calculate_qparams` never raises (unlike\n  `PlaceholderObserver`); it simply returns the stored `(scale, zero_point)`,\n  so it is valid to call immediately after construction with no forward passes.\n- **Buffers, not stats.** `scale` / `zero_point` are registered buffers, so they\n  persist through `state_dict` and move with `.to(device)` like any other buffer.\n- **Metadata fields.** `qscheme` / `eps` are stored for interface symmetry but do\n  not affect the returned qparams."
      ],
      "attributes": [
        {
          "name": "scale",
          "annotation": "Tensor",
          "description": "Scalar (shape `()`) fixed quantization step."
        },
        {
          "name": "zero_point",
          "annotation": "Tensor",
          "description": "Scalar (shape `()`) fixed zero-point (stored as an integer-valued float)."
        }
      ],
      "warns": [],
      "see_also": [
        {
          "name": "lucid.quantization.PlaceholderObserver",
          "description": "Dtype carrier whose qparams instead raise."
        },
        {
          "name": "lucid.quantization.MinMaxObserver",
          "description": "Statistics-collecting observer for unbounded ops."
        },
        {
          "name": "lucid.quantization.calculate_qparams",
          "description": "The range-based reduction this observer skips."
        },
        {
          "name": "lucid.quantization.FakeQuantize",
          "description": "Wraps an observer for QAT."
        }
      ]
    },
    {
      "name": "HistogramObserver",
      "path": "lucid.quantization.observer.HistogramObserver",
      "kind": "class",
      "class_kind": "regular",
      "bases": [
        "ObserverBase"
      ],
      "labels": [],
      "signature": "HistogramObserver(bins: int = 2048, qscheme: QScheme = per_tensor_affine, qdtype: QDtype = quint8, eps: float = 1e-08)",
      "source": "https://github.com/ChanLumerico/lucid/blob/4b22d32cc/lucid/quantization/observer.py#L597",
      "subcategory": "observer",
      "methods": [
        {
          "name": "__init__",
          "path": "lucid.quantization.observer.HistogramObserver.__init__",
          "kind": "function",
          "labels": [],
          "signature": "__init__(bins: int = 2048, qscheme: QScheme = per_tensor_affine, qdtype: QDtype = quint8, eps: float = 1e-08)",
          "source": "https://github.com/ChanLumerico/lucid/blob/4b22d32cc/lucid/quantization/observer.py#L714",
          "subcategory": "observer",
          "summary": null,
          "extended": null,
          "parameters": [],
          "returns": {
            "annotation": "None",
            "description": ""
          },
          "raises": [],
          "examples": [],
          "notes": [],
          "attributes": [],
          "warns": [],
          "see_also": []
        },
        {
          "name": "forward",
          "path": "lucid.quantization.observer.HistogramObserver.forward",
          "kind": "function",
          "labels": [],
          "signature": "forward(x: Tensor)",
          "source": "https://github.com/ChanLumerico/lucid/blob/4b22d32cc/lucid/quantization/observer.py#L746",
          "subcategory": "observer",
          "summary": "Update the running range and accumulate the histogram.",
          "extended": null,
          "parameters": [],
          "returns": {
            "annotation": "Tensor",
            "description": ""
          },
          "raises": [],
          "examples": [],
          "notes": [],
          "attributes": [],
          "warns": [],
          "see_also": []
        },
        {
          "name": "calculate_qparams",
          "path": "lucid.quantization.observer.HistogramObserver.calculate_qparams",
          "kind": "function",
          "labels": [],
          "signature": "calculate_qparams()",
          "source": "https://github.com/ChanLumerico/lucid/blob/4b22d32cc/lucid/quantization/observer.py#L799",
          "subcategory": "observer",
          "summary": "Return `(scale, zero_point)` from the accumulated statistics.",
          "extended": null,
          "parameters": [],
          "returns": {
            "annotation": "tuple[Tensor, Tensor]",
            "description": ""
          },
          "raises": [],
          "examples": [],
          "notes": [],
          "attributes": [],
          "warns": [],
          "see_also": []
        }
      ],
      "summary": "Per-tensor observer that records a value histogram and picks a clip range.",
      "extended": "The accuracy-maximising activation observer. Instead of committing to the raw\n`[min, max]` — which on a heavy-tailed activation would stretch the grid over a\nhandful of rare extremes and quantize the dense bulk coarsely — it accumulates a\n`bins`-bucket histogram of every value seen (via the engine `histc` primitive),\nthen at `calculate_qparams` time *searches* for the clip range\n`[new_min, new_max]` that minimises the expected L2 quantization error. Clipping\nthe tails costs a little error on the rare outliers but wins far finer resolution on\nthe common values, which is usually a large net accuracy gain.\n\n**When to pick it.** Use it for **activations** with skewed / heavy-tailed\ndistributions — it is the activation observer in\n`lucid.quantization.get_default_qconfig` (static PTQ). It is more expensive\nthan min/max (it maintains a histogram and runs a search) and applies to per-tensor\naffine schemes, so it is not used for per-channel weights (see\n`PerChannelMinMaxObserver`). When calibration is cheap and distributions are\nlight-tailed, plain `MinMaxObserver` / `MovingAverageMinMaxObserver`\nsuffice.\n\nThe running range widens with the data; when it grows the existing histogram is\n*rebinned* onto the wider support before the new batch is added. With bin centers\n$c_i$ and counts $n_i$, the search evaluates candidate clip endpoints\n$[a, b]$ (drawn from a few tail quantiles) and picks the pair minimising the\nper-value clip error plus the uniform-quantization noise variance:\n\n$$\n\\operatorname{err}(a, b) = \\sum_i n_i\n    \\Bigl[\\bigl(c_i - \\operatorname{clip}(c_i, a, b)\\bigr)^2\n          + \\tfrac{\\Delta^2}{12}\\Bigr],\n\\qquad \\Delta = \\frac{b - a}{q_{\\max} - q_{\\min}},\n$$\n\nwhere the first term is the squared clipping error (mass pushed onto the boundary)\nand $\\Delta^2/12$ is the variance of rounding to a uniform grid of step\n$\\Delta$. The winning $[a, b]$ is then fed to\n`lucid.quantization.calculate_qparams` for the final affine `(scale,\nzero_point)`.",
      "parameters": [
        {
          "name": "bins",
          "annotation": "int",
          "description": "Number of histogram buckets over the running `[min, max]`. More bins give a\nfiner clip search at higher memory / compute cost.",
          "default": "2048"
        },
        {
          "name": "qscheme",
          "annotation": "QScheme",
          "description": "Target quantization scheme (the clip search assumes a per-tensor grid).",
          "default": "per_tensor_affine"
        },
        {
          "name": "qdtype",
          "annotation": "QDtype",
          "description": "Target quantized dtype; `quint8` gives the unsigned `[0, 255]` grid.",
          "default": "quint8"
        },
        {
          "name": "eps",
          "annotation": "float",
          "description": "Lower floor on the derived `scale`.",
          "default": "1e-8"
        }
      ],
      "returns": null,
      "raises": [],
      "examples": [
        ">>> import lucid\n>>> import lucid.quantization as Q\n>>> obs = Q.HistogramObserver(bins=512)\n>>> for _ in range(4):\n...     _ = obs(lucid.randn(8, 1024))\n>>> obs.histogram.shape\n(512,)\n>>> scale, zero_point = obs.calculate_qparams()\n>>> bool(scale.item() > 0)\nTrue\nOn a heavy-tailed input the search clips the rare extreme, so the chosen range is\n*narrower* than the raw min/max the histogram spans:\n>>> import lucid\n>>> x = lucid.randn(1, 100000)\n>>> x[0, 0] = 50.0                           # a single far outlier\n>>> obs = Q.HistogramObserver(bins=2048)\n>>> _ = obs(x)\n>>> s, _ = obs.calculate_qparams()\n>>> bool(s.item() * 255 < 50.0)              # grid span clipped below the outlier\nTrue"
      ],
      "notes": [
        "- **Rebinning on growth.** When a batch widens `[min, max]`, the stored histogram\n  is redistributed onto the new, wider bin grid before the fresh counts are added, so\n  accumulated mass is preserved rather than discarded.\n- **Search is a coarse scan.** The clip endpoints are candidate tail quantiles\n  (`0, 1e-5, 1e-4, 1e-3, 5e-3, 1e-2`), not a full exhaustive sweep — a deliberate\n  speed / accuracy trade-off. The best-scoring pair wins.\n- **Data-dependent control flow.** `forward` reads `x.min()/.max()` via\n  `.item()` and the search runs in Python over `histogram.tolist()`; this is a\n  sanctioned CPU round-trip (data-dependent output), still H4-pure (engine `histc`,\n  no external histogram library).\n- **Buffer re-registration.** `min_val` / `max_val` / `histogram` are all\n  updated via `register_buffer` (same name) per the `ObserverBase` contract.\n- **Symmetric vs affine.** The reduction honours `qscheme`; the default per-tensor\n  affine leaves `zero_point` free to fit the (clipped) asymmetric range.\n- Default activation observer of `lucid.quantization.get_default_qconfig`."
      ],
      "attributes": [
        {
          "name": "min_val",
          "annotation": "Tensor",
          "description": "Scalar (shape `()`) running minimum of the histogram support, seeded `+inf`."
        },
        {
          "name": "max_val",
          "annotation": "Tensor",
          "description": "Scalar (shape `()`) running maximum of the histogram support, seeded `-inf`."
        },
        {
          "name": "histogram",
          "annotation": "Tensor",
          "description": "The accumulated value histogram, shape `(bins,)`, seeded to zeros."
        }
      ],
      "warns": [],
      "see_also": [
        {
          "name": "lucid.quantization.MinMaxObserver",
          "description": "Cheaper per-tensor observer (no clip search)."
        },
        {
          "name": "lucid.quantization.MovingAverageMinMaxObserver",
          "description": "EMA alternative for outliers."
        },
        {
          "name": "lucid.quantization.get_default_qconfig",
          "description": "Uses this as the activation observer."
        },
        {
          "name": "lucid.quantization.calculate_qparams",
          "description": "Reduces the chosen clip range to qparams."
        },
        {
          "name": "lucid.quantization.FakeQuantize",
          "description": "Wraps an observer for QAT."
        }
      ]
    },
    {
      "name": "MinMaxObserver",
      "path": "lucid.quantization.observer.MinMaxObserver",
      "kind": "class",
      "class_kind": "regular",
      "bases": [
        "ObserverBase"
      ],
      "labels": [],
      "signature": "MinMaxObserver(qscheme: QScheme = per_tensor_affine, qdtype: QDtype = quint8, eps: float = 1e-08)",
      "source": "https://github.com/ChanLumerico/lucid/blob/4b22d32cc/lucid/quantization/observer.py#L194",
      "subcategory": "observer",
      "methods": [
        {
          "name": "__init__",
          "path": "lucid.quantization.observer.MinMaxObserver.__init__",
          "kind": "function",
          "labels": [],
          "signature": "__init__(qscheme: QScheme = per_tensor_affine, qdtype: QDtype = quint8, eps: float = 1e-08)",
          "source": "https://github.com/ChanLumerico/lucid/blob/4b22d32cc/lucid/quantization/observer.py#L297",
          "subcategory": "observer",
          "summary": null,
          "extended": null,
          "parameters": [],
          "returns": {
            "annotation": "None",
            "description": ""
          },
          "raises": [],
          "examples": [],
          "notes": [],
          "attributes": [],
          "warns": [],
          "see_also": []
        },
        {
          "name": "forward",
          "path": "lucid.quantization.observer.MinMaxObserver.forward",
          "kind": "function",
          "labels": [],
          "signature": "forward(x: Tensor)",
          "source": "https://github.com/ChanLumerico/lucid/blob/4b22d32cc/lucid/quantization/observer.py#L307",
          "subcategory": "observer",
          "summary": "Fold `x`'s global min/max into the running statistics.",
          "extended": null,
          "parameters": [],
          "returns": {
            "annotation": "Tensor",
            "description": ""
          },
          "raises": [],
          "examples": [],
          "notes": [],
          "attributes": [],
          "warns": [],
          "see_also": []
        },
        {
          "name": "calculate_qparams",
          "path": "lucid.quantization.observer.MinMaxObserver.calculate_qparams",
          "kind": "function",
          "labels": [],
          "signature": "calculate_qparams()",
          "source": "https://github.com/ChanLumerico/lucid/blob/4b22d32cc/lucid/quantization/observer.py#L314",
          "subcategory": "observer",
          "summary": "Delegate to the wrapped observer.",
          "extended": null,
          "parameters": [],
          "returns": {
            "annotation": "tuple[Tensor, Tensor]",
            "description": ""
          },
          "raises": [],
          "examples": [],
          "notes": [],
          "attributes": [],
          "warns": [],
          "see_also": []
        }
      ],
      "summary": "Per-tensor observer tracking the global running min / max of the input.",
      "extended": "The simplest and most common calibration statistic: on every batch it folds the\ntensor's overall `min` / `max` into a *cumulative* running range, and\n`calculate_qparams` distills that single `[min_val, max_val]` into one\nper-tensor `(scale, zero_point)` pair. It is the natural default for activations\nand a serviceable choice for weights, but because the running range only ever grows,\na single spike in any calibration batch permanently widens it — wasting codes on a\nrange that most values never occupy.\n\n**When to pick it.** Reach for `MinMaxObserver` when the tensor's distribution is\nwell-behaved (roughly bounded, light-tailed) and you want the cheapest possible\ncalibration. If a few calibration batches carry outliers, prefer\n`MovingAverageMinMaxObserver`, whose EMA lets the range *relax* back toward\nthe bulk of the data; for heavy-tailed activations where clipping the tails buys real\naccuracy, prefer `HistogramObserver`. For weights, the per-channel\n`PerChannelMinMaxObserver` is almost always tighter.\n\nPer batch the running range is the cumulative min/max:\n\n$$\n\\text{min\\_val} \\leftarrow \\min(\\text{min\\_val},\\ \\min_i x_i),\\qquad\n\\text{max\\_val} \\leftarrow \\max(\\text{max\\_val},\\ \\max_i x_i)\n$$\n\nseeded from $(+\\infty, -\\infty)$, then reduced to qparams by the affine map of\n`lucid.quantization.calculate_qparams` (range first widened to include 0):\n\n$$\ns = \\frac{\\text{max\\_val} - \\text{min\\_val}}{q_{\\max} - q_{\\min}},\\qquad\nz = \\operatorname{clip}\\!\\bigl(q_{\\min} -\n    \\operatorname{round}(\\text{min\\_val}/s),\\ q_{\\min},\\ q_{\\max}\\bigr)\n$$\n\n(or the symmetric form of the base class if a symmetric `qscheme` is passed).",
      "parameters": [
        {
          "name": "qscheme",
          "annotation": "QScheme",
          "description": "Target quantization scheme. Affine spends a free `zero_point` to fit an\nasymmetric range tightly (e.g. post-ReLU activations).",
          "default": "per_tensor_affine"
        },
        {
          "name": "qdtype",
          "annotation": "QDtype",
          "description": "Target quantized dtype; `quint8` gives the unsigned grid `[0, 255]`.",
          "default": "quint8"
        },
        {
          "name": "eps",
          "annotation": "float",
          "description": "Lower floor on the derived `scale` — avoids a zero scale on a constant input\n(`min_val == max_val`).",
          "default": "1e-8"
        }
      ],
      "returns": null,
      "raises": [],
      "examples": [
        ">>> import lucid\n>>> import lucid.quantization as Q\n>>> obs = Q.MinMaxObserver()\n>>> for _ in range(4):                       # feed several calibration batches\n...     _ = obs(lucid.randn(32, 256))\n>>> scale, zero_point = obs.calculate_qparams()\n>>> bool(scale.item() > 0)\nTrue\nAn *uncalibrated* observer still holds the [+inf, -inf] seed, so its range is\ndegenerate until it has seen data:\n>>> fresh = Q.MinMaxObserver()\n>>> import math\n>>> math.isinf(fresh.min_val.item()), math.isinf(fresh.max_val.item())\n(True, True)"
      ],
      "notes": [
        "- **Cumulative, not per-batch.** `min_val` / `max_val` are monotone: they only\n  widen across calibration, so the final range reflects the extreme of *all* data\n  seen, not any single batch. This is the source of the outlier sensitivity.\n- **Buffer re-registration.** `forward` updates the range via `register_buffer`\n  (same name), per the `ObserverBase` in-place-update contract.\n- **Symmetric vs affine.** With a symmetric `qscheme` the base-class math pins\n  `zero_point` (0 for a signed dtype, mid-range for unsigned); with an affine\n  scheme `zero_point` is free.\n- **Uncalibrated.** Before the first `forward` the range is `[+inf, -inf]`;\n  calling `calculate_qparams` on it yields a meaningless (infinite) scale —\n  always calibrate first.\n- Not the default in `lucid.quantization.get_default_qconfig` (which uses\n  `HistogramObserver` for activations); it is the plain building block the EMA\n  and per-channel variants specialise."
      ],
      "attributes": [
        {
          "name": "min_val",
          "annotation": "Tensor",
          "description": "Scalar (shape `()`) running minimum, seeded `+inf`."
        },
        {
          "name": "max_val",
          "annotation": "Tensor",
          "description": "Scalar (shape `()`) running maximum, seeded `-inf`."
        }
      ],
      "warns": [],
      "see_also": [
        {
          "name": "lucid.quantization.MovingAverageMinMaxObserver",
          "description": "EMA variant, robust to outliers."
        },
        {
          "name": "lucid.quantization.PerChannelMinMaxObserver",
          "description": "Per-channel granularity for weights."
        },
        {
          "name": "lucid.quantization.HistogramObserver",
          "description": "Tail-clipping for heavy-tailed activations."
        },
        {
          "name": "lucid.quantization.calculate_qparams",
          "description": "The range → `(scale, zero_point)` reduction."
        },
        {
          "name": "lucid.quantization.FakeQuantize",
          "description": "Wraps an observer for QAT."
        }
      ]
    },
    {
      "name": "MovingAverageMinMaxObserver",
      "path": "lucid.quantization.observer.MovingAverageMinMaxObserver",
      "kind": "class",
      "class_kind": "regular",
      "bases": [
        "MinMaxObserver"
      ],
      "labels": [],
      "signature": "MovingAverageMinMaxObserver(qscheme: QScheme = per_tensor_affine, qdtype: QDtype = quint8, averaging_constant: float = 0.01, eps: float = 1e-08)",
      "source": "https://github.com/ChanLumerico/lucid/blob/4b22d32cc/lucid/quantization/observer.py#L321",
      "subcategory": "observer",
      "methods": [
        {
          "name": "__init__",
          "path": "lucid.quantization.observer.MovingAverageMinMaxObserver.__init__",
          "kind": "function",
          "labels": [],
          "signature": "__init__(qscheme: QScheme = per_tensor_affine, qdtype: QDtype = quint8, averaging_constant: float = 0.01, eps: float = 1e-08)",
          "source": "https://github.com/ChanLumerico/lucid/blob/4b22d32cc/lucid/quantization/observer.py#L422",
          "subcategory": "observer",
          "summary": null,
          "extended": null,
          "parameters": [],
          "returns": {
            "annotation": "None",
            "description": ""
          },
          "raises": [],
          "examples": [],
          "notes": [],
          "attributes": [],
          "warns": [],
          "see_also": []
        },
        {
          "name": "forward",
          "path": "lucid.quantization.observer.MovingAverageMinMaxObserver.forward",
          "kind": "function",
          "labels": [],
          "signature": "forward(x: Tensor)",
          "source": "https://github.com/ChanLumerico/lucid/blob/4b22d32cc/lucid/quantization/observer.py#L432",
          "subcategory": "observer",
          "summary": "EMA-update the running min/max (seeded on the first batch).",
          "extended": null,
          "parameters": [],
          "returns": {
            "annotation": "Tensor",
            "description": ""
          },
          "raises": [],
          "examples": [],
          "notes": [],
          "attributes": [],
          "warns": [],
          "see_also": []
        }
      ],
      "summary": "Per-tensor observer with an exponential moving average of min / max.",
      "extended": "An outlier-tolerant refinement of `MinMaxObserver`. Instead of taking a hard\ncumulative min/max, it tracks an *exponential moving average* of each batch's range,\nso a single anomalous batch nudges the running range only slightly and is soon\nforgotten as later batches pull it back toward the bulk of the data. The result is a\ntighter, more representative grid than a plain min/max on noisy activation streams —\nat the cost of one hyperparameter, the averaging constant.\n\n**When to pick it.** Prefer this over `MinMaxObserver` whenever calibration\ndata may contain occasional spikes (most real activation streams do) and you want a\nrange that reflects the *typical* extreme rather than the absolute worst case. It is\nthe activation observer wrapped by `FakeQuantize` in\n`lucid.quantization.get_default_qat_qconfig`, where smoothing also stabilises\ntraining. For heavy-tailed distributions where you would rather *clip* the tail than\naverage it, `HistogramObserver` is stronger; for weights, use\n`MovingAveragePerChannelMinMaxObserver`.\n\nThe first batch *seeds* the range directly (the `±inf` seed cannot be averaged);\nevery later batch applies the EMA update with averaging constant $c$:\n\n$$\n\\text{min\\_val} \\leftarrow (1-c)\\,\\text{min\\_val} + c\\,(\\min_i x_i)\n    = \\text{min\\_val} + c\\,(\\min_i x_i - \\text{min\\_val}),\n$$\n\n$$\n\\text{max\\_val} \\leftarrow (1-c)\\,\\text{max\\_val} + c\\,(\\max_i x_i)\n    = \\text{max\\_val} + c\\,(\\max_i x_i - \\text{max\\_val}).\n$$\n\nSmaller $c$ means heavier smoothing (slower to react, more outlier-robust);\n$c = 1$ degenerates to using only the latest batch. The reduction to\n`(scale, zero_point)` is the same affine / symmetric map inherited from\n`MinMaxObserver`.",
      "parameters": [
        {
          "name": "qscheme",
          "annotation": "QScheme",
          "description": "Target quantization scheme.",
          "default": "per_tensor_affine"
        },
        {
          "name": "qdtype",
          "annotation": "QDtype",
          "description": "Target quantized dtype (unsigned `[0, 255]` grid).",
          "default": "quint8"
        },
        {
          "name": "averaging_constant",
          "annotation": "float",
          "description": "EMA weight $c \\in (0, 1]$ of each new batch's range; smaller is smoother\nand more outlier-robust, larger reacts faster.",
          "default": "0.01"
        },
        {
          "name": "eps",
          "annotation": "float",
          "description": "Lower floor on the derived `scale`.",
          "default": "1e-8"
        }
      ],
      "returns": null,
      "raises": [],
      "examples": [
        ">>> import lucid\n>>> import lucid.quantization as Q\n>>> obs = Q.MovingAverageMinMaxObserver(averaging_constant=0.1)\n>>> for _ in range(8):\n...     _ = obs(lucid.randn(16, 128))\n>>> scale, zero_point = obs.calculate_qparams()\n>>> bool(scale.item() > 0)\nTrue\nA lone outlier batch barely moves the smoothed range (contrast the hard-min/max\nobserver, which would absorb the full spike):\n>>> obs = Q.MovingAverageMinMaxObserver(averaging_constant=0.01)\n>>> _ = obs(lucid.zeros(1, 4))               # seed range ~ [0, 0]\n>>> _ = obs(lucid.full((1, 4), 1000.0))      # one huge batch\n>>> bool(obs.max_val.item() < 20.0)          # EMA absorbed only ~1% of the spike\nTrue"
      ],
      "notes": [
        "- **First-batch seeding.** `forward` detects the `+inf` seed and copies the first\n  batch's range in verbatim; the EMA recurrence applies only from the second batch\n  on. Without this the average would be corrupted by the infinite seed.\n- **Not cumulative.** Unlike `MinMaxObserver`, the range can *shrink* between\n  batches — this is exactly why a lone outlier does not permanently widen the grid.\n- **Buffer re-registration.** The EMA update is written back via `register_buffer`\n  (same name), per the `ObserverBase` contract.\n- **Symmetric vs affine** behaves as in the base class; `averaging_constant` only\n  affects the running range, not the range → qparams reduction.\n- Default activation observer of `lucid.quantization.get_default_qat_qconfig`\n  (wrapped in `FakeQuantize`)."
      ],
      "attributes": [
        {
          "name": "min_val",
          "annotation": "Tensor",
          "description": "Scalar (shape `()`) EMA of the batch minimum, seeded `+inf`."
        },
        {
          "name": "max_val",
          "annotation": "Tensor",
          "description": "Scalar (shape `()`) EMA of the batch maximum, seeded `-inf`."
        },
        {
          "name": "averaging_constant",
          "annotation": "float",
          "description": "The stored EMA weight $c$."
        }
      ],
      "warns": [],
      "see_also": [
        {
          "name": "lucid.quantization.MinMaxObserver",
          "description": "Hard cumulative min/max (no smoothing)."
        },
        {
          "name": "lucid.quantization.MovingAveragePerChannelMinMaxObserver",
          "description": "Per-channel EMA analogue."
        },
        {
          "name": "lucid.quantization.HistogramObserver",
          "description": "Tail-clipping alternative for outliers."
        },
        {
          "name": "lucid.quantization.get_default_qat_qconfig",
          "description": "Uses this observer for activations."
        },
        {
          "name": "lucid.quantization.FakeQuantize",
          "description": "Wraps it for quantization-aware training."
        }
      ]
    },
    {
      "name": "MovingAveragePerChannelMinMaxObserver",
      "path": "lucid.quantization.observer.MovingAveragePerChannelMinMaxObserver",
      "kind": "class",
      "class_kind": "regular",
      "bases": [
        "PerChannelMinMaxObserver"
      ],
      "labels": [],
      "signature": "MovingAveragePerChannelMinMaxObserver(ch_axis: int = 0, qscheme: QScheme = per_channel_symmetric, qdtype: QDtype = qint8, averaging_constant: float = 0.01, eps: float = 1e-08)",
      "source": "https://github.com/ChanLumerico/lucid/blob/4b22d32cc/lucid/quantization/observer.py#L825",
      "subcategory": "observer",
      "methods": [
        {
          "name": "__init__",
          "path": "lucid.quantization.observer.MovingAveragePerChannelMinMaxObserver.__init__",
          "kind": "function",
          "labels": [],
          "signature": "__init__(ch_axis: int = 0, qscheme: QScheme = per_channel_symmetric, qdtype: QDtype = qint8, averaging_constant: float = 0.01, eps: float = 1e-08)",
          "source": "https://github.com/ChanLumerico/lucid/blob/4b22d32cc/lucid/quantization/observer.py#L916",
          "subcategory": "observer",
          "summary": null,
          "extended": null,
          "parameters": [],
          "returns": {
            "annotation": "None",
            "description": ""
          },
          "raises": [],
          "examples": [],
          "notes": [],
          "attributes": [],
          "warns": [],
          "see_also": []
        },
        {
          "name": "forward",
          "path": "lucid.quantization.observer.MovingAveragePerChannelMinMaxObserver.forward",
          "kind": "function",
          "labels": [],
          "signature": "forward(x: Tensor)",
          "source": "https://github.com/ChanLumerico/lucid/blob/4b22d32cc/lucid/quantization/observer.py#L927",
          "subcategory": "observer",
          "summary": "EMA-update the per-channel min/max (seeded on the first batch).",
          "extended": null,
          "parameters": [],
          "returns": {
            "annotation": "Tensor",
            "description": ""
          },
          "raises": [],
          "examples": [],
          "notes": [],
          "attributes": [],
          "warns": [],
          "see_also": []
        }
      ],
      "summary": "Per-channel observer with an EMA of the per-channel min / max.",
      "extended": "The per-channel analogue of `MovingAverageMinMaxObserver`, and the\noutlier-tolerant sibling of `PerChannelMinMaxObserver`. It keeps an\nindependent range per channel along `ch_axis` (reducing over every other axis) but\nupdates each channel's `[min, max]` with an exponential moving average instead of a\nhard cumulative min/max — so a spike in one calibration batch nudges that channel's\nrange only slightly and is relaxed back by later batches. `calculate_qparams`\nstill returns a length-`C` vector of `(scale, zero_point)`.\n\n**When to pick it.** Use it for **weights** when weight calibration is noisy (e.g.\nrunning statistics observed during QAT rather than on static weights) and you want\nper-channel tightness *and* per-batch robustness. For static weights the plain\n`PerChannelMinMaxObserver` is usually enough; for activations, use the\nper-tensor `MovingAverageMinMaxObserver` or `HistogramObserver`.\n\nWith $x^{(c)}$ the values of channel $c$ in the current batch, the first\nbatch seeds each channel directly and every later batch applies the per-channel EMA:\n\n$$\n\\text{min\\_val}_c \\leftarrow \\text{min\\_val}_c\n    + c\\,\\bigl(\\min x^{(c)} - \\text{min\\_val}_c\\bigr),\\qquad\n\\text{max\\_val}_c \\leftarrow \\text{max\\_val}_c\n    + c\\,\\bigl(\\max x^{(c)} - \\text{max\\_val}_c\\bigr),\n$$\n\nwhere $c$ is `averaging_constant`. The reduction to per-channel qparams is\nthe (default symmetric, signed) map inherited from\n`PerChannelMinMaxObserver`, pinning each `z_c = 0`.",
      "parameters": [
        {
          "name": "ch_axis",
          "annotation": "int",
          "description": "Output-channel axis quantized independently (0 for conv / linear weights).",
          "default": "0"
        },
        {
          "name": "qscheme",
          "annotation": "QScheme",
          "description": "Target quantization scheme.",
          "default": "per_channel_symmetric"
        },
        {
          "name": "qdtype",
          "annotation": "QDtype",
          "description": "Target quantized dtype (signed `[-128, 127]` grid).",
          "default": "qint8"
        },
        {
          "name": "averaging_constant",
          "annotation": "float",
          "description": "EMA weight $c \\in (0, 1]$ of each new batch's per-channel range; smaller is\nsmoother / slower / more outlier-robust.",
          "default": "0.01"
        },
        {
          "name": "eps",
          "annotation": "float",
          "description": "Lower floor on each derived per-channel `scale`.",
          "default": "1e-8"
        }
      ],
      "returns": null,
      "raises": [],
      "examples": [
        ">>> import lucid\n>>> import lucid.quantization as Q\n>>> obs = Q.MovingAveragePerChannelMinMaxObserver(ch_axis=0, averaging_constant=0.1)\n>>> for _ in range(8):\n...     _ = obs(lucid.randn(12, 32))         # 12 output channels\n>>> scale, zero_point = obs.calculate_qparams()\n>>> scale.shape\n(12,)\n>>> bool((zero_point == 0).all().item())     # per-channel symmetric ⇒ z = 0\nTrue"
      ],
      "notes": [
        "- **First-batch seeding is explicit.** Because the scalar `±inf` seed cannot be\n  broadcast into an elementwise `where` against a length-`C` batch range,\n  `forward` special-cases the first observation and copies the per-channel range in\n  directly; the EMA recurrence runs only from the second batch on.\n- **Not cumulative** — each channel's range can shrink between batches (the whole\n  point of the EMA), unlike `PerChannelMinMaxObserver`.\n- **Buffer re-registration** carries both the scalar-seed → `(C,)` shape change and\n  every later in-place EMA update, per the `ObserverBase` contract.\n- **Symmetric vs affine** behaves as in the per-channel base class; the default pins\n  each `zero_point` to 0."
      ],
      "attributes": [
        {
          "name": "min_val",
          "annotation": "Tensor",
          "description": "Per-channel EMA minimum, shape `(C,)` after the first batch (scalar `+inf`\nseed before it)."
        },
        {
          "name": "max_val",
          "annotation": "Tensor",
          "description": "Per-channel EMA maximum, shape `(C,)` after the first batch (scalar `-inf`\nseed before it)."
        },
        {
          "name": "averaging_constant",
          "annotation": "float",
          "description": "The stored EMA weight $c$."
        }
      ],
      "warns": [],
      "see_also": [
        {
          "name": "lucid.quantization.PerChannelMinMaxObserver",
          "description": "Hard per-channel min/max (no smoothing)."
        },
        {
          "name": "lucid.quantization.MovingAverageMinMaxObserver",
          "description": "Per-tensor EMA counterpart."
        },
        {
          "name": "lucid.quantization.get_default_qat_qconfig",
          "description": "QAT recipe using per-channel weights."
        },
        {
          "name": "lucid.quantization.calculate_qparams",
          "description": "The per-channel range → qparams reduction."
        },
        {
          "name": "lucid.quantization.FakeQuantize",
          "description": "Wraps an observer for QAT."
        }
      ]
    },
    {
      "name": "NoopObserver",
      "path": "lucid.quantization.observer.NoopObserver",
      "kind": "class",
      "class_kind": "regular",
      "bases": [
        "PlaceholderObserver"
      ],
      "labels": [],
      "signature": "NoopObserver(qscheme: QScheme = per_tensor_affine, qdtype: QDtype = quint8, eps: float = 1e-08)",
      "source": "https://github.com/ChanLumerico/lucid/blob/4b22d32cc/lucid/quantization/observer.py#L1160",
      "subcategory": "observer",
      "methods": [],
      "summary": "A no-op `PlaceholderObserver` — pure identity, collects no statistics.",
      "extended": "The intent-revealing name for a `PlaceholderObserver`: a tensor should flow\nthrough the observed graph **without any statistic gathering at all**. Whereas a bare\n`PlaceholderObserver` reads as \"qparams come from elsewhere (dynamic quant)\",\n`NoopObserver` reads as \"this tensor is deliberately *not* quantized here\" — an\nalready-quantized input, a residual passed straight through, or a layer explicitly\nexcluded from calibration. Behaviourally the two are identical: `forward` is the\nidentity and `calculate_qparams` raises. Only the name (and the reader's\nintent) differs.\n\n**When to pick it.** Use it to annotate a passthrough tensor in a QConfig / mapping\nso the tooling records the \"leave alone\" intent explicitly rather than by omission.\nWhen qparams really are produced at runtime, prefer `PlaceholderObserver`;\nwhen a fixed grid is known, use `FixedQParamsObserver`.\n\nLike its parent it holds no statistic and its qparams query is an error:\n\n$$\n\\text{forward}(x) = x,\\qquad\n\\text{calculate\\_qparams}() \\;\\Rightarrow\\;\n\\textbf{raise } \\texttt{RuntimeError}\n$$",
      "parameters": [
        {
          "name": "qscheme",
          "annotation": "QScheme",
          "description": "Target quantization scheme recorded for downstream consumers (metadata only).",
          "default": "per_tensor_affine"
        },
        {
          "name": "qdtype",
          "annotation": "QDtype",
          "description": "Target quantized dtype recorded for downstream consumers (metadata only).",
          "default": "quint8"
        },
        {
          "name": "eps",
          "annotation": "float",
          "description": "Retained for interface symmetry; unused (no range is derived).",
          "default": "1e-8"
        }
      ],
      "returns": null,
      "raises": [],
      "examples": [
        ">>> import lucid\n>>> import lucid.quantization as Q\n>>> obs = Q.NoopObserver()\n>>> x = lucid.randn(2, 3)\n>>> bool((obs(x) == x).all().item())         # identity passthrough\nTrue\n>>> isinstance(obs, Q.PlaceholderObserver)   # it *is* a PlaceholderObserver\nTrue"
      ],
      "notes": [
        "- **Pure alias.** Inherits the `qscheme` / `qdtype` / `eps` constructor and all\n  behaviour of `PlaceholderObserver` unchanged; it overrides nothing.\n- **Identity forward, raising qparams** — same contract as the parent; the class\n  exists to make \"no observation\" a first-class, greppable choice.\n- **No buffers**, so nothing is added to the `state_dict` beyond the metadata."
      ],
      "attributes": [],
      "warns": [],
      "see_also": [
        {
          "name": "lucid.quantization.PlaceholderObserver",
          "description": "The parent — dtype carrier, qparams raise."
        },
        {
          "name": "lucid.quantization.FixedQParamsObserver",
          "description": "Fixed qparams for range-bounded ops."
        },
        {
          "name": "lucid.quantization.MinMaxObserver",
          "description": "Statistics-collecting observer for static ranges."
        },
        {
          "name": "lucid.quantization.FakeQuantize",
          "description": "Wraps an observer for QAT."
        }
      ]
    },
    {
      "name": "ObserverBase",
      "path": "lucid.quantization.observer.ObserverBase",
      "kind": "class",
      "class_kind": "regular",
      "bases": [
        "Module"
      ],
      "labels": [],
      "signature": "ObserverBase(qscheme: QScheme, qdtype: QDtype, ch_axis: int | None = None, eps: float = 1e-08)",
      "source": "https://github.com/ChanLumerico/lucid/blob/4b22d32cc/lucid/quantization/observer.py#L42",
      "subcategory": "observer",
      "methods": [
        {
          "name": "__init__",
          "path": "lucid.quantization.observer.ObserverBase.__init__",
          "kind": "function",
          "labels": [],
          "signature": "__init__(qscheme: QScheme, qdtype: QDtype, ch_axis: int | None = None, eps: float = 1e-08)",
          "source": "https://github.com/ChanLumerico/lucid/blob/4b22d32cc/lucid/quantization/observer.py#L166",
          "subcategory": "observer",
          "summary": null,
          "extended": null,
          "parameters": [],
          "returns": {
            "annotation": "None",
            "description": ""
          },
          "raises": [],
          "examples": [],
          "notes": [],
          "attributes": [],
          "warns": [],
          "see_also": []
        },
        {
          "name": "calculate_qparams",
          "path": "lucid.quantization.observer.ObserverBase.calculate_qparams",
          "kind": "function",
          "labels": [],
          "signature": "calculate_qparams()",
          "source": "https://github.com/ChanLumerico/lucid/blob/4b22d32cc/lucid/quantization/observer.py#L179",
          "subcategory": "observer",
          "summary": "Return `(scale, zero_point)` from the accumulated statistics.",
          "extended": null,
          "parameters": [],
          "returns": {
            "annotation": "tuple[Tensor, Tensor]",
            "description": ""
          },
          "raises": [],
          "examples": [],
          "notes": [],
          "attributes": [],
          "warns": [],
          "see_also": []
        },
        {
          "name": "with_args",
          "path": "lucid.quantization.observer.ObserverBase.with_args",
          "kind": "function",
          "labels": [
            "classmethod"
          ],
          "signature": "with_args(kwargs: object = {})",
          "source": "https://github.com/ChanLumerico/lucid/blob/4b22d32cc/lucid/quantization/observer.py#L183",
          "subcategory": "observer",
          "summary": "Return a zero-arg factory that builds this observer with `kwargs`.",
          "extended": "Mirrors the reference framework's `Observer.with_args` ergonomic so\na `lucid.quantization.QConfig` can carry an observer *recipe*\nrather than a live instance.",
          "parameters": [],
          "returns": {
            "annotation": "functools.partial[ObserverBase]",
            "description": ""
          },
          "raises": [],
          "examples": [],
          "notes": [],
          "attributes": [],
          "warns": [],
          "see_also": []
        }
      ],
      "summary": "Abstract base for calibration observers that yield `(scale, zero_point)`.",
      "extended": "An *observer* is the calibration workhorse of the quantization subsystem: an\n`lucid.nn.Module` that watches a tensor stream during a calibration pass,\naccumulates a summary statistic (a running min/max, a channel-wise range, or a value\nhistogram), and on demand distills that statistic into the `(scale, zero_point)`\npair a quantized tensor needs. Its `forward` is the *identity* — it observes `x`\nand returns it untouched — so an observer can be spliced inline into a prepared graph\nor wrapped by a `FakeQuantize` without perturbing numerics. Concrete\nsubclasses override `forward` (to fold each batch into the running statistic)\nand `calculate_qparams` (to reduce that statistic to qparams).\n\n**What `calculate_qparams` computes.** Every min/max-family subclass funnels its\nobserved range through the shared affine / symmetric calibration in\n`lucid.quantization.calculate_qparams`. The range is first widened to include\nreal zero (so `0.0` is always representable), then mapped onto the integer grid\n`[q_min, q_max]` of `qdtype`:\n\n$$\n\\text{affine:}\\quad\ns = \\frac{\\max - \\min}{q_{\\max} - q_{\\min}},\\qquad\nz = \\operatorname{clip}\\!\\bigl(q_{\\min} - \\operatorname{round}(\\min/s),\\,\n    q_{\\min},\\, q_{\\max}\\bigr)\n$$\n\n$$\n\\text{symmetric:}\\quad\ns = \\frac{\\max(|\\min|,\\,|\\max|)}{(q_{\\max} - q_{\\min})/2},\\qquad\nz = \\begin{cases} 0 & \\text{signed} \\\\\n     \\lfloor (q_{\\max}+q_{\\min}+1)/2 \\rfloor & \\text{unsigned} \\end{cases}\n$$\n\nwith a floor $s \\leftarrow \\max(s, \\varepsilon)$ guarding a degenerate\n(constant-input) range. Affine spends a free `zero_point` to pack an asymmetric\nrange tightly (ideal for post-ReLU activations); symmetric pins `zero_point` so\nreal 0 lands exactly on a code (the standard choice for signed weights).\n\n**Choosing a subclass.** Pick along three axes. *Granularity* — per-tensor\n(`MinMaxObserver`, one pair for the whole tensor) vs per-channel\n(`PerChannelMinMaxObserver`, one pair per output channel, far tighter for\nconv / linear weights whose channels differ in range). *Outlier robustness* — a hard\nrunning min/max grabs every spike, an EMA variant\n(`MovingAverageMinMaxObserver`) smooths per-batch outliers, and\n`HistogramObserver` clips the tails to minimise L2 error on heavy-tailed\nactivations. *Fixed-range* — ops whose output range is known a priori use\n`FixedQParamsObserver`, and dtype-only carriers use\n`PlaceholderObserver` / `NoopObserver`.",
      "parameters": [
        {
          "name": "qscheme",
          "annotation": "QScheme",
          "description": "Target quantization scheme — one of `per_tensor_affine` /\n`per_tensor_symmetric` / `per_channel_affine` / `per_channel_symmetric`.\nSelects the affine-vs-symmetric branch above and whether qparams are scalar or\nper-channel vectors.",
          "default": null
        },
        {
          "name": "qdtype",
          "annotation": "QDtype",
          "description": "Target quantized dtype supplying the grid bounds `quant_min` / `quant_max`\n(e.g. `quint8` → `[0, 255]`, `qint8` → `[-128, 127]`) and the on-device\nstorage dtype that physically holds the integer codes.",
          "default": null
        },
        {
          "name": "ch_axis",
          "annotation": "int",
          "description": "Channel axis quantized independently. Forced to `None` for per-tensor schemes\nregardless of the value passed.",
          "default": "None"
        },
        {
          "name": "eps",
          "annotation": "float",
          "description": "Lower floor on the derived `scale`, forwarded to\n`lucid.quantization.calculate_qparams` to avoid division by zero.",
          "default": "1e-8"
        }
      ],
      "returns": null,
      "raises": [],
      "examples": [
        ">>> import lucid\n>>> import lucid.quantization as Q\n>>> obs = Q.MinMaxObserver()            # a concrete ObserverBase subclass\n>>> _ = obs(lucid.randn(64, 128))       # forward observes and returns x unchanged\n>>> scale, zero_point = obs.calculate_qparams()\n>>> scale.shape, zero_point.shape\n((), ())\nDeferring construction into a lucid.quantization.QConfig recipe:\n>>> factory = Q.PerChannelMinMaxObserver.with_args(ch_axis=0)\n>>> obs2 = factory()                    # zero-arg factory builds a fresh observer\n>>> type(obs2).__name__\n'PerChannelMinMaxObserver'"
      ],
      "notes": [
        "- **Identity forward.** `forward` returns its input unchanged; only the side effect\n  of updating the running buffers matters. This is what lets an observer be spliced\n  into a prepared model transparently.\n- **Buffer re-registration contract.** Subclasses update their running statistics by\n  *re-registering* the buffer (`register_buffer` with the same name), never by\n  plain attribute assignment: Lucid's `Module.__setattr__` drops a name from\n  `_buffers` on reassignment, so re-registration is the only correct in-place\n  update — and it transparently absorbs the scalar-seed → per-channel-vector shape\n  change on the first observation.\n- **Not instantiated directly.** `ObserverBase` is abstract; its\n  `calculate_qparams` raises `NotImplementedError`. Use a concrete\n  subclass, or `with_args` to defer construction into a\n  `lucid.quantization.QConfig`.\n- **H4 purity.** Every statistic is computed on the public `lucid.*` op surface —\n  no external numeric libraries — so the same code path is correct on both the\n  Accelerate (CPU) and MLX (GPU) streams."
      ],
      "attributes": [
        {
          "name": "qscheme",
          "annotation": "QScheme",
          "description": "The stored target scheme."
        },
        {
          "name": "qdtype",
          "annotation": "QDtype",
          "description": "The stored target dtype."
        },
        {
          "name": "ch_axis",
          "annotation": "int or None",
          "description": "The effective channel axis (`None` unless the scheme is per-channel)."
        },
        {
          "name": "eps",
          "annotation": "float",
          "description": "The stored scale floor."
        }
      ],
      "warns": [],
      "see_also": [
        {
          "name": "lucid.quantization.MinMaxObserver",
          "description": "Per-tensor running min/max (the common default)."
        },
        {
          "name": "lucid.quantization.PerChannelMinMaxObserver",
          "description": "Per-channel weight observer."
        },
        {
          "name": "lucid.quantization.HistogramObserver",
          "description": "Tail-clipping observer for activations."
        },
        {
          "name": "lucid.quantization.calculate_qparams",
          "description": "The shared range → `(scale, zero_point)` map."
        },
        {
          "name": "lucid.quantization.QConfig",
          "description": "Pairs activation + weight observer factories."
        },
        {
          "name": "lucid.quantization.FakeQuantize",
          "description": "Wraps an observer for quantization-aware training."
        }
      ]
    },
    {
      "name": "PerChannelMinMaxObserver",
      "path": "lucid.quantization.observer.PerChannelMinMaxObserver",
      "kind": "class",
      "class_kind": "regular",
      "bases": [
        "ObserverBase"
      ],
      "labels": [],
      "signature": "PerChannelMinMaxObserver(ch_axis: int = 0, qscheme: QScheme = per_channel_symmetric, qdtype: QDtype = qint8, eps: float = 1e-08)",
      "source": "https://github.com/ChanLumerico/lucid/blob/4b22d32cc/lucid/quantization/observer.py#L450",
      "subcategory": "observer",
      "methods": [
        {
          "name": "__init__",
          "path": "lucid.quantization.observer.PerChannelMinMaxObserver.__init__",
          "kind": "function",
          "labels": [],
          "signature": "__init__(ch_axis: int = 0, qscheme: QScheme = per_channel_symmetric, qdtype: QDtype = qint8, eps: float = 1e-08)",
          "source": "https://github.com/ChanLumerico/lucid/blob/4b22d32cc/lucid/quantization/observer.py#L565",
          "subcategory": "observer",
          "summary": null,
          "extended": null,
          "parameters": [],
          "returns": {
            "annotation": "None",
            "description": ""
          },
          "raises": [],
          "examples": [],
          "notes": [],
          "attributes": [],
          "warns": [],
          "see_also": []
        },
        {
          "name": "forward",
          "path": "lucid.quantization.observer.PerChannelMinMaxObserver.forward",
          "kind": "function",
          "labels": [],
          "signature": "forward(x: Tensor)",
          "source": "https://github.com/ChanLumerico/lucid/blob/4b22d32cc/lucid/quantization/observer.py#L576",
          "subcategory": "observer",
          "summary": "Fold per-channel min/max (reduced over all non-channel axes).",
          "extended": null,
          "parameters": [],
          "returns": {
            "annotation": "Tensor",
            "description": ""
          },
          "raises": [],
          "examples": [],
          "notes": [],
          "attributes": [],
          "warns": [],
          "see_also": []
        },
        {
          "name": "calculate_qparams",
          "path": "lucid.quantization.observer.PerChannelMinMaxObserver.calculate_qparams",
          "kind": "function",
          "labels": [],
          "signature": "calculate_qparams()",
          "source": "https://github.com/ChanLumerico/lucid/blob/4b22d32cc/lucid/quantization/observer.py#L590",
          "subcategory": "observer",
          "summary": "Delegate to the wrapped observer.",
          "extended": null,
          "parameters": [],
          "returns": {
            "annotation": "tuple[Tensor, Tensor]",
            "description": ""
          },
          "raises": [],
          "examples": [],
          "notes": [],
          "attributes": [],
          "warns": [],
          "see_also": []
        }
      ],
      "summary": "Per-channel observer tracking a running min / max along `ch_axis`.",
      "extended": "The standard **weight** observer. Rather than one range for the whole tensor, it\nkeeps an independent `[min, max]` for every slice along `ch_axis` — reducing over\nall *other* axes — so `calculate_qparams` returns a length-`C` *vector* of\n`(scale, zero_point)`, one per output channel. Because different output neurons /\nfilters routinely span wildly different dynamic ranges, a single per-tensor scale is\ndominated by the widest channel and quantizes the rest coarsely; a per-channel scale\ntracks each one tightly, which is why per-channel symmetric `qint8` is the standard\naccuracy-preserving choice for convolution and linear weights.\n\n**When to pick it.** Use it for **weights** — it is the weight observer in both\n`lucid.quantization.get_default_qconfig` (static PTQ) and\n`lucid.quantization.get_default_qat_qconfig` (QAT, wrapped in\n`FakeQuantize`). It is *not* used for activations, whose channel layout varies\nper op and which are cheaper to quantize per-tensor. For a smoothed per-channel range\nover noisy weight calibration, use `MovingAveragePerChannelMinMaxObserver`.\n\nWriting the input with `ch_axis` moved to the front and the rest flattened,\n$x^{(c)}$ denotes the values of channel $c$; each channel keeps its own\ncumulative range:\n\n$$\n\\text{min\\_val}_c \\leftarrow \\min\\!\\bigl(\\text{min\\_val}_c,\\ \\min x^{(c)}\\bigr),\n\\qquad\n\\text{max\\_val}_c \\leftarrow \\max\\!\\bigl(\\text{max\\_val}_c,\\ \\max x^{(c)}\\bigr)\n$$\n\nthen the (default symmetric, signed) reduction pins `zero_point` to 0 and scales\neach channel by its own peak magnitude:\n\n$$\ns_c = \\frac{\\max(|\\text{min\\_val}_c|,\\ |\\text{max\\_val}_c|)}\n           {(q_{\\max} - q_{\\min})/2},\\qquad z_c = 0\n$$\n\n(an affine `qscheme` instead yields per-channel `(s_c, z_c)` via the base-class\naffine map).",
      "parameters": [
        {
          "name": "ch_axis",
          "annotation": "int",
          "description": "Axis whose entries are quantized independently — the output-channel axis, which\nis 0 for both conv weights `(out, in, kh, kw)` and linear weights `(out, in)`.",
          "default": "0"
        },
        {
          "name": "qscheme",
          "annotation": "QScheme",
          "description": "Target quantization scheme; the per-channel default pins `zero_point` per\nchannel.",
          "default": "per_channel_symmetric"
        },
        {
          "name": "qdtype",
          "annotation": "QDtype",
          "description": "Target quantized dtype; `qint8` gives the signed grid `[-128, 127]`.",
          "default": "qint8"
        },
        {
          "name": "eps",
          "annotation": "float",
          "description": "Lower floor on each derived per-channel `scale`.",
          "default": "1e-8"
        }
      ],
      "returns": null,
      "raises": [],
      "examples": [
        ">>> import lucid\n>>> import lucid.quantization as Q\n>>> obs = Q.PerChannelMinMaxObserver(ch_axis=0)\n>>> _ = obs(lucid.randn(16, 8))              # 16 output channels\n>>> scale, zero_point = obs.calculate_qparams()\n>>> scale.shape                              # one scale per output channel\n(16,)\n>>> bool((zero_point == 0).all().item())     # symmetric ⇒ zero_point pinned to 0\nTrue\nPer-channel is far tighter than per-tensor when channels differ in range — here one\nrow is ~1000x larger, yet each row keeps its own scale:\n>>> import lucid\n>>> w = lucid.stack([lucid.ones(4) * 0.001, lucid.ones(4) * 1.0])\n>>> obs = Q.PerChannelMinMaxObserver(ch_axis=0)\n>>> _ = obs(w)\n>>> s, _ = obs.calculate_qparams()\n>>> bool(s[0].item() < s[1].item())          # small-range row ⇒ smaller step\nTrue"
      ],
      "notes": [
        "- **Scalar seed → per-channel vector.** The buffers start as scalar `±inf`; the\n  first `forward` broadcasts that seed against the length-`C` batch range, so\n  `min_val` / `max_val` acquire shape `(C,)` on the first observation. This\n  shape change rides on the buffer re-registration contract (plain assignment would\n  drop the buffer from `_buffers`).\n- **Cumulative** per channel — like `MinMaxObserver`, the ranges only widen,\n  so the observer is outlier-sensitive; smooth with the moving-average variant.\n- **Symmetric vs affine.** The default per-channel-symmetric scheme fixes\n  `zero_point = 0` (signed weights map 0 → 0 exactly); an affine scheme gives each\n  channel a free zero-point.\n- **Reduction axes.** Every axis except `ch_axis` is reduced, so the number of\n  qparams equals `x.shape[ch_axis]`.\n- Weight observer of both default QConfigs\n  (`lucid.quantization.get_default_qconfig` and\n  `lucid.quantization.get_default_qat_qconfig`)."
      ],
      "attributes": [
        {
          "name": "min_val",
          "annotation": "Tensor",
          "description": "Per-channel running minimum, shape `(C,)` after the first batch (scalar\n`+inf` seed before it)."
        },
        {
          "name": "max_val",
          "annotation": "Tensor",
          "description": "Per-channel running maximum, shape `(C,)` after the first batch (scalar\n`-inf` seed before it)."
        }
      ],
      "warns": [],
      "see_also": [
        {
          "name": "lucid.quantization.MinMaxObserver",
          "description": "Per-tensor counterpart."
        },
        {
          "name": "lucid.quantization.MovingAveragePerChannelMinMaxObserver",
          "description": "Per-channel EMA variant."
        },
        {
          "name": "lucid.quantization.get_default_qconfig",
          "description": "Uses this as the weight observer."
        },
        {
          "name": "lucid.nn.quantized.Linear",
          "description": "Consumes per-channel weight qparams at convert time."
        },
        {
          "name": "lucid.quantization.calculate_qparams",
          "description": "The per-channel range → qparams reduction."
        }
      ]
    },
    {
      "name": "PlaceholderObserver",
      "path": "lucid.quantization.observer.PlaceholderObserver",
      "kind": "class",
      "class_kind": "regular",
      "bases": [
        "ObserverBase"
      ],
      "labels": [],
      "signature": "PlaceholderObserver(qscheme: QScheme = per_tensor_affine, qdtype: QDtype = quint8, eps: float = 1e-08)",
      "source": "https://github.com/ChanLumerico/lucid/blob/4b22d32cc/lucid/quantization/observer.py#L1062",
      "subcategory": "observer",
      "methods": [
        {
          "name": "__init__",
          "path": "lucid.quantization.observer.PlaceholderObserver.__init__",
          "kind": "function",
          "labels": [],
          "signature": "__init__(qscheme: QScheme = per_tensor_affine, qdtype: QDtype = quint8, eps: float = 1e-08)",
          "source": "https://github.com/ChanLumerico/lucid/blob/4b22d32cc/lucid/quantization/observer.py#L1139",
          "subcategory": "observer",
          "summary": null,
          "extended": null,
          "parameters": [],
          "returns": {
            "annotation": "None",
            "description": ""
          },
          "raises": [],
          "examples": [],
          "notes": [],
          "attributes": [],
          "warns": [],
          "see_also": []
        },
        {
          "name": "forward",
          "path": "lucid.quantization.observer.PlaceholderObserver.forward",
          "kind": "function",
          "labels": [],
          "signature": "forward(x: Tensor)",
          "source": "https://github.com/ChanLumerico/lucid/blob/4b22d32cc/lucid/quantization/observer.py#L1147",
          "subcategory": "observer",
          "summary": "Identity — no statistics are gathered.",
          "extended": null,
          "parameters": [],
          "returns": {
            "annotation": "Tensor",
            "description": ""
          },
          "raises": [],
          "examples": [],
          "notes": [],
          "attributes": [],
          "warns": [],
          "see_also": []
        },
        {
          "name": "calculate_qparams",
          "path": "lucid.quantization.observer.PlaceholderObserver.calculate_qparams",
          "kind": "function",
          "labels": [],
          "signature": "calculate_qparams()",
          "source": "https://github.com/ChanLumerico/lucid/blob/4b22d32cc/lucid/quantization/observer.py#L1152",
          "subcategory": "observer",
          "summary": "Return `(scale, zero_point)` from the accumulated statistics.",
          "extended": null,
          "parameters": [],
          "returns": {
            "annotation": "tuple[Tensor, Tensor]",
            "description": ""
          },
          "raises": [],
          "examples": [],
          "notes": [],
          "attributes": [],
          "warns": [],
          "see_also": []
        }
      ],
      "summary": "Carries `qdtype` / `qscheme` metadata but collects no statistics.",
      "extended": "A pure metadata carrier for the cases where a range cannot — or should not — be\ncalibrated ahead of time. The prototypical use is **dynamic quantization**, where an\nactivation's `(scale, zero_point)` is computed *at runtime* from the actual input\nof each forward, so estimating a static range during a calibration pass would be\nmeaningless. The observer still needs to advertise the *target* dtype and scheme to\ndownstream consumers (the convert pass, the quantized module), which is exactly what\nit records — and nothing more. Its `forward` is the identity, and because there is\nno statistic to reduce, `calculate_qparams` deliberately **raises**.\n\n**When to pick it.** Use it wherever qparams are supplied elsewhere or produced\nlater: the activation slot of a dynamic-quant recipe, or a tensor deliberately\nexcluded from static calibration. If the range *is* fixed and known, use\n`FixedQParamsObserver` (whose `calculate_qparams` returns the constants\ninstead of raising); if the range should be *learned* from data, use a\nstatistics-collecting observer such as `MinMaxObserver` or\n`HistogramObserver`.\n\nThere is no statistic and no reduction — only the recorded metadata; asking for\nqparams is an error:\n\n$$\n\\text{calculate\\_qparams}() \\;\\Rightarrow\\;\n\\textbf{raise } \\texttt{RuntimeError}\n$$",
      "parameters": [
        {
          "name": "qscheme",
          "annotation": "QScheme",
          "description": "Target quantization scheme recorded for downstream consumers.",
          "default": "per_tensor_affine"
        },
        {
          "name": "qdtype",
          "annotation": "QDtype",
          "description": "Target quantized dtype recorded for downstream consumers (grid + storage).",
          "default": "quint8"
        },
        {
          "name": "eps",
          "annotation": "float",
          "description": "Retained for interface symmetry with `ObserverBase`; unused because no\nrange is ever derived.",
          "default": "1e-8"
        }
      ],
      "returns": null,
      "raises": [],
      "examples": [
        ">>> import lucid\n>>> import lucid.quantization as Q\n>>> obs = Q.PlaceholderObserver()\n>>> _ = obs(lucid.randn(4, 8))               # identity: observes nothing\n>>> try:\n...     obs.calculate_qparams()              # no statistic ⇒ raises\n... except RuntimeError as e:\n...     print(\"raised\")\nraised"
      ],
      "notes": [
        "- **No buffers.** Unlike the min/max family, this observer registers no running\n  statistic — there is nothing to persist beyond the metadata fields.\n- **Identity forward.** `forward` returns `x` unchanged and gathers nothing.\n- **`calculate_qparams` raises.** It throws `RuntimeError` by design — the\n  qparams are produced at runtime (dynamic quantization) or supplied elsewhere, so\n  there is no stored range to return. Callers must not treat this observer as a\n  static-range source.\n- **Relation to** `NoopObserver` — `NoopObserver` is a named subclass with\n  identical behaviour, used to mark tensors that should pass through untouched."
      ],
      "attributes": [
        {
          "name": "qscheme",
          "annotation": "QScheme",
          "description": "The recorded target scheme (metadata only)."
        },
        {
          "name": "qdtype",
          "annotation": "QDtype",
          "description": "The recorded target dtype (metadata only)."
        }
      ],
      "warns": [],
      "see_also": [
        {
          "name": "lucid.quantization.NoopObserver",
          "description": "Named no-op subclass with the same behaviour."
        },
        {
          "name": "lucid.quantization.FixedQParamsObserver",
          "description": "Fixed qparams (returns, does not raise)."
        },
        {
          "name": "lucid.quantization.MinMaxObserver",
          "description": "Statistics-collecting observer for static ranges."
        },
        {
          "name": "lucid.quantization.FakeQuantize",
          "description": "Wraps an observer for QAT."
        }
      ]
    },
    {
      "name": "QConfig",
      "path": "lucid.quantization.qconfig.QConfig",
      "kind": "class",
      "class_kind": "regular",
      "bases": [
        "NamedTuple"
      ],
      "labels": [],
      "signature": "QConfig()",
      "source": "https://github.com/ChanLumerico/lucid/blob/4b22d32cc/lucid/quantization/qconfig.py#L35",
      "subcategory": "qconfig",
      "methods": [],
      "summary": "The atomic `(activation, weight)` observer / fake-quant recipe.",
      "extended": "A `QConfig` is the smallest unit of quantization *policy*: it pairs the\nrecipe for how **activations** are observed / fake-quantized with the recipe for\nhow **weights** are. It carries no state of its own — only two *factories*. When\n`lucid.quantization.prepare` (static PTQ) or\n`lucid.quantization.prepare_qat` (QAT) walks a model and decides a module\nshould be quantized, it calls `qconfig.activation()` and `qconfig.weight()` to\nmint a **fresh** observer (PTQ) or `lucid.quantization.FakeQuantize` (QAT)\nfor that specific insertion site. One `QConfig` therefore parameterizes the\nquantization of an entire network without any two layers sharing observer state.\n\nConceptually it is the direct analogue of an AMP autocast policy: a small,\ndeclarative object the framework consults while rewriting the graph, rather than\nsomething that participates in the forward pass itself. A `QConfigMapping`\nsits one level above and answers the orthogonal question of *which* QConfig\napplies to *which* module.\n\n**Why factories, not instances.** Both fields hold zero-arg callables — almost\nalways produced by `Observer.with_args(...)` or `FakeQuantize.with_args(...)`,\nwhich capture the desired `lucid.quantization.QScheme` /\n`lucid.quantization.QDtype` / `ch_axis` and defer construction. Storing\nfactories (not built modules) is what lets one recipe instantiate independent,\nper-layer observers across the whole model; sharing a single observer instance\nbetween layers would conflate their statistics and corrupt calibration.",
      "parameters": [],
      "returns": null,
      "raises": [],
      "examples": [
        "Build a recipe by hand from two with_args factories:\n>>> import lucid.quantization as Q\n>>> from lucid.quantization.observer import (\n...     HistogramObserver, PerChannelMinMaxObserver)\n>>> qcfg = Q.QConfig(\n...     activation=HistogramObserver.with_args(\n...         qscheme=Q.per_tensor_affine, qdtype=Q.quint8),\n...     weight=PerChannelMinMaxObserver.with_args(\n...         ch_axis=0, qscheme=Q.per_channel_symmetric, qdtype=Q.qint8),\n... )\n>>> act_observer = qcfg.activation()   # fresh observer for one insertion site\n>>> wt_observer = qcfg.weight()        # independent observer, no shared state\n>>> type(act_observer).__name__\n'HistogramObserver'\nIn practice the defaults cover the common case, so hand-building is rare:\n>>> qcfg = Q.get_default_qconfig()       # static-PTQ recipe\n>>> qat = Q.get_default_qat_qconfig()    # QAT recipe (FakeQuantize-wrapped)"
      ],
      "notes": [
        "- **Factory semantics (`with_args`).** `HistogramObserver.with_args(qscheme=...,\n  qdtype=...)` returns a zero-arg callable (a captured partial); calling it yields\n  a configured observer. This is why the two fields are typed\n  `Callable[..., nn.Module]` and not `nn.Module`.\n- **PTQ vs QAT.** For static PTQ the factories build plain *observers* (which only\n  record statistics); for QAT they build `lucid.quantization.FakeQuantize`\n  modules (which also apply straight-through rounding so gradients see the\n  quantization error). The two default recipes differ only in that wrapper.\n- **Pairing, not a scalar.** Activations and weights almost always want *different*\n  schemes — activations are asymmetric (post-ReLU ranges start at 0, hence\n  per-tensor-affine) while weights are near-zero-mean (hence per-channel-symmetric).\n  Bundling both keeps that pairing explicit and consistent.\n- Being a `typing.NamedTuple`, a `QConfig` is immutable and unpacks\n  positionally as `activation, weight = qconfig`."
      ],
      "attributes": [
        {
          "name": "activation",
          "annotation": "Callable[..., nn.Module]",
          "description": "Factory for the **activation** observer / fake-quant. Called with no arguments\nat every insertion site to build a fresh module that tracks the dynamic range\nof that layer's *output* activations."
        },
        {
          "name": "weight",
          "annotation": "Callable[..., nn.Module]",
          "description": "Factory for the **weight** observer / fake-quant. Called with no arguments to\nbuild a fresh module that quantizes that layer's learned *weight* (typically\nper-output-channel, symmetric `qint8`)."
        }
      ],
      "warns": [],
      "see_also": [
        {
          "name": "QConfigMapping",
          "description": "Decides which `QConfig` applies to which module."
        },
        {
          "name": "get_default_qconfig",
          "description": "The standard static-PTQ recipe."
        },
        {
          "name": "get_default_qat_qconfig",
          "description": "The standard QAT recipe."
        },
        {
          "name": "lucid.quantization.FakeQuantize",
          "description": "The QAT observer + straight-through wrapper."
        }
      ]
    },
    {
      "name": "QConfigMapping",
      "path": "lucid.quantization.qconfig.QConfigMapping",
      "kind": "class",
      "class_kind": "regular",
      "bases": [],
      "labels": [],
      "signature": "QConfigMapping()",
      "source": "https://github.com/ChanLumerico/lucid/blob/4b22d32cc/lucid/quantization/qconfig.py#L261",
      "subcategory": "qconfig",
      "methods": [
        {
          "name": "__init__",
          "path": "lucid.quantization.qconfig.QConfigMapping.__init__",
          "kind": "function",
          "labels": [],
          "signature": "__init__()",
          "source": "https://github.com/ChanLumerico/lucid/blob/4b22d32cc/lucid/quantization/qconfig.py#L327",
          "subcategory": "qconfig",
          "summary": null,
          "extended": null,
          "parameters": [],
          "returns": {
            "annotation": "None",
            "description": ""
          },
          "raises": [],
          "examples": [],
          "notes": [],
          "attributes": [],
          "warns": [],
          "see_also": []
        },
        {
          "name": "set_global",
          "path": "lucid.quantization.qconfig.QConfigMapping.set_global",
          "kind": "function",
          "labels": [],
          "signature": "set_global(qconfig: QConfig | None)",
          "source": "https://github.com/ChanLumerico/lucid/blob/4b22d32cc/lucid/quantization/qconfig.py#L332",
          "subcategory": "qconfig",
          "summary": "Set the fallback `QConfig` used when no more specific rule matches.",
          "extended": "The global tier is the lowest-priority tier (below per-type and per-name), so\nit defines the model-wide default behaviour. Pass `None` to make \"leave in\nfloat\" the default and then *opt in* specific types / names on top.",
          "parameters": [
            {
              "name": "qconfig",
              "annotation": "QConfig or None",
              "description": "The recipe applied to any module without a matching per-type or per-name\nentry, or `None` to leave such modules unquantized.",
              "default": null
            }
          ],
          "returns": {
            "annotation": "QConfigMapping",
            "description": "`self`, so setters can be chained fluently."
          },
          "raises": [],
          "examples": [],
          "notes": [],
          "attributes": [],
          "warns": [],
          "see_also": []
        },
        {
          "name": "set_object_type",
          "path": "lucid.quantization.qconfig.QConfigMapping.set_object_type",
          "kind": "function",
          "labels": [],
          "signature": "set_object_type(module_type: type, qconfig: QConfig | None)",
          "source": "https://github.com/ChanLumerico/lucid/blob/4b22d32cc/lucid/quantization/qconfig.py#L353",
          "subcategory": "qconfig",
          "summary": "Bind a `QConfig` to every module of an exact type.",
          "extended": "A per-type rule sits above the global fallback but below a per-name rule.\nMatching is by exact class identity, not `isinstance` — register a subclass\nexplicitly if you need it. Pass `None` to exclude every module of this type\nfrom quantization.",
          "parameters": [
            {
              "name": "module_type",
              "annotation": "type",
              "description": "The module class to match (e.g. `lucid.nn.Linear`).",
              "default": null
            },
            {
              "name": "qconfig",
              "annotation": "QConfig or None",
              "description": "The recipe for modules of `module_type`, or `None` to skip them.",
              "default": null
            }
          ],
          "returns": {
            "annotation": "QConfigMapping",
            "description": "`self`, so setters can be chained fluently."
          },
          "raises": [],
          "examples": [],
          "notes": [],
          "attributes": [],
          "warns": [],
          "see_also": []
        },
        {
          "name": "set_module_name",
          "path": "lucid.quantization.qconfig.QConfigMapping.set_module_name",
          "kind": "function",
          "labels": [],
          "signature": "set_module_name(name: str, qconfig: QConfig | None)",
          "source": "https://github.com/ChanLumerico/lucid/blob/4b22d32cc/lucid/quantization/qconfig.py#L378",
          "subcategory": "qconfig",
          "summary": "Bind a `QConfig` to the single module at a qualified name.",
          "extended": "A per-name rule is the highest-priority tier: it overrides both the per-type\nrule and the global fallback for exactly the module whose dotted path in the\nparent model equals `name` (e.g. `\"encoder.layer.0.attention\"`). Pass\n`None` to exclude just that one module.",
          "parameters": [
            {
              "name": "name",
              "annotation": "str",
              "description": "The fully-qualified module name (dotted path from the root model).",
              "default": null
            },
            {
              "name": "qconfig",
              "annotation": "QConfig or None",
              "description": "The recipe for that module, or `None` to leave it unquantized.",
              "default": null
            }
          ],
          "returns": {
            "annotation": "QConfigMapping",
            "description": "`self`, so setters can be chained fluently."
          },
          "raises": [],
          "examples": [],
          "notes": [],
          "attributes": [],
          "warns": [],
          "see_also": []
        },
        {
          "name": "get_qconfig",
          "path": "lucid.quantization.qconfig.QConfigMapping.get_qconfig",
          "kind": "function",
          "labels": [],
          "signature": "get_qconfig(module_type: type, module_name: str)",
          "source": "https://github.com/ChanLumerico/lucid/blob/4b22d32cc/lucid/quantization/qconfig.py#L401",
          "subcategory": "qconfig",
          "summary": "Resolve the `QConfig` for a module, most-specific tier first.",
          "extended": "Implements the resolution rule: a per-name entry (tier 1) wins over a per-type\nentry (tier 2), which wins over the global fallback (tier 3). Returns `None`\nwhen the winning tier maps to `None` *or* when no tier matches and no global\ndefault was set — in both cases the caller leaves the module in float.",
          "parameters": [
            {
              "name": "module_type",
              "annotation": "type",
              "description": "The exact class of the module being resolved.",
              "default": null
            },
            {
              "name": "module_name",
              "annotation": "str",
              "description": "The fully-qualified (dotted) name of the module in the root model.",
              "default": null
            }
          ],
          "returns": {
            "annotation": "QConfig or None",
            "description": "The resolved recipe, or `None` to signal \"do not quantize\"."
          },
          "raises": [],
          "examples": [],
          "notes": [],
          "attributes": [],
          "warns": [],
          "see_also": []
        }
      ],
      "summary": "Resolves which `QConfig` applies to each module in a model.",
      "extended": "A `QConfig` says *how* to quantize; a `QConfigMapping` says *where*.\n`lucid.quantization.prepare` / `lucid.quantization.prepare_qat`\nconsult a mapping for every candidate module and insert (or skip) observers\naccording to the resolved recipe. The mapping is a small three-tier lookup table\nplus a resolution rule.\n\n**Three tiers, most-specific-wins.** For a module of type `T` at qualified name\n`\"a.b.c\"` the mapping is queried in order:\n\n1. **module name** — an exact `set_module_name` entry;\n2. **module type** — a `set_object_type` entry;\n3. **global** — the `set_global` fallback.\n\nThe first tier that has an entry wins, so a per-name rule overrides a per-type\nrule, which overrides the global default. This lets you set one recipe for the\nwhole model and then carve out exceptions with surgical precision.\n\n**Mapping to `None` means \"do not quantize\".** Any tier may map to `None` to\n*exclude* a module from quantization even when a broader tier would have quantized\nit — e.g. keep a numerically sensitive final classifier in float while quantizing\neverything else.\n\n**Fluent construction.** Every setter returns `self`, so a mapping is typically\nbuilt in a single chained expression.",
      "parameters": [],
      "returns": null,
      "raises": [],
      "examples": [
        ">>> import lucid.nn as nn\n>>> import lucid.quantization as Q\n>>> mapping = (\n...     Q.QConfigMapping()\n...     .set_global(Q.get_default_qconfig())               # default everywhere\n...     .set_object_type(nn.Linear, None)                  # ... except Linears\n...     .set_module_name(\"head\", Q.get_default_qconfig())  # ... but quantize \"head\"\n... )\n>>> # \"head\" is a Linear, but the per-name rule (tier 1) beats the per-type rule:\n>>> mapping.get_qconfig(nn.Linear, \"head\") is not None\nTrue\n>>> # any other Linear falls to the per-type rule → excluded from quantization:\n>>> mapping.get_qconfig(nn.Linear, \"encoder.fc\") is None\nTrue\n>>> # a Conv2d has no name / type rule → the global default applies:\n>>> mapping.get_qconfig(nn.Conv2d, \"features.0\") is not None\nTrue"
      ],
      "notes": [
        "- The name / type tiers are plain dicts, so re-setting the same key overwrites the\n  previous entry; there is no merge.\n- Type matching is by *exact* type identity (the dict is keyed on the class\n  object), not `isinstance` — a subclass is not matched by a base-class rule\n  unless you register the subclass explicitly.\n- The default mappings from `get_default_qconfig_mapping` /\n  `get_default_qat_qconfig_mapping` set only the global tier; you add\n  per-type / per-name overrides on top."
      ],
      "attributes": [],
      "warns": [],
      "see_also": [
        {
          "name": "QConfig",
          "description": "The recipe a mapping resolves to."
        },
        {
          "name": "get_default_qconfig_mapping",
          "description": "A mapping preloaded with the static-PTQ default."
        },
        {
          "name": "get_default_qat_qconfig_mapping",
          "description": "A mapping preloaded with the QAT default."
        }
      ]
    },
    {
      "name": "get_default_qat_qconfig",
      "path": "lucid.quantization.qconfig.get_default_qat_qconfig",
      "kind": "function",
      "labels": [],
      "signature": "get_default_qat_qconfig()",
      "source": "https://github.com/ChanLumerico/lucid/blob/4b22d32cc/lucid/quantization/qconfig.py#L193",
      "subcategory": "qconfig",
      "summary": "Return the standard **quantization-aware-training** recipe.",
      "extended": "The QAT analogue of `get_default_qconfig`: it uses the *same* grid geometry\nfor activations and weights, but wraps each observer in a\n`lucid.quantization.FakeQuantize` module. During training that wrapper both\nrefreshes `(scale, zero_point)` from live statistics *and* applies\nstraight-through fake-quantization, so the training graph experiences the exact\nrounding / clamping of int8 inference while gradients keep flowing. Fine-tuning\nunder that simulated error lets the weights adapt to it, recovering accuracy that\nplain PTQ leaves on the table for aggressive (low-bit or quantization-sensitive)\nmodels.\n\n* **Activations** — a per-tensor-affine `quint8` fake-quant driven by a\n  `MovingAverageMinMaxObserver`. The moving average smooths the per-batch\n  range so the simulated grid does not jitter from step to step during training.\n* **Weights** — a per-channel-symmetric `qint8` fake-quant on `ch_axis=0`,\n  matching the eventual inference-time weight quantization exactly.",
      "parameters": [],
      "returns": {
        "annotation": "QConfig",
        "description": "A recipe whose two factories build `lucid.quantization.FakeQuantize`\nmodules (rather than bare observers) for activations and weights."
      },
      "raises": [],
      "examples": [
        ">>> import lucid.quantization as Q\n>>> qat = Q.get_default_qat_qconfig()\n>>> type(qat.activation()).__name__\n'FakeQuantize'\n>>> type(qat.weight()).__name__\n'FakeQuantize'"
      ],
      "notes": [
        "- Install it with `lucid.quantization.prepare_qat`, not\n  `lucid.quantization.prepare`; after training,\n  `lucid.quantization.convert` folds the fake-quant statistics into a\n  genuinely quantized model.\n- The standard QAT schedule toggles the two `FakeQuantize` switches over time —\n  observe + fake-quant early, then freeze the observer (and BN stats) as training\n  converges — via `lucid.quantization.FakeQuantize.disable_observer`, etc.\n- Activations use a *moving-average* min/max here rather than a histogram: the range\n  is re-estimated every step, so a cheap running statistic is both sufficient and\n  more stable than a per-step histogram."
      ],
      "attributes": [],
      "warns": [],
      "see_also": [
        {
          "name": "get_default_qconfig",
          "description": "The static-PTQ counterpart (bare observers, no fake-quant)."
        },
        {
          "name": "get_default_qat_qconfig_mapping",
          "description": "Installs this recipe as a model-wide mapping."
        },
        {
          "name": "lucid.quantization.FakeQuantize",
          "description": "The wrapper both factories build."
        }
      ]
    },
    {
      "name": "get_default_qat_qconfig_mapping",
      "path": "lucid.quantization.qconfig.get_default_qat_qconfig_mapping",
      "kind": "function",
      "labels": [],
      "signature": "get_default_qat_qconfig_mapping()",
      "source": "https://github.com/ChanLumerico/lucid/blob/4b22d32cc/lucid/quantization/qconfig.py#L471",
      "subcategory": "qconfig",
      "summary": "Build the default **QAT** `QConfigMapping`.",
      "extended": "Convenience constructor returning a fresh mapping whose *global* tier is\n`get_default_qat_qconfig` and whose per-type / per-name tiers are empty. Every\neligible module therefore trains with `lucid.quantization.FakeQuantize` on\nits activations and weights unless you add an override. This is the mapping\n`lucid.quantization.prepare_qat` uses when called without an explicit one.",
      "parameters": [],
      "returns": {
        "annotation": "QConfigMapping",
        "description": "A mapping whose global `QConfig` is the default QAT config and whose\ntype / name tiers are empty (ready to receive overrides)."
      },
      "raises": [],
      "examples": [
        ">>> import lucid.nn as nn\n>>> import lucid.quantization as Q\n>>> mapping = Q.get_default_qat_qconfig_mapping()\n>>> type(mapping.get_qconfig(nn.Linear, \"fc\").activation()).__name__\n'FakeQuantize'"
      ],
      "notes": [
        "- Add exceptions with `QConfigMapping.set_object_type` /\n  `QConfigMapping.set_module_name`; mapping a module to `None` trains it\n  in float (no fake-quant inserted).\n- For static post-training quantization use `get_default_qconfig_mapping`\n  instead — it installs bare observers rather than fake-quant modules."
      ],
      "attributes": [],
      "warns": [],
      "see_also": [
        {
          "name": "get_default_qat_qconfig",
          "description": "The recipe installed as the global tier."
        },
        {
          "name": "get_default_qconfig_mapping",
          "description": "The static-PTQ counterpart."
        },
        {
          "name": "QConfigMapping",
          "description": "The three-tier resolution object this returns."
        }
      ]
    },
    {
      "name": "get_default_qconfig",
      "path": "lucid.quantization.qconfig.get_default_qconfig",
      "kind": "function",
      "labels": [],
      "signature": "get_default_qconfig()",
      "source": "https://github.com/ChanLumerico/lucid/blob/4b22d32cc/lucid/quantization/qconfig.py#L125",
      "subcategory": "qconfig",
      "summary": "Return the standard **static post-training-quantization** recipe.",
      "extended": "The recipe to reach for first when quantizing a trained float model for int8\ninference without any fine-tuning. It pairs the two schemes the literature and the\nreference framework have settled on as the accuracy-preserving default for\nconvolution / linear stacks:\n\n* **Activations** — a per-tensor-affine `quint8` `HistogramObserver`.\n  Activations are fed through a histogram so the calibrated `(scale, zero_point)`\n  minimizes quantization error under the *observed distribution* rather than merely\n  clamping to min/max; this matters because activations are typically heavy-tailed\n  (a few large outliers would otherwise stretch the grid and starve the bulk of the\n  mass of resolution). The affine (asymmetric) grid with a free `zero_point` fits\n  post-ReLU ranges that start at 0 without wasting half the codes on negatives.\n* **Weights** — a per-channel-symmetric `qint8` `PerChannelMinMaxObserver`\n  on `ch_axis=0`. Each output channel gets its own scale, so channels with very\n  different magnitudes are each tracked tightly; the symmetric grid pins\n  `zero_point` to 0, which the low-precision GEMM kernels assume for the weight\n  operand.",
      "parameters": [],
      "returns": {
        "annotation": "QConfig",
        "description": "A recipe whose `activation` factory builds a histogram observer and whose\n`weight` factory builds a per-channel min/max observer."
      },
      "raises": [],
      "examples": [
        ">>> import lucid.nn as nn\n>>> import lucid.quantization as Q\n>>> qcfg = Q.get_default_qconfig()\n>>> type(qcfg.activation()).__name__\n'HistogramObserver'\n>>> type(qcfg.weight()).__name__\n'PerChannelMinMaxObserver'\n>>> # Apply it globally, then keep every Linear in float:\n>>> mapping = (Q.QConfigMapping()\n...     .set_global(qcfg)\n...     .set_object_type(nn.Linear, None))"
      ],
      "notes": [
        "- This is the *global* recipe installed by `get_default_qconfig_mapping`;\n  override it per-type or per-name through a `QConfigMapping` when a specific\n  layer needs different treatment (or should stay in float).\n- Both fields are `with_args` factories, so `lucid.quantization.prepare`\n  builds an independent observer at every site (see `QConfig`).\n- The activation histogram is costlier to calibrate than a plain min/max observer\n  but is the single biggest lever on PTQ accuracy for real activations; swap in a\n  `MinMaxObserver` only when calibration speed dominates."
      ],
      "attributes": [],
      "warns": [],
      "see_also": [
        {
          "name": "get_default_qat_qconfig",
          "description": "The QAT counterpart (same schemes, FakeQuantize-wrapped)."
        },
        {
          "name": "get_default_qconfig_mapping",
          "description": "Installs this recipe as a model-wide mapping."
        },
        {
          "name": "QConfig",
          "description": "The recipe object this returns."
        }
      ]
    },
    {
      "name": "get_default_qconfig_mapping",
      "path": "lucid.quantization.qconfig.get_default_qconfig_mapping",
      "kind": "function",
      "labels": [],
      "signature": "get_default_qconfig_mapping()",
      "source": "https://github.com/ChanLumerico/lucid/blob/4b22d32cc/lucid/quantization/qconfig.py#L428",
      "subcategory": "qconfig",
      "summary": "Build the default **static-PTQ** `QConfigMapping`.",
      "extended": "Convenience constructor returning a fresh mapping whose *global* tier is\n`get_default_qconfig` and whose per-type / per-name tiers are empty. Every\neligible module is therefore quantized with the standard histogram-activation /\nper-channel-weight recipe unless you add an override. This is the mapping\n`lucid.quantization.prepare` uses when called without an explicit mapping.",
      "parameters": [],
      "returns": {
        "annotation": "QConfigMapping",
        "description": "A mapping whose global `QConfig` is the default static-PTQ config and\nwhose type / name tiers are empty (ready to receive overrides)."
      },
      "raises": [],
      "examples": [
        ">>> import lucid.nn as nn\n>>> import lucid.quantization as Q\n>>> mapping = Q.get_default_qconfig_mapping()\n>>> mapping.get_qconfig(nn.Conv2d, \"features.0\") is not None\nTrue\n>>> # Keep the classifier in float while quantizing the rest:\n>>> _ = mapping.set_module_name(\"classifier\", None)\n>>> mapping.get_qconfig(nn.Linear, \"classifier\") is None\nTrue"
      ],
      "notes": [
        "- Add exceptions with `QConfigMapping.set_object_type` /\n  `QConfigMapping.set_module_name` (e.g. map a sensitive head to `None`).\n- For quantization-aware training use `get_default_qat_qconfig_mapping`\n  instead — it installs `lucid.quantization.FakeQuantize` recipes."
      ],
      "attributes": [],
      "warns": [],
      "see_also": [
        {
          "name": "get_default_qconfig",
          "description": "The recipe installed as the global tier."
        },
        {
          "name": "get_default_qat_qconfig_mapping",
          "description": "The QAT counterpart."
        },
        {
          "name": "QConfigMapping",
          "description": "The three-tier resolution object this returns."
        }
      ]
    },
    {
      "name": "convert",
      "path": "lucid.quantization._quantize.convert",
      "kind": "function",
      "labels": [],
      "signature": "convert(model: nn.Module, inplace: bool = False)",
      "source": "https://github.com/ChanLumerico/lucid/blob/4b22d32cc/lucid/quantization/_quantize.py#L314",
      "subcategory": "quantize",
      "summary": "Bake a calibrated model into quantized inference modules.",
      "extended": "`convert` is the final step of the static-PTQ pipeline (`prepare` →\n*calibrate* → `convert`). It walks the calibrated model and swaps every\nobserved float submodule for its quantized counterpart: the learned float\nweight is quantized once into int8 codes plus per-channel `scale` /\n`zero_point` (dropping the float weight), and each activation observer's\nrecorded range is frozen into that layer's output qparams. What comes back\nis a self-contained, `eval`-mode model with no observers left — ready to\nrun or serialize, with a checkpoint roughly `4x` smaller than the float\noriginal.\n\nA submodule is convertible when calibration attached an\n`activation_post_process` to it; a small always-convert set\n(`DeQuantStub`, `Embedding`, `EmbeddingBag`) converts even without an\nactivation observer. Replacements are spliced in through `_modules` rather\nthan `setattr` so a `Sequential`'s child order is preserved.\n\nWhen `lucid.backends.quantized.use_mlx()` is true (the MLX quantized engine\nis present), the Linear family is routed to the real weight-only\n`quantized_matmul` GEMM — the genuine compute + memory win, at W8A16\nnumerics (weights int8/int4, activations kept float) — instead of the\ndequantize-to-float reference path. On a fully-Metal model this is bandwidth-\nbound and measures ~`3.15x` faster at the memory-bound decode shape\n`M = 1` with a weight payload ~`3.55x` smaller; layers whose\n`in_features` is not divisible by a supported MLX group size fall back to\nthe dequant path automatically. Set `backends.quantized.engine =\n\"reference\"` to force the exact W8A8 dequant numerics on every layer.",
      "parameters": [
        {
          "name": "model",
          "annotation": "nn.Module",
          "description": "A model previously returned by `prepare` and then run through\ncalibration, so every quantizable submodule carries frozen activation\nqparams. QAT models (from `prepare_qat`, fine-tuned) also convert\nhere — their `nn.qat` layers map to the same quantized inference layers.",
          "default": null
        },
        {
          "name": "inplace",
          "annotation": "bool",
          "description": "If `True` mutate and return `model` itself; if `False` (default)\nconvert a `copy.deepcopy` and leave the calibrated model intact.",
          "default": "False"
        }
      ],
      "returns": {
        "annotation": "nn.Module",
        "description": "The converted, `eval`-mode model with int8 weights baked in and its\nfloat submodules replaced by their quantized counterparts."
      },
      "raises": [],
      "examples": [
        ">>> import lucid, lucid.nn as nn\n>>> import lucid.quantization as Q\n>>> model = nn.Sequential(nn.Linear(128, 64))\n>>> model.qconfig = Q.get_default_qconfig()\n>>> prepared = Q.prepare(model)\n>>> for _ in range(8):                       # calibrate before converting\n...     _ = prepared(lucid.randn(32, 128))\n>>> qmodel = Q.convert(prepared)\n>>> type(qmodel[0]).__name__\n'Linear'\nThe common mistake — converting straight after prepare with **no**\ncalibration — silently produces a model with meaningless activation qparams:\n>>> prepared = Q.prepare(nn.Sequential(nn.Linear(128, 64)))\n>>> qmodel = Q.convert(prepared)   # no data ran through -> garbage output scale"
      ],
      "notes": [
        "- Non-destructive by default (deep-copy); pass `inplace=True` to convert\n  the calibrated model in place and reclaim its float weights.\n- Only int8 codes + qparams + float bias enter the resulting `state_dict`;\n  the float weight never does (a whole checkpoint shrinks ~`3.97x` on\n  `resnet_18`).\n- The reference (non-MLX) path wins on **memory**, not compute — its GEMM\n  runs in float and so is roughly float-speed on the CPU stream. Convert with\n  the MLX backend active for the genuine low-precision compute speed-up.\n- Calling `convert` on a model that was never calibrated is a silent\n  correctness bug: the output qparams are whatever the fresh observers hold\n  (often identity), so run calibration data through `prepare`'s output\n  first."
      ],
      "attributes": [],
      "warns": [],
      "see_also": [
        {
          "name": "lucid.quantization.prepare",
          "description": "Insert observers ahead of calibration."
        },
        {
          "name": "lucid.quantization.convert_fx",
          "description": "The graph-mode counterpart."
        },
        {
          "name": "lucid.quantization.quantize_dynamic",
          "description": "Calibration-free alternative."
        },
        {
          "name": "lucid.nn.quantized.Linear",
          "description": "The quantized layer this installs for Linear."
        }
      ]
    },
    {
      "name": "prepare",
      "path": "lucid.quantization._quantize.prepare",
      "kind": "function",
      "labels": [],
      "signature": "prepare(model: nn.Module, qconfig: QConfig | QConfigMapping | None = None, inplace: bool = False)",
      "source": "https://github.com/ChanLumerico/lucid/blob/4b22d32cc/lucid/quantization/_quantize.py#L204",
      "subcategory": "quantize",
      "summary": "Attach activation observers to a float model ahead of calibration.",
      "extended": "`prepare` is step one of the eager-mode static post-training quantization\n(PTQ) workflow — the classic `prepare` → *calibrate* → `convert`\npipeline. It does not touch weights or change any numerics; it walks the\nmodel and, for every quantizable module the `QConfigMapping` selects,\nattaches an **activation observer** as a forward hook. As calibration data\nflows through the returned model, each observer records the running range\n(min / max, or a histogram) of that module's output, and `convert`\nlater reads those ranges to freeze the per-layer activation `scale` /\n`zero_point`.\n\nOnly module types in the observed set — Linear, the Conv / ConvTranspose\nfamily, the intrinsic fused Conv/Linear+ReLU modules, a handful of\nactivations, and the `QuantStub` / `FloatFunctional` boundaries — receive\nan observer, and only where the mapping returns a non-`None` `QConfig`\nfor them. The model is switched to `eval` mode so BatchNorm / Dropout stay\ndeterministic during calibration. Use this when you want the best accuracy\nand can afford a calibration pass over representative data; for a\ncalibration-free path see `quantize_dynamic`, and to make quantization\nerror visible to the optimizer see `prepare_qat`.",
      "parameters": [
        {
          "name": "model",
          "annotation": "nn.Module",
          "description": "The float model to observe. Explicit `QuantStub` / `DeQuantStub`\nboundaries are picked up automatically; if you would rather not place\nthem by hand, use `prepare_fx`, which wraps them for you.",
          "default": null
        },
        {
          "name": "qconfig",
          "annotation": "QConfig or QConfigMapping",
          "description": "The quantization recipe — a single `QConfig` is promoted to a\nglobal mapping, and `None` (default) falls back to\n`get_default_qconfig_mapping` (per-channel int8 weights,\nper-tensor affine activations).",
          "default": "None"
        },
        {
          "name": "inplace",
          "annotation": "bool",
          "description": "If `True` mutate and return `model` itself; if `False` (default)\nobserve a `copy.deepcopy` and leave the original untouched.",
          "default": "False"
        }
      ],
      "returns": {
        "annotation": "nn.Module",
        "description": "The prepared, `eval`-mode model carrying activation observers. Run\nrepresentative data through it to calibrate, then pass it to\n`convert`."
      },
      "raises": [],
      "examples": [
        ">>> import lucid, lucid.nn as nn\n>>> import lucid.quantization as Q\n>>> model = nn.Sequential(nn.Linear(128, 64), nn.ReLU(), nn.Linear(64, 10))\n>>> model.qconfig = Q.get_default_qconfig()\n>>> prepared = Q.prepare(model)            # observers inserted (deep copy)\n>>> for _ in range(8):                      # calibrate on representative data\n...     _ = prepared(lucid.randn(32, 128))\n>>> qmodel = Q.convert(prepared)            # freeze qparams, bake int8 weights\nTargeting a type the model does not contain warns, and the downstream\nconvert becomes a no-op:\n>>> mapping = Q.QConfigMapping().set_object_type(nn.Conv2d, Q.get_default_qconfig())\n>>> _ = Q.prepare(nn.Sequential(nn.Linear(8, 8)), mapping)\nUserWarning: prepare(): the QConfigMapping matched no quantizable modules ..."
      ],
      "notes": [
        "- Non-destructive: with `inplace=False` the source model is deep-copied,\n  so the returned graph is a separate object.\n- Nothing is quantized yet — the returned model still runs in full float and\n  is marginally *slower* than the original because of the observer hooks.\n  The size / speed win only lands after `convert`.\n- If the mapping matches no quantizable module a `UserWarning` is raised\n  and a later `convert` leaves the model unchanged — usually a sign\n  the `QConfigMapping` targets types the model does not contain.\n- Fuse Conv/BN/ReLU runs with `fuse_modules` *before* `prepare` so\n  the observer sees the true post-fusion (e.g. post-ReLU) activation range."
      ],
      "attributes": [],
      "warns": [],
      "see_also": [
        {
          "name": "lucid.quantization.convert",
          "description": "Bake the calibrated model into quantized modules."
        },
        {
          "name": "lucid.quantization.quantize_dynamic",
          "description": "Calibration-free dynamic quantization."
        },
        {
          "name": "lucid.quantization.prepare_qat",
          "description": "Insert fake-quant for quantization-aware training."
        },
        {
          "name": "lucid.quantization.fuse_modules",
          "description": "Fuse Conv/BN/ReLU runs before `prepare`."
        }
      ]
    },
    {
      "name": "prepare_qat",
      "path": "lucid.quantization._quantize.prepare_qat",
      "kind": "function",
      "labels": [],
      "signature": "prepare_qat(model: nn.Module, qconfig: QConfig | QConfigMapping | None = None, inplace: bool = False)",
      "source": "https://github.com/ChanLumerico/lucid/blob/4b22d32cc/lucid/quantization/_quantize.py#L575",
      "subcategory": "quantize",
      "summary": "Insert fake-quant modules for quantization-aware training.",
      "extended": "`prepare_qat` is the entry point of the QAT workflow — `prepare_qat` →\n*fine-tune* → `convert`. Where static PTQ only *observes* a frozen\nfloat model, QAT makes the quantization grid part of the forward pass: this\nswaps every weighted layer for its `lucid.nn.qat` counterpart (which\nfake-quantizes both its weight and its output) and attaches a fake-quant to\nevery `QuantStub`. During the subsequent fine-tuning the network *feels*\nthe int8 grid on each step — via the straight-through estimator of\n`fake_quantize` — and learns weights that minimize the rounding error,\ntypically recovering most of the accuracy a low-bit model would otherwise\nlose. Reach for it when plain static PTQ drops too much accuracy (aggressive\nbit widths, quantization-sensitive architectures).\n\nUnlike `prepare`, the returned model is left in **`train` mode**: BN\nkeeps updating its running stats and the fake-quant grids track the evolving\nranges. Fuse Conv/BN with `fuse_modules_qat` first so the folded\n`ConvBn*` modules stay trainable and fold BN per forward under the STE.\nAfter fine-tuning, `convert` maps the `nn.qat` layers to the same\nint8 inference layers a static-PTQ `convert` produces.",
      "parameters": [
        {
          "name": "model",
          "annotation": "nn.Module",
          "description": "Float model with `QuantStub` / `DeQuantStub` boundaries (and,\nideally, Conv/BN runs already fused for QAT).",
          "default": null
        },
        {
          "name": "qconfig",
          "annotation": "QConfig or QConfigMapping",
          "description": "The QAT recipe — a single `QConfig` is promoted to a global\nmapping, and `None` (default) falls back to\n`get_default_qat_qconfig_mapping` (fused weight + activation\nfake-quant).",
          "default": "None"
        },
        {
          "name": "inplace",
          "annotation": "bool",
          "description": "If `True` mutate and return `model` itself; if `False` (default)\nprepare a `copy.deepcopy` and leave the original untouched.",
          "default": "False"
        }
      ],
      "returns": {
        "annotation": "nn.Module",
        "description": "The QAT-ready model in `train` mode. Fine-tune it, then call\n`convert` to obtain the int8 inference model."
      },
      "raises": [],
      "examples": [
        ">>> import lucid, lucid.nn as nn\n>>> import lucid.quantization as Q\n>>> model = nn.Sequential(nn.Linear(128, 64), nn.ReLU(), nn.Linear(64, 10))\n>>> model.qconfig = Q.get_default_qat_qconfig()\n>>> qat = Q.prepare_qat(model)              # fake-quant inserted, train mode\n>>> qat.training\nTrue\n>>> # ... fine-tune qat for a few epochs with real labels ...\n>>> qmodel = Q.convert(qat.eval())          # bake into the int8 inference model"
      ],
      "notes": [
        "- Returned in `train` mode (contrast `prepare`, which returns\n  `eval`) — fake-quant and BN both need training-time behavior.\n- Weighted layers become trainable fake-quant modules; their float weights\n  are kept and updated by the optimizer (nothing is baked to int8 until\n  `convert`).\n- Fuse with `fuse_modules_qat` (not `fuse_modules`) beforehand:\n  the eval-time BN fold would freeze BN, which QAT needs to keep learning.\n- Non-destructive by default (deep-copy)."
      ],
      "attributes": [],
      "warns": [],
      "see_also": [
        {
          "name": "lucid.quantization.prepare",
          "description": "Static-PTQ observers (no fake-quant, eval mode)."
        },
        {
          "name": "lucid.quantization.convert",
          "description": "Bake the fine-tuned QAT model into int8."
        },
        {
          "name": "lucid.quantization.fuse_modules_qat",
          "description": "Trainable Conv/BN fusion for QAT."
        },
        {
          "name": "lucid.quantization.fake_quantize",
          "description": "The STE primitive QAT trains through."
        }
      ]
    },
    {
      "name": "quantize_dynamic",
      "path": "lucid.quantization._quantize.quantize_dynamic",
      "kind": "function",
      "labels": [],
      "signature": "quantize_dynamic(model: nn.Module, qconfig_spec: set[type] | None = None, dtype: QDtype = qint8, inplace: bool = False)",
      "source": "https://github.com/ChanLumerico/lucid/blob/4b22d32cc/lucid/quantization/_quantize.py#L429",
      "subcategory": "quantize",
      "summary": "Dynamically quantize a model — int8 weights, runtime-quantized activations.",
      "extended": "`quantize_dynamic` is the whole dynamic-PTQ workflow in one call: unlike\nthe static `prepare` → *calibrate* → `convert` flow it needs **no\ncalibration data and no observers**. Every module whose type is in\n`qconfig_spec` (default `{Linear, LSTM}`) is swapped in place for its\ndynamic counterpart, which stores its weight as int8 codes but measures the\nactivation range and quantizes activations *per forward*, at runtime. That\nmakes it the go-to for models where activation statistics vary widely between\ninputs and a fixed calibrated range would be a poor fit — most notably the\nLinear-heavy stacks of Transformers and RNN language models, where the weight\nmemory dominates and per-call activation quantization costs little.\n\nBecause activation qparams are recomputed each call, dynamic quantization\ncaptures the `4x` weight-memory win without an offline calibration pass, at\nthe cost of a small per-forward quantize overhead. Prefer static PTQ\n(`prepare` / `convert`) when you can calibrate and want the\nlowest inference latency; prefer this when calibration is impractical or the\nstack is Linear-only.\n\nWhen `lucid.backends.quantized.use_mlx()` is true, the Linear family is\nrouted to the real weight-only `quantized_matmul` MLX GEMM (the genuine\nspeed + memory win, W8A16 numerics), falling back to the dequantize path for\nlayers whose `in_features` is not group-size-divisible.",
      "parameters": [
        {
          "name": "model",
          "annotation": "nn.Module",
          "description": "The float model to quantize. A bare top-level target (e.g. calling this\ndirectly on an `nn.Linear`) is handled too — it has no parent to swap\nit into, so the quantized module is returned directly.",
          "default": null
        },
        {
          "name": "qconfig_spec",
          "annotation": "set of module types",
          "description": "Which module types to quantize; `None` (default) uses the mapping's\nkeys — `{nn.Linear, nn.LSTM}`.",
          "default": "None"
        },
        {
          "name": "dtype",
          "annotation": "QDtype",
          "description": "The quantized dtype for the weights (its `bits` also selects the MLX\nfast-path bit width when that backend is active).",
          "default": "qint8"
        },
        {
          "name": "inplace",
          "annotation": "bool",
          "description": "If `True` mutate and return `model` itself; if `False` (default)\nquantize a `copy.deepcopy` and leave the original untouched.",
          "default": "False"
        }
      ],
      "returns": {
        "annotation": "nn.Module",
        "description": "The dynamically-quantized, `eval`-mode model (or the single quantized\nmodule when a bare target was passed)."
      },
      "raises": [],
      "examples": [
        ">>> import lucid, lucid.nn as nn\n>>> import lucid.quantization as Q\n>>> mlp = nn.Sequential(nn.Linear(512, 512), nn.ReLU(), nn.Linear(512, 10))\n>>> qmlp = Q.quantize_dynamic(mlp)          # no calibration needed\n>>> type(qmlp[0]).__name__\n'Linear'\n>>> qmlp(lucid.randn(1, 512)).shape\n(1, 10)\nA model with no targeted layers warns and comes back unchanged — e.g. a\nconv-only stack, since Conv2d is not a dynamic-quant target:\n>>> conv = nn.Sequential(nn.Conv2d(3, 8, 3))\n>>> Q.quantize_dynamic(conv) is not None\nUserWarning: quantize_dynamic(): no module of a targeted type (Linear, LSTM) found ..."
      ],
      "notes": [
        "- No calibration and no observers are inserted — the model is ready to run\n  immediately after this call.\n- Only the targeted types are swapped; everything else (activations,\n  norms, attention softmax) stays float, so this is a *partial* quantization\n  aimed at the weight-heavy layers.\n- If no module of a targeted type is found, a `UserWarning` is raised and\n  the model is returned unchanged.\n- The model is switched to `eval` mode before swapping."
      ],
      "attributes": [],
      "warns": [],
      "see_also": [
        {
          "name": "lucid.quantization.prepare",
          "description": "Static-PTQ observer insertion (needs calibration)."
        },
        {
          "name": "lucid.quantization.convert",
          "description": "Static-PTQ bake step."
        },
        {
          "name": "lucid.quantization.prepare_qat",
          "description": "Quantization-aware training entry point."
        },
        {
          "name": "lucid.nn.quantized.dynamic.Linear",
          "description": "The dynamic quantized Linear installed here."
        }
      ]
    },
    {
      "name": "convert_fx",
      "path": "lucid.quantization._quantize_fx.convert_fx",
      "kind": "function",
      "labels": [],
      "signature": "convert_fx(model: nn.Module, inplace: bool = False)",
      "source": "https://github.com/ChanLumerico/lucid/blob/4b22d32cc/lucid/quantization/_quantize_fx.py#L112",
      "subcategory": "quantize_fx",
      "summary": "Graph-mode convert — bake a calibrated model into quantized modules.",
      "extended": "`convert_fx` is the graph-mode counterpart of\n`lucid.quantization.convert` and the final step of the graph-mode\nstatic-PTQ pipeline (`prepare_fx` → *calibrate* → `convert_fx`). It swaps\neach observed float module for its quantized inference equivalent, quantizing\nweights to int8 codes and folding the calibrated activation qparams into each\nlayer. Because `prepare_fx` already wrapped the quant boundaries, the\nresult is a plain, self-contained quantized model that both runs directly and\ntraces cleanly through `lucid.compile` — where the quantized graph fuses\ninto a single executable.\n\nThis is a thin pass-through over `lucid.quantization.convert`, so it\nshares all of its behavior: MLX-backend routing of the Linear family to the\nreal weight-only `quantized_matmul` GEMM (~`3.15x` decode speed-up at\n`M = 1`, ~`3.55x` smaller weights) when that backend is active, the\nreference dequant path otherwise, and the same deep-copy-by-default contract.",
      "parameters": [
        {
          "name": "model",
          "annotation": "nn.Module",
          "description": "A model returned by `prepare_fx` and then run through calibration,\nso its observed submodules carry frozen activation qparams.",
          "default": null
        },
        {
          "name": "inplace",
          "annotation": "bool",
          "description": "If `True` mutate and return `model` itself; if `False` (default)\nconvert a deep copy and leave the calibrated model intact.",
          "default": "False"
        }
      ],
      "returns": {
        "annotation": "nn.Module",
        "description": "The converted, `eval`-mode quantized model — ready to run or hand to\n`lucid.compile`."
      },
      "raises": [],
      "examples": [
        ">>> import lucid, lucid.nn as nn\n>>> import lucid.quantization as Q\n>>> model = nn.Sequential(nn.Linear(128, 64), nn.ReLU(), nn.Linear(64, 10))\n>>> model.qconfig = Q.get_default_qconfig()\n>>> prepared = Q.prepare_fx(model)\n>>> for _ in range(8):\n...     _ = prepared(lucid.randn(32, 128))\n>>> qmodel = Q.convert_fx(prepared)\n>>> compiled = lucid.compile(qmodel)         # single fused quantized graph"
      ],
      "notes": [
        "- Delegates to `lucid.quantization.convert`; the only reason to call\n  this variant is symmetry with `prepare_fx` (the wrapping already\n  happened at prepare time, so nothing extra is needed here).\n- Convert only *after* calibration — an uncalibrated model bakes in\n  meaningless activation qparams.\n- The MLX compute win lands in the inference / generation regime and fades\n  toward parity in compute-bound training GEMMs; force exact reference\n  numerics with `backends.quantized.engine = \"reference\"`."
      ],
      "attributes": [],
      "warns": [],
      "see_also": [
        {
          "name": "lucid.quantization.prepare_fx",
          "description": "The graph-mode observer-insertion step."
        },
        {
          "name": "lucid.quantization.convert",
          "description": "The eager-mode bake step this delegates to."
        },
        {
          "name": "lucid.compile",
          "description": "Fuses the converted quantized graph into one executable."
        }
      ]
    },
    {
      "name": "prepare_fx",
      "path": "lucid.quantization._quantize_fx.prepare_fx",
      "kind": "function",
      "labels": [],
      "signature": "prepare_fx(model: nn.Module, qconfig_mapping: QConfig | QConfigMapping | None = None, example_inputs: Tensor | None = None, inplace: bool = False)",
      "source": "https://github.com/ChanLumerico/lucid/blob/4b22d32cc/lucid/quantization/_quantize_fx.py#L30",
      "subcategory": "quantize_fx",
      "summary": "Graph-mode prepare — auto-wrap the quant boundaries and insert observers.",
      "extended": "`prepare_fx` is the graph-mode entry point of static PTQ, the counterpart\nto eager `lucid.quantization.prepare`. It removes the one piece of\nmanual bookkeeping the eager flow demands: instead of hand-placing a\n`QuantStub` at the model input and a `DeQuantStub` at the output, it wraps\nthe model in a `lucid.nn.quantized.QuantWrapper` so the input is\nquantized and the output dequantized automatically, then runs the ordinary\neager `lucid.quantization.prepare` underneath to attach activation\nobservers. From there the workflow is identical — run calibration data\nthrough the returned model, then `convert_fx`.\n\nThe graph-mode payoff is downstream: because every quantized replacement is\nan ordinary composite, the converted model traces cleanly through\n`lucid.compile`, letting the dispatch-bound quantized graph fuse into a\nsingle executable — the regime where quantization's inference win is\nstructurally permanent. (True FX-style observer insertion into a *traced*\ngraph would need C++ TraceGraph mutation; quantizing at the module level and\nletting `compile` capture it reaches the same result.)",
      "parameters": [
        {
          "name": "model",
          "annotation": "nn.Module",
          "description": "The float model — **no** manual `QuantStub` / `DeQuantStub` needed;\nthe wrapper adds the boundaries.",
          "default": null
        },
        {
          "name": "qconfig_mapping",
          "annotation": "QConfig or QConfigMapping",
          "description": "The quantization recipe, forwarded to the underlying\n`lucid.quantization.prepare`; `None` (default) uses the\nstatic-PTQ default mapping.",
          "default": "None"
        },
        {
          "name": "example_inputs",
          "annotation": "Tensor",
          "description": "Reserved for a future trace-time insertion path; ignored on the current\ncomposite path (accepted for API compatibility).",
          "default": "None"
        },
        {
          "name": "inplace",
          "annotation": "bool",
          "description": "If `True` mutate and return `model` itself; if `False` (default)\nprepare a deep copy.",
          "default": "False"
        }
      ],
      "returns": {
        "annotation": "nn.Module",
        "description": "The prepared, `eval`-mode model (input/output boundaries wrapped,\nobservers attached). Calibrate it, then call `convert_fx`."
      },
      "raises": [],
      "examples": [
        ">>> import lucid, lucid.nn as nn\n>>> import lucid.quantization as Q\n>>> model = nn.Sequential(nn.Linear(128, 64), nn.ReLU(), nn.Linear(64, 10))\n>>> model.qconfig = Q.get_default_qconfig()\n>>> prepared = Q.prepare_fx(model)          # no QuantStub placed by hand\n>>> for _ in range(8):                       # calibrate\n...     _ = prepared(lucid.randn(32, 128))\n>>> qmodel = Q.convert_fx(prepared)\n>>> compiled = lucid.compile(qmodel)         # quantized graph fuses cleanly"
      ],
      "notes": [
        "- The only behavioral difference from eager `lucid.quantization.prepare`\n  is the automatic `QuantWrapper` — everything else (observer types,\n  calibration semantics, deep-copy default) is shared.\n- `example_inputs` is a placeholder today; passing it changes nothing.\n- Nothing is quantized yet — calibrate before `convert_fx`, exactly as\n  in the eager flow."
      ],
      "attributes": [],
      "warns": [],
      "see_also": [
        {
          "name": "lucid.quantization.convert_fx",
          "description": "The graph-mode bake step."
        },
        {
          "name": "lucid.quantization.prepare",
          "description": "The eager-mode counterpart (manual boundaries)."
        },
        {
          "name": "lucid.nn.quantized.QuantWrapper",
          "description": "The input/output boundary wrapper used here."
        }
      ]
    },
    {
      "name": "fuse_modules",
      "path": "lucid.quantization.fuse_modules.fuse_modules",
      "kind": "function",
      "labels": [],
      "signature": "fuse_modules(model: nn.Module, modules_to_fuse: Sequence[Sequence[str]], inplace: bool = False)",
      "source": "https://github.com/ChanLumerico/lucid/blob/4b22d32cc/lucid/quantization/fuse_modules.py#L92",
      "subcategory": "fuse_modules",
      "summary": "Collapse Conv/BN/ReLU (or Linear/ReLU) runs into single fused modules.",
      "extended": "`fuse_modules` is a pre-processing step you run **before**\n`lucid.quantization.prepare` in the static-PTQ workflow. It rewrites\nthe model so each named run of adjacent layers becomes one module: the first\nmember is replaced by the fused module and the rest by\n`lucid.nn.Identity` (so the model's original `forward` still routes\nthrough them harmlessly). BatchNorm is folded into the preceding convolution's\nweight via the eval-time fusion, and a trailing ReLU is absorbed into an\n`nn.intrinsic` fused module.\n\nFusion matters for quantization on two counts. First, folding BN into the\nconv weight removes a separately-quantized layer and its rounding error, and\nit means there is only one weight tensor to quantize per block. Second,\nabsorbing the ReLU makes the downstream activation observer see the *post-*\nReLU range, so the calibrated grid is not wasted on the negative half that\nthe ReLU would clip anyway — a direct accuracy win. Put the model in `eval`\nmode first: the BN fold uses the frozen running statistics, which is only\ncorrect in eval.",
      "parameters": [
        {
          "name": "model",
          "annotation": "nn.Module",
          "description": "The model to fuse. Put it in `eval` mode first — BN folding is\neval-time (it consumes the frozen running mean / variance).",
          "default": null
        },
        {
          "name": "modules_to_fuse",
          "annotation": "sequence of name groups",
          "description": "Each group is a list of dotted module names to fuse together, e.g.\n`[[\"conv1\", \"bn1\"], [\"layer1.0.conv1\", \"layer1.0.bn1\", \"layer1.0.relu\"]]`.\nSupported runs: `[Conv, BN]`, `[Conv, BN, ReLU]`, `[Conv, ReLU]`,\nand `[Linear, ReLU]`; any other pattern raises `ValueError`.",
          "default": null
        },
        {
          "name": "inplace",
          "annotation": "bool",
          "description": "If `True` fuse and return `model` itself; if `False` (default) fuse\na `copy.deepcopy` and leave the original untouched.",
          "default": "False"
        }
      ],
      "returns": {
        "annotation": "nn.Module",
        "description": "The fused model, with each run's first module replaced by the fused\nmodule and the remaining members turned into `Identity`."
      },
      "raises": [],
      "examples": [
        ">>> import lucid.nn as nn\n>>> import lucid.quantization as Q\n>>> class Block(nn.Module):\n...     def __init__(self):\n...         super().__init__()\n...         self.conv = nn.Conv2d(3, 8, 3)\n...         self.bn = nn.BatchNorm2d(8)\n...         self.relu = nn.ReLU()\n...     def forward(self, x):\n...         return self.relu(self.bn(self.conv(x)))\n>>> m = Block().eval()\n>>> fused = Q.fuse_modules(m, [[\"conv\", \"bn\", \"relu\"]])\n>>> type(fused.conv).__name__               # Conv+BN+ReLU -> one fused module\n'ConvReLU2d'\n>>> type(fused.bn).__name__, type(fused.relu).__name__\n('Identity', 'Identity')\nAn unsupported run (here [BN, ReLU] with no leading Conv/Linear) raises:\n>>> Q.fuse_modules(m.eval(), [[\"bn\", \"relu\"]])\nValueError: fuse_modules: unsupported fusion pattern ['BatchNorm2d', 'ReLU']"
      ],
      "notes": [
        "- Order-preserving: replacements are spliced in through `_modules` rather\n  than `setattr`, so a `Sequential`'s execution order is unchanged\n  (`setattr` would move a re-added key to the end).\n- The trailing members become `Identity` rather than being deleted, so any\n  hand-written `forward` that still calls them keeps working.\n- This is the **eval / static-PTQ** fusion. For quantization-aware training\n  use `fuse_modules_qat`, which keeps Conv+BN trainable and folds BN\n  per forward instead of freezing it.\n- Non-destructive by default (deep-copy)."
      ],
      "attributes": [],
      "warns": [],
      "see_also": [
        {
          "name": "lucid.quantization.fuse_modules_qat",
          "description": "Trainable Conv/BN fusion for QAT."
        },
        {
          "name": "lucid.quantization.prepare",
          "description": "Run this before inserting observers."
        },
        {
          "name": "lucid.nn.utils.fusion.fuse_conv_bn_eval",
          "description": "The eval-time BN-into-conv fold."
        }
      ]
    },
    {
      "name": "fuse_modules_qat",
      "path": "lucid.quantization.fuse_modules.fuse_modules_qat",
      "kind": "function",
      "labels": [],
      "signature": "fuse_modules_qat(model: nn.Module, modules_to_fuse: Sequence[Sequence[str]], qconfig: QConfig, inplace: bool = False)",
      "source": "https://github.com/ChanLumerico/lucid/blob/4b22d32cc/lucid/quantization/fuse_modules.py#L219",
      "subcategory": "fuse_modules",
      "summary": "Fuse for **QAT** — Conv+BN runs become trainable `nn.intrinsic.qat` modules.",
      "extended": "`fuse_modules_qat` is the fusion step of the quantization-aware-training\nworkflow, run **before** `lucid.quantization.prepare_qat`. It mirrors\n`fuse_modules` but keeps BatchNorm *learnable*. Where the eval-time\n`fuse_modules` folds BN into the conv weight once and freezes it — fine\nfor a frozen PTQ model — that would stop BN from training, which QAT needs.\nSo here a `[Conv, BN]` / `[Conv, BN, ReLU]` run becomes a trainable\n`ConvBn*` / `ConvBnReLU*` module that folds BN **per forward**: the BN\nparameters keep updating and the fold is recomputed each step, all under the\nstraight-through estimator of the fake-quant grid. `[Conv, ReLU]` and\n`[Linear, ReLU]` runs (no BN) become the plain `nn.intrinsic` fused\nmodule, which `lucid.quantization.prepare_qat` then swaps to its QAT\nform.\n\nBecause the trainable `ConvBn*` modules fake-quantize at construction time,\nthe `qconfig` must be supplied *here* rather than later — it is baked into\nthe fused module so its weight fake-quant grid is active from the first\nforward. As with `fuse_modules`, the first member of each run is\nreplaced by the fused module and the rest become `Identity`.",
      "parameters": [
        {
          "name": "model",
          "annotation": "nn.Module",
          "description": "The model to fuse. Kept **trainable** (unlike `fuse_modules`, do\nnot put it in eval — BN must keep learning).",
          "default": null
        },
        {
          "name": "modules_to_fuse",
          "annotation": "sequence of name groups",
          "description": "Dotted-name groups exactly as in `fuse_modules` — e.g.\n`[[\"conv1\", \"bn1\"], [\"layer1.0.conv1\", \"layer1.0.bn1\"]]`. Same supported\nruns: `[Conv, BN]`, `[Conv, BN, ReLU]`, `[Conv, ReLU]`,\n`[Linear, ReLU]`.",
          "default": null
        },
        {
          "name": "qconfig",
          "annotation": "QConfig",
          "description": "The QAT recipe handed to the fused `ConvBn*` modules; they fake-quant\nat construction, so it is needed at fusion time rather than at\n`lucid.quantization.prepare_qat` time.",
          "default": null
        },
        {
          "name": "inplace",
          "annotation": "bool",
          "description": "If `True` fuse and return `model` itself; if `False` (default) fuse\na `copy.deepcopy` and leave the original untouched.",
          "default": "False"
        }
      ],
      "returns": {
        "annotation": "nn.Module",
        "description": "The fused, still-trainable model — hand it to\n`lucid.quantization.prepare_qat`, fine-tune, then\n`lucid.quantization.convert`."
      },
      "raises": [],
      "examples": [
        ">>> import lucid.nn as nn\n>>> import lucid.quantization as Q\n>>> class Block(nn.Module):\n...     def __init__(self):\n...         super().__init__()\n...         self.conv = nn.Conv2d(3, 8, 3)\n...         self.bn = nn.BatchNorm2d(8)\n...     def forward(self, x):\n...         return self.bn(self.conv(x))\n>>> m = Block()                              # kept in train mode\n>>> qcfg = Q.get_default_qat_qconfig()\n>>> fused = Q.fuse_modules_qat(m, [[\"conv\", \"bn\"]], qcfg)\n>>> type(fused.conv).__name__               # trainable Conv+BN, folds per forward\n'ConvBn2d'\n>>> qat = Q.prepare_qat(fused)               # continue the QAT workflow"
      ],
      "notes": [
        "- Conv+BN stays a single trainable module that folds BN **per forward**; BN\n  is never frozen (contrast `fuse_modules`, which folds once at eval).\n- The `qconfig` argument is mandatory precisely because the fused\n  `ConvBn*` fake-quantizes at construction — there is no later hook to pass\n  it through.\n- Order-preserving splice via `_modules`; trailing members become\n  `Identity` so the original `forward` still routes through them.\n- Non-destructive by default (deep-copy)."
      ],
      "attributes": [],
      "warns": [],
      "see_also": [
        {
          "name": "lucid.quantization.fuse_modules",
          "description": "Eval-time (static-PTQ) fusion; freezes BN."
        },
        {
          "name": "lucid.quantization.prepare_qat",
          "description": "Run this after fusing, before fine-tuning."
        },
        {
          "name": "lucid.quantization.convert",
          "description": "Bake the fine-tuned QAT model into int8."
        }
      ]
    }
  ],
  "summary": "`lucid.quantization` — post-training quantization, QAT, and quantized inference.",
  "extended": "This sub-package brings weights and activations from `float32` down to\nlow-precision integer grids (int8 / int4), the top capability lever for\ninference and serving on Apple Silicon.  It mirrors the reference\nframework's quantization surface while keeping a single canonical path\nper name (H8): the *workflow* API lives here (observers, `QConfig`,\n`prepare` / `convert` / `quantize_dynamic` / `prepare_qat`), and\nthe quantized *module* layer lives under `lucid.nn.quantized` /\n`lucid.nn.qat` / `lucid.nn.intrinsic`.\n\nRepresentation follows the *sidecar* design (B): a quantized weight is an\nordinary integer `lucid.Tensor` plus float `scale` /\n`zero_point` buffers held by the owning module — no dedicated quantized\ntensor subtype and no new engine dtype.\n\n**Phase 0 surface (this commit):** the schemes / dtypes / parameter\ncontainer and the three arithmetic primitives (`quantize`,\n`dequantize`, `fake_quantize`).  Observers, `QConfig`, the\neager / dynamic / graph workflows, and the real low-precision GEMM land\nin later phases.",
  "parameters": [],
  "returns": null,
  "raises": [],
  "examples": [],
  "notes": [],
  "attributes": [],
  "warns": [],
  "see_also": []
}