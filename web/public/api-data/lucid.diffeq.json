{
  "slug": "lucid.diffeq",
  "name": "diffeq",
  "path": "lucid.diffeq",
  "kind": "module",
  "source": "https://github.com/ChanLumerico/lucid/blob/9a501f2b1/lucid/diffeq/__init__.py#L1",
  "members": [
    {
      "name": "odeint_adjoint",
      "path": "lucid.diffeq._adjoint.odeint_adjoint",
      "kind": "function",
      "labels": [],
      "signature": "odeint_adjoint(func: Callable[..., object], y0: State, t: Tensor | Sequence[float], rtol: float = 1e-07, atol: float = 1e-09, method: str | ButcherTableau | None = None, options: dict[str, object] | None = None, event_fn: Callable[..., Tensor] | None = None, adjoint_rtol: float | None = None, adjoint_atol: float | None = None, adjoint_method: str | ButcherTableau | None = None, adjoint_options: dict[str, object] | None = None, adjoint_params: Sequence[Tensor] | None = None)",
      "source": "https://github.com/ChanLumerico/lucid/blob/9a501f2b1/lucid/diffeq/_adjoint.py#L246",
      "subcategory": "adjoint",
      "summary": "Integrate `dy/dt = f(t, y)` and differentiate it at constant memory.",
      "extended": "Same result as `odeint`, different gradient strategy.  `odeint`\ndifferentiates the discretisation — exact for what it computed, but every\nstage of every step stays alive until backward.  This instead discards the\nforward graph and reconstructs the gradient by integrating the adjoint\nsystem backwards, so memory no longer grows with the number of steps.\n\nUse it when the solve is long or the tolerance tight enough that the\nretained stages dominate memory.  For a short solve, `odeint` is\nboth faster and more accurate.",
      "parameters": [
        {
          "name": "func",
          "annotation": "callable",
          "description": "Right-hand side `f(t, y) -> dy/dt`.",
          "default": null
        },
        {
          "name": "y0",
          "annotation": "Tensor",
          "description": "Initial state at `t[0]`.",
          "default": null
        },
        {
          "name": "t",
          "annotation": "Tensor or sequence of float",
          "description": "Strictly monotonic 1-D grid of at least two finite time points.",
          "default": null
        },
        {
          "name": "rtol",
          "annotation": "float",
          "description": "Tolerances for the forward solve.",
          "default": "1e-07"
        },
        {
          "name": "atol",
          "annotation": "float",
          "description": "Tolerances for the forward solve.",
          "default": "1e-07"
        },
        {
          "name": "method",
          "annotation": "str or ButcherTableau or None",
          "description": "Forward method; `None` selects `\"dopri5\"`.",
          "default": "None"
        },
        {
          "name": "options",
          "annotation": "dict or None",
          "description": "Forward solver options.",
          "default": "None"
        },
        {
          "name": "event_fn",
          "annotation": "callable or None",
          "description": "Not implemented yet; passing one raises.",
          "default": "None"
        },
        {
          "name": "adjoint_rtol",
          "annotation": "float or None",
          "description": "Tolerances for the backward solve.  `None` inherits the forward\nvalues.",
          "default": "None"
        },
        {
          "name": "adjoint_atol",
          "annotation": "float or None",
          "description": "Tolerances for the backward solve.  `None` inherits the forward\nvalues.",
          "default": "None"
        },
        {
          "name": "adjoint_method",
          "annotation": "str or ButcherTableau or None",
          "description": "Method for the backward solve.  `None` inherits `method`.",
          "default": "None"
        },
        {
          "name": "adjoint_options",
          "annotation": "dict or None",
          "description": "Options for the backward solve.  `None` inherits `options`.",
          "default": "None"
        },
        {
          "name": "adjoint_params",
          "annotation": "sequence of Tensor or None",
          "description": "Tensors to accumulate gradients for.  `None` uses\n`func.parameters()` when `func` exposes it, otherwise nothing.",
          "default": "None"
        }
      ],
      "returns": {
        "annotation": "Tensor",
        "description": "Shape `(len(t), *y0.shape)`; index 0 is `y0` itself."
      },
      "raises": [
        {
          "annotation": "NotImplementedError",
          "description": "If `event_fn` is given."
        },
        {
          "annotation": "TypeError",
          "description": "If any entry of `adjoint_params` is not a tensor."
        },
        {
          "annotation": "ValueError",
          "description": "Anything `odeint` rejects, on either solve."
        }
      ],
      "examples": [
        ">>> import lucid, lucid.diffeq as diffeq\n>>> k = lucid.tensor([0.5], dtype=lucid.float64, requires_grad=True)\n>>> y0 = lucid.tensor([1.0], dtype=lucid.float64)\n>>> ys = diffeq.odeint_adjoint(\n...     lambda t, y: -k * y, y0, [0.0, 1.0], adjoint_params=[k]\n... )\n>>> ys[-1].sum().backward()\n>>> bool(abs(float(k.grad.item()) + 0.60653066) < 1e-5)\nTrue"
      ],
      "notes": [
        "The gradient is an approximation.  It solves the continuous adjoint\nequation numerically, so it converges to the true derivative as the\ntolerances tighten rather than matching `odeint`'s gradient\nexactly — tightening `adjoint_rtol` / `adjoint_atol` is what closes\nthe gap.\n\nCost is a second integration whose right-hand side performs one\nvector-Jacobian product through `func`, so expect roughly a doubling\nof solve time on top of the backward passes.\n\nGradients with respect to `t` are not produced.  `t` is read to the\nhost once as an integration grid throughout `lucid.diffeq`, so it is\nnot differentiable in `odeint` either."
      ],
      "attributes": [],
      "warns": [],
      "see_also": [
        {
          "name": "lucid.diffeq.odeint",
          "description": "Direct differentiation; exact but memory-hungry."
        }
      ]
    },
    {
      "name": "odeint",
      "path": "lucid.diffeq._solvers.odeint",
      "kind": "function",
      "labels": [],
      "signature": "odeint(func: Callable[..., object], y0: State, t: Tensor | Sequence[float], rtol: float = 1e-07, atol: float = 1e-09, method: str | ButcherTableau | None = None, options: dict[str, object] | None = None, event_fn: Callable[..., Tensor] | None = None, return_trajectory: bool = True)",
      "source": "https://github.com/ChanLumerico/lucid/blob/9a501f2b1/lucid/diffeq/_solvers.py#L313",
      "subcategory": "solvers",
      "summary": "Integrate `dy/dt = f(t, y)`.",
      "extended": "Four families are reachable through the same call, and which one runs\nfollows from `method`:\n\n**Adaptive** (the default, and anything carrying an embedded error\nestimate) treats `t` as *output* times.  The solver picks its own step\nsizes to hold the local error inside `rtol` / `atol` and interpolates\nto each requested time, so a coarse `t` costs nothing in accuracy.\n\n**Fixed step** (`euler` / `midpoint` / `heun2` / `heun3` / `rk4`)\ntreats `t` as the integration grid itself by default: consecutive\nentries are one step each, with no sub-stepping and no interpolation.\n`rtol` / `atol` are unused there, and accuracy is controlled by\nchoosing a finer grid — or by handing `options[\"step_size\"]`, which\ndecouples the two the same way an adaptive method does.\n\n**Adams multistep** (`explicit_adams` / `implicit_adams` /\n`fixed_adams`) is fixed-step too, but reaches high order by reusing the\nderivatives from previous steps instead of taking more of them inside one\nstep — one new evaluation per step regardless of order.  That pays off\nwhen `func` is expensive.  See `options[\"max_order\"]` for the caveat\nthat comes with it.\n\nGradient mode is left entirely to the caller.  Under grad the whole solve\nis differentiable end-to-end (discretise-then-optimise) and every stage is\nretained for backward; wrap the call in `lucid.no_grad` for sampling,\nwhere the stage graph is pure overhead.",
      "parameters": [
        {
          "name": "func",
          "annotation": "callable",
          "description": "Right-hand side `f(t, y) -> dy/dt`.  Receives the stage time as a\n0-D tensor matching `y0` in dtype and device, and must return a\ntensor with the same shape and device as `y0`.",
          "default": null
        },
        {
          "name": "y0",
          "annotation": "Tensor",
          "description": "Initial state at `t[0]`.  Any shape; must have a floating dtype.",
          "default": null
        },
        {
          "name": "t",
          "annotation": "Tensor or sequence of float",
          "description": "Strictly monotonic 1-D grid of at least two finite time points.\nDescending grids integrate backwards in time.",
          "default": null
        },
        {
          "name": "rtol",
          "annotation": "float",
          "description": "Relative tolerance.  Adaptive methods only.",
          "default": "1e-7"
        },
        {
          "name": "atol",
          "annotation": "float",
          "description": "Absolute tolerance.  Adaptive methods only.",
          "default": "1e-9"
        },
        {
          "name": "method",
          "annotation": "str or ButcherTableau or None",
          "description": "`None` selects `\"dopri5\"`.  Otherwise one of `\"dopri5\"`,\n`\"tsit5\"`, `\"bosh3\"`, `\"fehlberg2\"`, `\"adaptive_heun\"`,\n`\"euler\"`, `\"midpoint\"`, `\"heun2\"`, `\"heun3\"`, `\"rk4\"`,\n`\"explicit_adams\"`, `\"implicit_adams\"`, `\"fixed_adams\"`,\n`\"implicit_euler\"`, `\"implicit_midpoint\"`, `\"trapezoid\"`,\n`\"radauIIA3\"`, `\"radauIIA5\"`, `\"gl4\"`, `\"gl6\"`, `\"sdirk2\"`,\n`\"trbdf2\"`, or a custom `ButcherTableau`.",
          "default": "None"
        },
        {
          "name": "options",
          "annotation": "dict or None",
          "description": "Per-method settings.  Adaptive methods accept `min_step`,\n`max_step`, `first_step`, `step_t`, `jump_t`, `safety`,\n`ifactor`, `dfactor`, `max_num_steps` (plus `dtype` and\n`norm`, accepted and ignored).  Fixed-step methods accept\n`step_size`, `grid_constructor`, `interp` and `perturb` —\ngiving either of the first two decouples the integration grid from\n`t`, which is then reached by interpolation.  Adams methods accept\nall four of those plus `max_order` (default 12) and `max_iters`\n(default 4, corrector sweeps, ignored by `explicit_adams`).\n\nA high `max_order` is not simply more accurate.  Explicit Adams\nloses stability as the order climbs — at the default 12 its stable\nstep is small enough that `explicit_adams` diverges on problems\n`rk4` handles comfortably — so lower it, or use one of the\ncorrected variants, whose stability holds up far better.  Order also\nramps from a Runge-Kutta start, which caps the accuracy the first\nsteps can contribute regardless of `max_order`.\n\nImplicit methods accept the fixed-step keys plus `max_iters`\n(default 100), the ceiling on iterations of the nonlinear solve.  A\nstep whose solve runs out of iterations warns rather than passing off\nan unconverged iterate as a completed step.",
          "default": "None"
        },
        {
          "name": "event_fn",
          "annotation": "callable or None",
          "description": "`g(t, y)` returning a single-element tensor.  When given, the solve\nignores every entry of `t` but the first, runs until `g` changes\nsign, and returns a `(event_t, solution)` pair instead of a\ntrajectory.  The direction of `t` still decides which way it\nsearches.",
          "default": "None"
        },
        {
          "name": "return_trajectory",
          "annotation": "bool",
          "description": "Return the state at every time in `t`.  Set `False` to keep only\nthe final state — for a sampling run of many steps over a batch of\nimages, stacking the full trajectory multiplies peak memory by the\nnumber of steps.  Ignored when `event_fn` is given.  **Lucid\nextension**, not part of the reference interface.",
          "default": "True"
        }
      ],
      "returns": {
        "annotation": "Tensor or tuple",
        "description": "Without `event_fn`: shape `(len(t), *y0.shape)` when\n`return_trajectory` is `True` (index 0 is `y0` itself),\notherwise `y0.shape`.  The dtype is the promotion of `y0` with\neverything `func` returns, matching what `y + dt * k` would\nproduce.\n\nWith `event_fn`: a `(event_t, solution)` pair, where `event_t`\nis a 0-D tensor and `solution` has shape `(2, *y0.shape)` — the\nstate at `t[0]` and the state at the event."
      },
      "raises": [
        {
          "annotation": "ValueError",
          "description": "If `t` is not a strictly monotonic 1-D grid of at least two finite\npoints, if `y0` has a non-floating dtype, if `method` names no\nregistered method, if `options` holds a key the method does not\naccept, or if `func` returns a tensor whose shape or device differs\nfrom `y0`."
        },
        {
          "annotation": "TypeError",
          "description": "If `method` is neither a string nor a `ButcherTableau`, or\nif `func` returns something other than a tensor."
        },
        {
          "annotation": "RuntimeError",
          "description": "If an adaptive solve exceeds `max_num_steps` or its step size\ncollapses."
        }
      ],
      "examples": [
        "Exponential decay against its closed form:\n>>> import lucid, lucid.diffeq as diffeq\n>>> y0 = lucid.tensor([1.0], dtype=lucid.float64)\n>>> y = diffeq.odeint(lambda s, y: -y, y0, [0.0, 1.0], return_trajectory=False)\n>>> abs(float(y.item()) - 0.36787944117) < 1e-8\nTrue"
      ],
      "notes": [
        "A fixed-step solve costs exactly `(len(t) - 1) * method.stages` calls to\n`func`.  An adaptive solve costs as many as the tolerances demand, and\nreads one scalar back to the host per step to decide whether to accept it\n— that host synchronisation is intrinsic to adaptivity, which is why the\ntwo families have different performance characters.\n\nHigher-order differentiation works: the fused step opts into\ngraph-recording backward, so `create_graph=True` through a solve behaves\nexactly as it would for the unfused arithmetic."
      ],
      "attributes": [],
      "warns": [],
      "see_also": [
        {
          "name": "lucid.diffeq.ButcherTableau",
          "description": "Coefficient table selected by `method`."
        }
      ]
    },
    {
      "name": "odeint_dense",
      "path": "lucid.diffeq._solvers.odeint_dense",
      "kind": "function",
      "labels": [],
      "signature": "odeint_dense(func: Callable[..., object], y0: State, t0: float, t1: float, rtol: float = 1e-07, atol: float = 1e-09, method: str | ButcherTableau | None = None, options: dict[str, object] | None = None)",
      "source": "https://github.com/ChanLumerico/lucid/blob/9a501f2b1/lucid/diffeq/_solvers.py#L556",
      "subcategory": "solvers",
      "summary": "Solve once across an interval and return a continuous solution.",
      "extended": "Where `odeint` wants the output times up front, this integrates\n`[t0, t1]` a single time and hands back a function of `t`.  Every\nstep's interpolating polynomial is kept, so any later query costs a\nbinary search and one polynomial evaluation — no re-integration, and no\ncommitment to a grid before you know which times you need.",
      "parameters": [
        {
          "name": "func",
          "annotation": "callable",
          "description": "Right-hand side `f(t, y) -> dy/dt`.  Receives the stage time as a\n0-D tensor matching `y0` in dtype and device, and must return a\ntensor with the same shape and device as `y0`.",
          "default": null
        },
        {
          "name": "y0",
          "annotation": "Tensor",
          "description": "Initial state at `t0`.  Any shape; must have a floating dtype.",
          "default": null
        },
        {
          "name": "t0",
          "annotation": "float",
          "description": "Ends of the interval.  `t1 < t0` integrates backwards.",
          "default": null
        },
        {
          "name": "t1",
          "annotation": "float",
          "description": "Ends of the interval.  `t1 < t0` integrates backwards.",
          "default": null
        },
        {
          "name": "rtol",
          "annotation": "float",
          "description": "Relative tolerance.  Adaptive methods only.",
          "default": "1e-7"
        },
        {
          "name": "atol",
          "annotation": "float",
          "description": "Absolute tolerance.  Adaptive methods only.",
          "default": "1e-9"
        },
        {
          "name": "method",
          "annotation": "str or ButcherTableau or None",
          "description": "`None` selects `\"dopri5\"`.  See `odeint` for the full list.",
          "default": "None"
        },
        {
          "name": "options",
          "annotation": "dict or None",
          "description": "Per-method settings, exactly as `odeint` takes them.",
          "default": "None"
        }
      ],
      "returns": {
        "annotation": "callable",
        "description": "`dense(t) -> Tensor`, accepting a float or a 0-D tensor and\nreturning the state there.  Raises `ValueError` for a time\noutside `[t0, t1]`."
      },
      "raises": [
        {
          "annotation": "ValueError",
          "description": "If `t0` and `t1` are equal or non-finite, if `y0` has a\nnon-floating dtype, if `method` names no registered method, if\n`options` holds a key the method does not accept, or if `func`\nreturns a tensor whose shape or device differs from `y0`."
        },
        {
          "annotation": "TypeError",
          "description": "If `method` is neither a string nor a `ButcherTableau`, or\nif `func` returns something other than a tensor."
        },
        {
          "annotation": "RuntimeError",
          "description": "If an adaptive solve exceeds `max_num_steps` or its step size\ncollapses."
        }
      ],
      "examples": [
        ">>> import lucid, lucid.diffeq as diffeq\n>>> y0 = lucid.tensor([1.0], dtype=lucid.float64)\n>>> dense = diffeq.odeint_dense(lambda t, y: -y, y0, 0.0, 1.0)\n>>> abs(float(dense(0.5).item()) - 0.6065306597) < 1e-7\nTrue"
      ],
      "notes": [
        "Memory grows with the number of accepted steps: each keeps a handful of\ntensors the size of the state.  A long solve over a large state is\ntherefore much heavier than `odeint` with `return_trajectory=False`.\n\nA fixed-step method interpolates with whatever `options[\"interp\"]`\nsays, and without `step_size` its grid is the single interval\n`[t0, t1]` — usually far too coarse, so pass one."
      ],
      "attributes": [],
      "warns": [],
      "see_also": [
        {
          "name": "lucid.diffeq.odeint",
          "description": "Same solvers, output times fixed up front."
        }
      ]
    },
    {
      "name": "odeint_event",
      "path": "lucid.diffeq._solvers.odeint_event",
      "kind": "function",
      "labels": [],
      "signature": "odeint_event(func: Callable[..., object], y0: State, t0: float | Tensor, event_fn: Callable[..., Tensor], reverse_time: bool = False, odeint_interface: Callable[..., object] = odeint, kwargs: object = {})",
      "source": "https://github.com/ChanLumerico/lucid/blob/9a501f2b1/lucid/diffeq/_solvers.py#L698",
      "subcategory": "solvers",
      "summary": "Integrate from `t0` until an event fires.",
      "extended": "The convenience form of `odeint(..., event_fn=...)` for the common case\nwhere there is no output grid at all — you have a starting time and a\ncondition, and want to know when the condition is met.",
      "parameters": [
        {
          "name": "func",
          "annotation": "callable",
          "description": "Right-hand side `f(t, y) -> dy/dt`.",
          "default": null
        },
        {
          "name": "y0",
          "annotation": "Tensor",
          "description": "Initial state at `t0`.",
          "default": null
        },
        {
          "name": "t0",
          "annotation": "float or Tensor",
          "description": "Start time.",
          "default": null
        },
        {
          "name": "event_fn",
          "annotation": "callable",
          "description": "`g(t, y)` returning a single-element tensor.  The solve ends the\nmoment its sign changes; a value of exactly zero at `t0` fires\nimmediately.",
          "default": null
        },
        {
          "name": "reverse_time",
          "annotation": "bool",
          "description": "Search backwards in time instead of forwards.",
          "default": "False"
        },
        {
          "name": "odeint_interface",
          "annotation": "callable",
          "description": "Solver to delegate to.  Pass `odeint_adjoint` to get the event\nsolve at constant memory.",
          "default": "odeint"
        },
        {
          "name": "**kwargs",
          "annotation": "object",
          "description": "Forwarded to `odeint_interface` — `rtol`, `atol`, `method`,\n`options`.",
          "default": "{}"
        }
      ],
      "returns": {
        "annotation": "tuple",
        "description": "`(event_t, solution)`; `event_t` is a 0-D tensor and\n`solution` has shape `(2, *y0.shape)` — the state at `t0` and\nthe state at the event."
      },
      "raises": [
        {
          "annotation": "ValueError",
          "description": "If a fixed-step method is used without `options[\"step_size\"]`, or\nif `event_fn` does not return a single-element tensor."
        },
        {
          "annotation": "RuntimeError",
          "description": "If no sign change is found within the step budget."
        }
      ],
      "examples": [
        "A body falling from rest hits the ground at \\sqrt{2h/g}:\n>>> import lucid, lucid.diffeq as diffeq\n>>> y0 = lucid.tensor([10.0, 0.0], dtype=lucid.float64)  # height, velocity\n>>> def fall(t, y):\n...     return lucid.stack([y[1], lucid.tensor(-9.8, dtype=y.dtype)], dim=0)\n>>> event_t, sol = diffeq.odeint_event(\n...     fall, y0, 0.0, event_fn=lambda t, y: y[0]\n... )\n>>> abs(float(event_t.item()) - (2 * 10.0 / 9.8) ** 0.5) < 1e-6\nTrue"
      ],
      "notes": [
        "The event time itself is located by bisecting the interpolant of the\nstep that brackets it, so it costs event-function calls but no extra\nright-hand-side evaluations.\n\n`event_t` carries no gradient.  Making it differentiable needs the\nimplicit-function rerouting the reference implementation applies, which\nis not implemented here; `solution` is differentiable as usual."
      ],
      "attributes": [],
      "warns": [],
      "see_also": [
        {
          "name": "lucid.diffeq.odeint",
          "description": "Takes `event_fn` directly alongside an output grid."
        }
      ]
    },
    {
      "name": "ButcherTableau",
      "path": "lucid.diffeq._tableau.ButcherTableau",
      "kind": "class",
      "class_kind": "dataclass",
      "bases": [],
      "labels": [],
      "signature": "ButcherTableau(a: tuple[tuple[float, ...], ...], b: tuple[float, ...], c: tuple[float, ...], order: int, name: str, b_error: tuple[float, ...] | None = None, mid: tuple[float, ...] | None = None)",
      "source": "https://github.com/ChanLumerico/lucid/blob/9a501f2b1/lucid/diffeq/_tableau.py#L51",
      "subcategory": "tableau",
      "methods": [
        {
          "name": "is_adaptive",
          "path": "lucid.diffeq._tableau.ButcherTableau.is_adaptive",
          "kind": "function",
          "labels": [
            "property"
          ],
          "signature": "is_adaptive: bool",
          "source": "https://github.com/ChanLumerico/lucid/blob/9a501f2b1/lucid/diffeq/_tableau.py#L203",
          "summary": "bool: Whether the tableau carries an embedded error estimate.",
          "extended": "Adaptive methods pair two solutions of different order; their weight\ndifference (`b_error`) estimates the local error, which is what a\nstep-size controller needs.  A tableau without it can only be stepped\non a fixed grid.",
          "parameters": [],
          "returns": {
            "annotation": "bool",
            "description": ""
          },
          "raises": [],
          "examples": [],
          "notes": [],
          "attributes": [],
          "warns": [],
          "see_also": []
        },
        {
          "name": "is_implicit",
          "path": "lucid.diffeq._tableau.ButcherTableau.is_implicit",
          "kind": "function",
          "labels": [
            "property"
          ],
          "signature": "is_implicit: bool",
          "source": "https://github.com/ChanLumerico/lucid/blob/9a501f2b1/lucid/diffeq/_tableau.py#L214",
          "summary": "bool: Whether any stage depends on itself or on a later stage.",
          "extended": "An explicit method computes its stages in order, each from the ones\nbefore it.  An implicit one does not: the stage equations are coupled,\nso a step has to solve a nonlinear system instead of evaluating a\nrecipe.  That is a different solver, not a different coefficient set,\nwhich is what this flag selects.",
          "parameters": [],
          "returns": {
            "annotation": "bool",
            "description": ""
          },
          "raises": [],
          "examples": [],
          "notes": [],
          "attributes": [],
          "warns": [],
          "see_also": []
        },
        {
          "name": "is_dirk",
          "path": "lucid.diffeq._tableau.ButcherTableau.is_dirk",
          "kind": "function",
          "labels": [
            "property"
          ],
          "signature": "is_dirk: bool",
          "source": "https://github.com/ChanLumerico/lucid/blob/9a501f2b1/lucid/diffeq/_tableau.py#L231",
          "summary": "bool: Whether the stage matrix is lower triangular, diagonal included.",
          "extended": "Diagonally implicit methods still couple each stage to itself, but not\nto any stage after it, so the stages can be solved one at a time.  For\na state of `n` elements that means `s` solves of size `n` rather\nthan one of size `s*n` — the difference between a dense Jacobian of\n`n**2` entries and one of `(s*n)**2`.",
          "parameters": [],
          "returns": {
            "annotation": "bool",
            "description": ""
          },
          "raises": [],
          "examples": [],
          "notes": [],
          "attributes": [],
          "warns": [],
          "see_also": []
        },
        {
          "name": "is_fsal",
          "path": "lucid.diffeq._tableau.ButcherTableau.is_fsal",
          "kind": "function",
          "labels": [
            "property"
          ],
          "signature": "is_fsal: bool",
          "source": "https://github.com/ChanLumerico/lucid/blob/9a501f2b1/lucid/diffeq/_tableau.py#L250",
          "summary": "bool: Whether the last stage derivative equals `f(t + dt, y_next)`.",
          "extended": "True when the final stage row reproduces the solution weights, so the\nstage already evaluated the right-hand side at the new state.  That\nderivative can then be reused as the next step's first stage, saving\none call per accepted step — a real saving when the right-hand side is\na neural network.",
          "parameters": [],
          "returns": {
            "annotation": "bool",
            "description": ""
          },
          "raises": [],
          "examples": [],
          "notes": [],
          "attributes": [],
          "warns": [],
          "see_also": []
        },
        {
          "name": "stages",
          "path": "lucid.diffeq._tableau.ButcherTableau.stages",
          "kind": "function",
          "labels": [
            "property"
          ],
          "signature": "stages: int",
          "source": "https://github.com/ChanLumerico/lucid/blob/9a501f2b1/lucid/diffeq/_tableau.py#L262",
          "summary": "int: Number of stage derivatives evaluated per step.",
          "extended": "Equals the number of right-hand-side calls the method makes for each\nstep, which is the dominant cost when the right-hand side is a neural\nnetwork.",
          "parameters": [],
          "returns": {
            "annotation": "int",
            "description": ""
          },
          "raises": [],
          "examples": [],
          "notes": [],
          "attributes": [],
          "warns": [],
          "see_also": []
        },
        {
          "name": "__init__",
          "path": "lucid.diffeq._tableau.ButcherTableau.__init__",
          "kind": "function",
          "labels": [],
          "signature": "__init__(a: tuple[tuple[float, ...], ...], b: tuple[float, ...], c: tuple[float, ...], order: int, name: str, b_error: tuple[float, ...] | None = None, mid: tuple[float, ...] | None = None)",
          "source": "https://github.com/ChanLumerico/lucid/blob/9a501f2b1/lucid/diffeq/_tableau.py#L1",
          "subcategory": "tableau",
          "summary": null,
          "extended": null,
          "parameters": [],
          "returns": {
            "annotation": "None",
            "description": ""
          },
          "raises": [],
          "examples": [],
          "notes": [],
          "attributes": [],
          "warns": [],
          "see_also": []
        }
      ],
      "summary": "Coefficient table defining one explicit Runge-Kutta method.",
      "extended": "Given a step from $t$ to $t + \\Delta t$, the method evaluates\n$s$ stage derivatives and combines them into the new state:\n\n$$\n\\begin{aligned}\nk_i    &= f\\!\\left(t + c_i \\Delta t,\\;\n           y + \\Delta t \\textstyle\\sum_{j<i} a_{ij} k_j\\right) \\\\\ny_{n+1} &= y_n + \\Delta t \\textstyle\\sum_i b_i k_i\n\\end{aligned}\n$$",
      "parameters": [
        {
          "name": "a",
          "annotation": "tuple of tuple of float",
          "description": "Stage coefficients, row by row.  Two shapes are accepted.  An\n*explicit* method is written in ragged form with the zeros above the\ndiagonal omitted — row `i` holds exactly `i` entries, so `a[0]`\nis empty.  An *implicit* method needs those entries, so it is written\nsquare: every row holds all `s`.  Sequences of any kind are accepted\nand coerced to nested tuples.",
          "default": null
        },
        {
          "name": "b",
          "annotation": "tuple of float",
          "description": "Combination weights, one per stage.  Must sum to `1`.",
          "default": null
        },
        {
          "name": "c",
          "annotation": "tuple of float",
          "description": "Stage times as fractions of the step.  Entry `i` must equal\n`sum(a[i])`, the standard consistency condition.",
          "default": null
        },
        {
          "name": "order",
          "annotation": "int",
          "description": "Order of accuracy $p$: halving the step size shrinks the global\nerror by roughly $2^p$.  Also the exponent the adaptive\ncontroller uses when proposing the next step size.",
          "default": null
        },
        {
          "name": "name",
          "annotation": "str",
          "description": "Human-readable method name, surfaced in error messages.",
          "default": null
        },
        {
          "name": "b_error",
          "annotation": "tuple of float",
          "description": "Difference between the two embedded solutions' weights,\n$b - \\hat{b}$, one per stage.  Present exactly on the adaptive\nmethods; its presence is what `is_adaptive` reports.  Must sum\nto `0`, since both solutions are consistent.",
          "default": "None"
        },
        {
          "name": "mid",
          "annotation": "tuple of float",
          "description": "Weights producing the state at the midpoint of a step, used to anchor\nthe quartic interpolant that gives dense output.  Required whenever\n`b_error` is given.",
          "default": "None"
        }
      ],
      "returns": null,
      "raises": [
        {
          "annotation": "ValueError",
          "description": "If `a` / `b` / `c` disagree on stage count, if any row of `a`\nhas the wrong length for a strictly lower-triangular matrix, if `b`\ndoes not sum to `1`, if any `c[i] != sum(a[i])`, or if `order`\nor `name` are unset."
        }
      ],
      "examples": [
        "The classical fourth-order method, written out in full:\n>>> import lucid.diffeq as diffeq\n>>> rk4 = diffeq.ButcherTableau(\n...     a=((), (0.5,), (0.0, 0.5), (0.0, 0.0, 1.0)),\n...     b=(1 / 6, 1 / 3, 1 / 3, 1 / 6),\n...     c=(0.0, 0.5, 0.5, 1.0),\n...     order=4,\n...     name=\"rk4\",\n... )\n>>> rk4.stages\n4"
      ],
      "notes": [],
      "attributes": [],
      "warns": [],
      "see_also": [
        {
          "name": "lucid.diffeq.odeint",
          "description": "Consumes a tableau, by name or by instance."
        }
      ]
    }
  ],
  "summary": "lucid.diffeq — differential-equation solvers.",
  "extended": "Numerical integration of ordinary differential equations, `dy/dt = f(t, y)`,\nwhere the right-hand side is an arbitrary Lucid callable — most often a neural\nnetwork, as in continuous normalising flows, flow matching, and rectified flow.\n\nSurface: `odeint` integrates to a set of times you name up front and\n`odeint_dense` returns a continuous solution you can query afterwards, and\n`odeint_adjoint` trades exactness for constant memory when the solve is\nlong enough that retained stages dominate, and `odeint_event` runs until a\ncondition fires rather than to a time you name;\n`ButcherTableau` is the coefficient table they dispatch on.  Nineteen\nbuilt-in tableaux are exported, and a custom tableau is accepted anywhere a\nmethod name is.\n\nThe tableau decides how `t` is read.  An **adaptive** method — the default\n`DOPRI5`, plus `TSIT5`, `BOSH3`, `FEHLBERG2` and\n`ADAPTIVE_HEUN` —\ncarries an embedded error estimate, so it picks its own step sizes to hold the\nerror inside `rtol` / `atol` and interpolates to the times in `t`.  A\n**fixed-step** method — `EULER`, `MIDPOINT`, `HEUN2`,\n`HEUN3`, `RK4` — steps once per interval of `t`, so `t` is the\nintegration grid itself.\n\nTwo further fixed-step families trade a different way.  **Adams multistep** —\nmethod names `\"explicit_adams\"`, `\"implicit_adams\"` and `\"fixed_adams\"`,\nwhich are not tableaux — reaches high order by reusing derivatives from\nearlier steps, so a step costs one evaluation whatever its order.  **Implicit**\nmethods — `IMPLICIT_EULER`, `IMPLICIT_MIDPOINT`, `TRAPEZOID`,\n`RADAU_IIA3`, `RADAU_IIA5`, `GL4`, `GL6`,\n`SDIRK2` and `TRBDF2` — solve a nonlinear system every step, which\nis expensive but is what lets them take large steps on a stiff problem, where\nan explicit method is held to tiny ones by stability alone.\n\nEverything here is reached as `lucid.diffeq.*` and nowhere else — there are\nno top-level aliases and no `Tensor` methods, so each name has exactly one\ncanonical path.\n\nStochastic differential equations are not implemented yet.",
  "parameters": [],
  "returns": null,
  "raises": [],
  "examples": [],
  "notes": [],
  "attributes": [
    {
      "name": "DOPRI5",
      "annotation": "ButcherTableau",
      "description": "Dormand-Prince 5(4) — seven stages, adaptive, fifth order.  The default\nmethod.  Method name `\"dopri5\"`."
    },
    {
      "name": "TSIT5",
      "annotation": "ButcherTableau",
      "description": "Tsitouras 5(4) — seven stages, adaptive, fifth order.  Method name\n`\"tsit5\"`."
    },
    {
      "name": "BOSH3",
      "annotation": "ButcherTableau",
      "description": "Bogacki-Shampine 3(2) — four stages, adaptive, third order.  Method name\n`\"bosh3\"`."
    },
    {
      "name": "FEHLBERG2",
      "annotation": "ButcherTableau",
      "description": "Fehlberg 2(1) — three stages, adaptive, second order.  Method name\n`\"fehlberg2\"`."
    },
    {
      "name": "ADAPTIVE_HEUN",
      "annotation": "ButcherTableau",
      "description": "Heun-Euler 2(1) — two stages, adaptive, second order.  Method name\n`\"adaptive_heun\"`."
    },
    {
      "name": "EULER",
      "annotation": "ButcherTableau",
      "description": "Forward Euler — one stage, first order.  Method name `\"euler\"`."
    },
    {
      "name": "MIDPOINT",
      "annotation": "ButcherTableau",
      "description": "Explicit midpoint — two stages, second order.  Method name `\"midpoint\"`."
    },
    {
      "name": "HEUN2",
      "annotation": "ButcherTableau",
      "description": "Heun's method (explicit trapezoid) — two stages, second order.  Method\nname `\"heun2\"`."
    },
    {
      "name": "HEUN3",
      "annotation": "ButcherTableau",
      "description": "Heun's third-order method — three stages, third order.  Method name\n`\"heun3\"`."
    },
    {
      "name": "RK4",
      "annotation": "ButcherTableau",
      "description": "The classical Runge-Kutta method — four stages, fourth order.  Method\nname `\"rk4\"`."
    },
    {
      "name": "IMPLICIT_EULER",
      "annotation": "ButcherTableau",
      "description": "Backward Euler — one stage, first order, implicit.  The most stable\nmethod here and the least accurate.  Method name `\"implicit_euler\"`."
    },
    {
      "name": "IMPLICIT_MIDPOINT",
      "annotation": "ButcherTableau",
      "description": "The implicit midpoint rule — one stage, second order, implicit.  Method\nname `\"implicit_midpoint\"`."
    },
    {
      "name": "TRAPEZOID",
      "annotation": "ButcherTableau",
      "description": "The trapezoidal rule — two stages, second order, implicit.  Method name\n`\"trapezoid\"`."
    },
    {
      "name": "RADAU_IIA3",
      "annotation": "ButcherTableau",
      "description": "Radau IIA — two stages, third order, implicit.  Method name\n`\"radauIIA3\"`."
    },
    {
      "name": "RADAU_IIA5",
      "annotation": "ButcherTableau",
      "description": "Radau IIA — three stages, fifth order, implicit.  Method name\n`\"radauIIA5\"`."
    },
    {
      "name": "GL4",
      "annotation": "ButcherTableau",
      "description": "Gauss-Legendre — two stages, fourth order, implicit.  Method name\n`\"gl4\"`."
    },
    {
      "name": "GL6",
      "annotation": "ButcherTableau",
      "description": "Gauss-Legendre — three stages, sixth order, implicit.  The highest order\nper stage any Runge-Kutta method reaches.  Method name `\"gl6\"`."
    },
    {
      "name": "SDIRK2",
      "annotation": "ButcherTableau",
      "description": "Singly diagonally implicit — two stages, second order.  Method name\n`\"sdirk2\"`."
    },
    {
      "name": "TRBDF2",
      "annotation": "ButcherTableau",
      "description": "TR-BDF2 — three stages, second order, implicit.  Method name\n`\"trbdf2\"`."
    }
  ],
  "warns": [],
  "see_also": []
}