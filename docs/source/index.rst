:html_theme.sidebar_secondary.remove:

Lucid 3.0
=========

.. raw:: html

   <div class="lucid-hero">
     <h1 class="lucid-gradient-text">Lucid 3.0</h1>
     <p>
       A production-grade machine learning framework purpose-built for Apple Silicon.
       Native C++ engine dispatches to <strong>Accelerate</strong> on CPU and
       <strong>MLX / Metal</strong> on GPU — zero-copy, lazy, fast.
     </p>
   </div>

.. grid:: 3
   :gutter: 3
   :margin: 0 0 3 0

   .. grid-item-card:: Tensor Core
      :text-align: center

      High-performance multi-dimensional arrays backed by a C++ compute engine.
      Supports Accelerate (CPU) and Metal GPU with a single ``.to()`` call.

      .. button-ref:: api/tensor
         :color: primary
         :outline:
         :expand:

         Tensor API

   .. grid-item-card:: Autograd
      :text-align: center

      Reverse-mode automatic differentiation — dynamic graphs, checkpointing,
      anomaly detection, and custom ``Function`` nodes.

      .. button-ref:: api/autograd
         :color: primary
         :outline:
         :expand:

         Autograd API

   .. grid-item-card:: Metal GPU
      :text-align: center

      Zero-copy CPU ↔ Metal transfers via MLX lazy evaluation.
      Write ``.to('metal')`` — no driver setup needed on Apple Silicon.

      .. button-ref:: api/metal
         :color: primary
         :outline:
         :expand:

         Metal API

.. grid:: 3
   :gutter: 3
   :margin: 0 0 4 0

   .. grid-item-card:: Neural Networks
      :text-align: center

      Linear, Conv 1–3D, LSTM, GRU, Transformer, MultiHeadAttention,
      BatchNorm, LayerNorm, and more — all with ``state_dict`` I/O.

      .. button-ref:: api/nn
         :color: secondary
         :outline:
         :expand:

         nn API

   .. grid-item-card:: linalg · fft · einops
      :text-align: center

      SVD, QR, Cholesky, eigendecomposition via Accelerate LAPACK.
      FFT/rFFT via vDSP. Einsum / rearrange / reduce via C++ patterns.

      .. button-ref:: api/linalg
         :color: secondary
         :outline:
         :expand:

         linalg API

   .. grid-item-card:: C++ Engine
      :text-align: center

      Browse pybind11 binding headers, kernel primitives, and backend
      dispatch interfaces auto-generated by Exhale + Doxygen.

      .. button-ref:: cpp_api/index
         :color: secondary
         :outline:
         :expand:

         C++ API

----

Quick Example
-------------

.. code-block:: python
   :caption: 30-second end-to-end on Apple GPU

   import lucid
   import lucid.nn as nn
   import lucid.optim as optim

   class MLP(nn.Module):
       def __init__(self) -> None:
           super().__init__()
           self.fc1 = nn.Linear(784, 256)
           self.fc2 = nn.Linear(256, 10)

       def forward(self, x: lucid.Tensor) -> lucid.Tensor:
           return self.fc2(lucid.relu(self.fc1(x)))

   model = MLP().to("metal")                      # move to Apple GPU
   opt   = optim.Adam(model.parameters(), lr=1e-3)

   x = lucid.randn(32, 784, device="metal")
   y = lucid.randint(0, 10, shape=(32,), device="metal")

   loss = nn.CrossEntropyLoss()(model(x), y)
   loss.backward()
   opt.step()

   lucid.eval(loss)           # flush Metal lazy graph
   print(loss.item())

----

.. toctree::
   :maxdepth: 1
   :caption: Getting Started
   :hidden:

   getting_started/installation
   getting_started/quickstart

.. toctree::
   :maxdepth: 2
   :caption: API Reference
   :hidden:

   api/index

.. toctree::
   :maxdepth: 1
   :caption: C++ Engine
   :hidden:

   cpp_api/index

.. toctree::
   :maxdepth: 1
   :caption: Guides
   :hidden:

   guides/metal_device
   guides/autograd
   guides/performance

.. toctree::
   :maxdepth: 1
   :caption: About
   :hidden:

   changelog
