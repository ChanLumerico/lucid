# Lucid Performance Benchmarks

Phase 0.5 creates this directory as the stable home for continuous performance
tracking. Phase 8 will replace the placeholder with latency benchmarks against
Lucid CPU, Lucid GPU, PyTorch CPU, PyTorch MPS, and MLX-direct baselines.
