"""Diagnostics + trace inspection for :mod:`lucid.compile` (internal).

* :mod:`~lucid.compile._debug.diagnose` — ``diagnose`` / ``DiagnosisReport`` /
  ``OpInfo`` manual-VJP coverage diagnostics.
* :mod:`~lucid.compile._debug.trace_dump` — ``TraceGraph`` JSON serialisation
  for offline inspection (``LUCID_COMPILE_DEBUG=1``).
"""
