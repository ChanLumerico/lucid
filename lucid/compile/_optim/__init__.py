"""Optimizer-step compilation for :mod:`lucid.compile`.

* :mod:`~lucid.compile._optim.compiler` — ``compile_optimizer`` and the
  per-optimizer compiled update wrappers (in-place output path).
* :mod:`~lucid.compile._optim.spec` — single source of truth for each
  optimizer's compile-time hyperparameters + state schema.
"""
