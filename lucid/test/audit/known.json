{
  "comment": "Findings accepted as decisions rather than defects. Each key is 'axis::symbol'; the value is why it is accepted. Delete an entry to make the audit report it again. Add entries with --update-known, but write a real reason afterwards — an unexplained entry is indistinguishable from a bug someone silenced.",
  "accepted": {
    "nonfinite::lucid.sign": "sign(NaN) returns 0 rather than NaN. Consistent across cpu and metal, and the GPU path inherits MLX's own sign convention, so this is a convention Lucid has taken rather than an internal disagreement. Changing it would mean patching NaN on every GPU sign call and altering three internal consumers (L1 norm backward, abs backward, the relu mask). Reviewed 2026-08-02.",
    "nonfinite::Tensor.sign": "Same decision as nonfinite::lucid.sign — one op, two spellings."
  }
}
