"""Generators for the audit's checked-in reference data.

Everything here needs the reference framework and is therefore opt-in:
the audit itself runs without it and reads the generated JSON instead.
"""
